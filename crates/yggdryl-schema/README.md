# yggdryl-schema

The Arrow-compatible schema layer of **yggdryl** (`DataType` / `Field` and the
schema types), with conversion to and from Apache Arrow's `arrow-schema`.

> **Project reset.** A buildable scaffold; the schema types will be introduced
> around the Arrow-centralized design. See `CLAUDE.md` at the repository root for
> contributor rules.
