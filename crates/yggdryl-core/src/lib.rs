//! # yggdryl-core
//!
//! Blank slate. The previous implementation was removed in a project reset; the
//! core foundations will be reimplemented around an Arrow-centralized design.
//!
//! Contributor and agent rules live in `CLAUDE.md` at the repository root —
//! mirror the existing patterns, keep the Rust core and the two bindings in
//! lockstep, and make every value type serializable and hashable.
