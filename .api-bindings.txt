# Binding API inventory
#
# Maintained by hand against Python's public stubs/package exports and
# JavaScript's public declarations/runtime exports: there is no generator, so a
# change that adds or retires a public name edits this file too.
#
# `scripts/check_api_inventory.py` resolves every entry key against the tree
# its section's bracket names - a segment is a module beside its parent, or a
# name that parent binds - so a path this file keeps after a rename is
# reported rather than read.

### python: yggdryl  [python/yggdryl]
  FieldPath: __hash__, __len__, __reduce__, __repr__, __richcmp__, __str__, is_root, join, name, parent, root, segments, stable_hash
  TextEntries: __getitem__, __len__, __repr__, __str__, entry_by_path, get_entry_by_path
  TextEntry: __repr__, __richcmp__, __str__, entries, key, key_bytes, value, value_bytes
  TextLine: __init__(index, body: str | bytes | bytearray | memoryview, captures=None, options: TextOptions | None = None) - an empty body is a ValueError, because a line is the line it holds, __repr__, __richcmp__, __str__, body (the whole line, row header included), bodytype: MimeType, captures, crosscode: str, crosshashcode: int, crossuuid: Scalar (uuid), currhashcode: int, currunix: int, curruuid: Scalar (uuid), decoded_byte_size, dropped_byte_size, entries, entry_by_path, get_entry_by_path, index: int (writable physical row number; changing it rederives seqnum and curruuid), mtime: int | None (the captured instant, else the handle's modification time), remove_entry_by_path, seqnum: int (the full uint64 row number under start_rownum, else index), set_entry_by_path(path, value: str | bytes | bytearray | memoryview), sourceuri: Uri | None (the identifier the line was read under, as the narrowing its scheme names; what the cross code spells, where sourceurl answers where that identifier is - itself where it is a location, which a located read spells with one text, and where the name resolves to under the working directory where it is a name), sourceurl
    Rust-only: the TextLine Arrow converters media::text::into_arrow_batch, into_arrow_reader, from_arrow_batch, from_arrow_reader (no binding)
  TextLines: __iter__, __next__
  StringEnum: __contains__, __eq__, __iter__, __len__, __repr__, from_json, from_logical_name, get, get_member, insert, into_intenum, into_json, into_members, member_name, members, name, prebuilt, remove
  StringParameters: __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __init__(layout='string', charset=None, bound=None), __le__, __lt__, __ne__, __reduce__, __repr__, __str__, bound, charset, fixed, layout, max, stable_hash; frozen: the declaration of a string column - one of the eighteen leaves, layout answering the canonical leaf name - what DataType.string reads and DataType.string_parameters answers; a charset is accepted only beside a charset-free spelling (string, fixed_string, string_view, large_string, large_string_view, varchar, char, ...)
  BytesParameters: __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __init__(layout='binary', bound=None), __le__, __lt__, __ne__, __reduce__, __repr__, __str__, bound, fixed, layout, max, stable_hash; frozen: the declaration of a byte column, what DataType.bytes reads and DataType.bytes_parameters answers
  Bound: column_indices, columns, eval, evaluate_arrow_batch, explain, field, filter_arrow_batch, filter_arrow_reader, filter_mask_arrow_batch, is_predicate, matches, partition_split, reads_rows, schema, statistics_certainty, statistics_prune, term
  BoundSelector: apply_arrow_batch, apply_arrow_reader, apply_row, explain, is_all, is_identity, output, projections, schema
  DataType: PARSE_RECURSION_LIMIT, __arrow_c_schema__, __contains__, __iter__, __len__, arrow_scalar, ascii, ascii_packed, ascii_value, binary, binary_view, bytes, bytes_parameters, charset, code_name, code_width, crs, decimal, default_pyhint, default_scalar, dictionary_key, dictionary_value, duration32, duration64, edge_algorithm, equals, explode_fields, field, field_at, field_by_path, fixed_ascii, fixed_byte_width, fixed_size_binary, fixed_utf8, from_arrow, from_dict, from_fields, from_json, from_logical_name, from_pyhint, from_regex, from_str, from_toml, from_value, from_yaml, geography, geometry, get_field, get_field_at, get_field_by_path, has_default_crs, id, into_arrow, into_arrow_schema, into_dict, into_json, into_json_bytes, into_scheme_compat, into_toml, into_yaml, is_binary, is_bytes, is_code, is_decimal, is_default_value, is_floating, is_integer, is_nested, is_numeric, is_ordered, is_parameterized, is_signed_integer, is_string, is_temporal, is_unsigned_integer, is_wrapper, keys_sorted, kind, large_binary, large_utf8, logical_names, merge_with, pretty, remove_field, remove_field_at, remove_field_by_path, scalar, set_field, set_field_at, set_field_by_path, show_diff, show_diffs, stable_hash, string, string_parameters, time, timezone, union_mode, union_type_ids, unnest_fields, utf8, utf8_view, uuid_packed, uuid_value, validate, variant, with_fields
  Term: add, all, always_false, always_true, any, as_column, as_literal, at, between, bind, call, case, cast, check_budget, child, column, columns, compare, conjuncts, depth, divide, eq, explain, field, from_json, ge, glob, gt, ilike, into_json, is_always_false, is_always_true, is_in, is_literal, is_not_null, is_null, le, like, literal, lt, multiply, ne, negate, node_count, parameter, parameters, parse, path, remainder, simplify, slice, stable_hash, subtract, try_cast, typed_literal
  Filter: all, always_false, always_true, any, apply_arrow, apply_arrow_array, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_field, apply_records, bind, check_budget, columns, conjuncts, explain, from_json, into_json, is_always_false, is_always_true, parameters, parse, simplify, stable_hash, term
  Selector: __len__, all, all_except, apply_arrow, apply_arrow_array, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_field, apply_records, bind, check_budget, columns, excluded, explain, from_columns, from_field, from_json, has_star, into_field, into_json, is_all, is_columns, names, parameters, parse, projections, select, simplify, stable_hash, with_projection, without_columns
  Plan: apply_arrow, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_records, check_budget, columns, create_target, execute, explain, field, field_from, filter, from_field, from_json, into_expression, into_json, is_empty, is_identity, limit, merge_by, offset, ordering, parameters, parse, read_columns, read_sections, root_metadata, root_name, schema, selector, simplify, source, source_plan, stable_hash, verb, with_create, with_filter, with_limit, with_merge_by, with_offset, with_ordering, with_select, with_source, with_write, write_target
  Expression: apply_arrow, apply_arrow_array, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_field, apply_records, as_filter, as_plan, as_selector, check_budget, columns, explain, filter, from_json, into_json, is_filter, is_identity, is_plan, is_selector, is_sequence, kind, parameters, parse, plan, select, sequence, simplify, stable_hash, steps
  Records: __iter__, __next__, collect, field, from_arrow_reader, into_arrow_reader
  Field: __arrow_c_schema__, __contains__, __iter__, __len__, accept, accept_encoding, accept_language, accept_ranges, alias, apply_arrow_batch, apply_arrow_reader, apply_arrow_schema, arrow, arrow_scalar, assign_parquet_field_ids, az, cache_control, canonicalize_value, clear_properties, comment, content_disposition, content_encoding, content_language, content_length, content_location, content_range, content_type, default_pyhint, default_scalar, description, dictionary_id, dictionary_is_ordered, digest, digest_field_len, digest_field_names, digest_fields, display, dtype, equals, etag, expires, explode_fields, field, field_at, field_by_parquet_field_id, field_by_path, field_properties, file, fix, from_arrow, from_arrow_schema, from_dict, from_json, from_natural_value, from_pyhint, from_str, from_toml, from_value, from_yaml, get_field, get_field_at, get_field_by_path, get_property, glue, gs, has_partition_fields, has_property, http, http_location, iceberg, identity, index_of, into_arrow, into_arrow_schema, into_dataclass, into_dict, into_json, into_json_bytes, into_scheme_compat, into_toml, into_yaml, is_init, is_partition, is_struct, last_modified, layout_eq, location, max_parquet_field_id, media_type, merge_with, metadata, mime_type, mysql, name, nullable, only_digest_fields, only_partition_fields, pandas, parquet_field_id, partition, partition_field_len, partition_field_names, partition_fields, polars, postgres, postgresql, pretty, property_iter, protocol, python, range, remove_accept, remove_accept_encoding, remove_accept_language, remove_accept_ranges, remove_alias, remove_cache_control, remove_comment, remove_content_disposition, remove_content_encoding, remove_content_language, remove_content_length, remove_content_location, remove_content_range, remove_content_type, remove_description, remove_display, remove_etag, remove_expires, remove_field, remove_field_at, remove_field_by_path, remove_http_location, remove_last_modified, remove_location, remove_media_type, remove_mime_type, remove_parquet_field_id, remove_property, remove_range, remove_string_enum, remove_vary, require_struct, s3, scalar, set_accept, set_accept_encoding, set_accept_language, set_accept_ranges, set_alias, set_cache_control, set_comment, set_content_disposition, set_content_encoding, set_content_language, set_content_length, set_content_location, set_content_range, set_content_type, set_description, set_dictionary_options, set_display, set_dtype, set_etag, set_expires, set_field, set_field_at, set_field_by_path, set_http_location, set_init, set_last_modified, set_location, set_media_type, set_metadata, set_mime_type, set_name, set_nullable, set_parquet_field_id, set_partition, set_property, set_range, set_string_enum, set_vary, show_diff, show_diffs, spark, sql, stable_hash, stable_layout_hash, string_enum, transform, unnest_fields, urn, validate, validate_struct_root, validate_value, vary, with_partition_fields, without_fields, without_partition_fields
  IOBase (the base class; uri is the identifier a handle is addressed by and url that identifier when it names a location, while bound_uri and masked_uri are the bound filesystem's own spelling; construction answers the role subclass a name composes to, and buffered / into_text / into_coded / mkdir / create_dir answer the handle they built rather than self): __iter__, __len__, append, append_arrow_batch, append_arrow_reader, append_arrow_table, append_bytes, append_pandas, append_pandas_frame, append_polars, append_polars_frame, append_records, buffered, capacity, children_where, clear, close, closed, codec, column_size, compress_into, copy_into, create_dir, cursor, decompress_into, delete_dir, delete_dir_contents, delete_file, delete_root_dir_contents, exists, filesystem, flush, from_bytes, from_fs, from_uri, glob, info, into_coded, into_media, into_text, is_atomic, is_dir, is_file, is_io, is_tabular, iterdir, joinpath, kind, ls, bound_uri, masked_uri, media_type, merge_arrow_batch, merge_arrow_reader, merge_arrow_table, merge_pandas, merge_pandas_frame, merge_polars, merge_polars_frame, merge_records, mkdir, move_into, name, normalize_path, open, open_append_stream, open_input_file, open_input_stream, open_output_stream, opened, overwrite_arrow_batch, overwrite_arrow_reader, overwrite_arrow_table, overwrite_pandas, overwrite_pandas_frame, overwrite_polars, overwrite_polars_frame, overwrite_records, parent, partitions, path, pread_exact, pstream_bytes, pwrite, pwrite_all, read_arrow_field, read_arrow_reader, read_arrow, read_bytes, read_digest, read_pandas, read_pandas_frame, read_parquet_geospatial_statistics, read_parquet_null_count, read_parquet_split_offsets, read_parquet_statistics, read_polars, read_polars_frame, read_range, read_range_bytes, read_range_digest, read_records, read_scalar, read_text, record_options, remove, reserve, rglob, row_size, same_location, scan_arrow, scan_polars, size, touch, truncate, unlink, uri, url, write_arrow_batch, write_arrow_reader, write_arrow_table, write_arrow, write_bytes, write_pandas, write_pandas_frame, write_polars, write_polars_frame, write_records, write_scalar, write_text
    properties: every record read and write takes options=None and **properties, each set on a copy of the options - the ones given, or the handle's own - by its own setter; an Ellipsis value is skipped; read_arrow(*, options=None, **properties) and write_arrow(value, mode="overwrite", *, options=None, **properties) are the shape-free pair, a structured text document reading only the field off them
  Xxh32: algorithm, apply_arrow_batch, as_digest, as_int, clear, seed, write_bytes, write_reader, write_scalar
  Xxh64: algorithm, apply_arrow_batch, as_digest, as_int, clear, seed, write_bytes, write_reader, write_scalar
  Xxh3: algorithm, apply_arrow_batch, as_digest, as_int, clear, secret, seed, write_bytes, write_reader, write_scalar
  Xxh128: algorithm, apply_arrow_batch, as_digest, as_int, clear, secret, seed, write_bytes, write_reader, write_scalar
  Digest: __bytes__, __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __index__, __int__, __le__, __len__, __lt__, __ne__, __reduce__, __repr__, __str__, algorithm, bits, from_bytes, from_int, stable_hash, width
  MediaType: base, charset, clear_encodings, encoding, encodings, extension, extensions, from_content_headers, from_extension, from_extensions, from_file_name, from_json, from_parts, from_path, from_str, from_value, into_json, is_application, is_archive, is_audio, is_binary, is_encoded, is_encoding, is_font, is_haptics, is_image, is_io, is_message, is_model, is_multipart, is_structured, is_tabular, is_text, is_textual, is_video, push_encoding, set_base, set_charset, set_encodings, stable_hash
  ProtocolField: __contains__, __iter__, __len__, add_branch, algorithm, apply_arrow_batch, branches, class_metadata, class_name, clear, codeset, comment, component, counter, derivation, description, directions, display, field_ref, get, group, has_branch, id, identifiers, idmap, import_path, is_coupled, is_holder, items, key, keys, kind, merge_with, module, msgcat, msgtype, names, nulls, prefix, qualname, remove_algorithm, remove_role, remove_sources, remove_time, remove_unit, scheme, set, set_holder, sources, tag, tags, term, time, transform, unit, update, values
    FIX references: counter: int | None; component, field_ref, group, msgcat, msgtype: str | None; each is a read/write property and assignment of None removes it
    FIX tags: tag, tags and counter admit positive i32 tags only - 0 (what an unresolved arrival entry records) or a negative is the core's ValueError, written or read back from stored metadata
    FIX identifiers: identifiers: list[str], direct scalar member names in component order; assignment accepts Iterable[str] of names, aliases or decimal tags, resolved by the native setter; empty input removes the property; invalid, ambiguous or duplicate selection raises ValueError atomically
    FIX identifier maps: idmap: list[FixIdSource] (TypedDict map: str, key: str, follow: bool, role: str | None), the FIX:idmap entries in document order, [] when absent; assignable from any iterable of mappings, follow and role optional, an empty one removes the property, a key that is not 1 to 32 upper-case ASCII letters or digits, a follow flag off altids, a role that is not letters and digits, or one map and key twice is ValueError leaving the field unchanged
    FIX directions: directions: list[FixDirection] (TypedDict code: str, patterns: list[str]), the FIX:directions rules of tag 385's field in the dictionary's order, [] when absent; assignable from any iterable of mappings, an empty one removes the property, a code outside the field's set, a code named twice under any spelling or a pattern the regex crate refuses is ValueError leaving the field unchanged
    FIX derivation: derivation: str | None, the FIX:derivation term in its canonical text - how the field's value is derived from the message where the message states none, evaluated by the enriching pass to a fixpoint; assignable from text, parsed through the expression grammar and stored canonical, a text that is not a term or one past the budget is ValueError leaving the field unchanged; assigning None removes the property
  MimeType: ARROW_FILE, ARROW_STREAM, AVRO, BROTLI, BZIP2, CBOR, COMPRESS, CSS, CSV, DIRECTORY, DOC, DOCX, FILE, FIX, FIXML, FIXUL, FLAC, GIF, GZIP, HTML, JAVASCRIPT, JPEG, JSON, JSON_LINES, LZ4, MARKDOWN, MESSAGE_PACK, MP3, MP4, OCTET_STREAM, ODS, OGG, ORC, OTF, PARQUET, PDF, PLAIN_TEXT, PNG, PROTOBUF, PUFFIN, RAR, SEVEN_ZIP, SNAPPY, SQLITE3, SVG, TAR, TOML, TSV, TTF, ULLINK, WAV, WEBM, WEBP, WOFF, WOFF2, XLS, XLSX, XML, XZ, YAML, ZIP, ZLIB, ZSTD, content_coding, extension, format, from_content_coding, from_content_type, from_extension, from_json, from_local_path, from_path, from_str, from_value, into_json, is_application, is_archive, is_audio, is_binary, is_directory, is_encoding, is_filesystem, is_font, is_haptics, is_image, is_io, is_known, is_message, is_model, is_multipart, is_structured, is_tabular, is_text, is_textual, is_video, stable_hash, structured_suffix, subtype, top_level
  PythonMetadata: __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __le__, __lt__, __ne__, __reduce__, __repr__, __str__, class_name, from_type, import_path, is_importable, is_keyword_constructed, kind, module, properties, qualname, stable_hash
  RecordOptions: apply_arrow_batch, apply_arrow_reader, batch_row_size, block_codec, commit_row_size, compression, field, filter, for_media_type, key_value_metadata, level, limit_arrow_reader, max_byte_size, max_row_group_size, max_row_size, merge_by, mime_type, name, partition_pairs, plan, remove_field, require_field, safe, select, stable_hash, sync_marker, timezone, write_batch_row_size
    sections: field, filter, select, merge_by and plan setters take any value Scalar.from_ reads - a native value, text, a list of projections or conditions, a dict of aliases, None - and the core reads it with from_scalar
  TextOptions: apply_arrow_batch, apply_arrow_reader, autotype, batch_row_size, capture_names, commit_row_size, field, filter, framing, leading_fragment, level, limit_arrow_reader, linesep, lstrip, max_byte_size, max_record_byte_size, max_row_size, merge_by, mime_type, name, parse_mtime, partition_pairs, plan, remove_field, rename_columns, require_field, rowheader, rstrip, safe, select, source_field, stable_hash, start_rownum, timezone, write_batch_row_size
  Timezone: NAIVE, UTC, abbreviation_at, aliases, from_offset, from_value, into_local, into_utc, is_fixed, is_known, is_naive, is_saving_at, is_utc, key, observes_saving, offset_at, registered, stable_hash, standard_offset, utcoffset
  Uri: __contains__, __iter__, __len__, account, authority, bucket, clear_extensions, default_port, extension, extensions, file_name, fragment, from_json, from_parts, from_path, from_str, from_value, has_authority, host_port, hostname, into_arn, into_json, into_path, into_url, into_urn, is_storage, is_virtual_hosted, joinpath, key, locator, media_type, mime_type, parameters, parent, parents, parts, password, path, path_segments, path_text, port, query, region, remove_extension, scheme, set_extension, set_extensions, set_file_name, set_media_type, set_mime_type, set_query, set_stem, stable_hash, stem, store_endpoint, user, validate
    Url, Urn and Arn are Python subclasses of Uri: each holds no value of its own, reads the one canonical identifier the base holds, and inherits every member listed above. The lines below name only what each narrowing adds or overrides. Uri(value) and Uri.from_value(value) answer the narrowing the scheme names - a Url, a Urn, an Arn, or a Uri when the value is none of them - while from_str, from_path, from_parts and from_json answer Uri itself.
  Url: as_posix, as_uri, exists, from_json, from_path, from_str, from_uri, from_value, full_match, full_match_under, glob_parts, into_uri, is_absolute, is_dir, is_file, is_glob, is_local, is_partitioned, is_pattern, is_private, is_recursive_glob, is_relative_to, joinpath, local_mime_type, match, name, parent, parents, partition, partitions, partitions_under, relative_to, suffix, suffixes, with_name, with_partition, with_stem, with_suffix
    Url(value) is the location door and resolves a name - a Urn under the working directory, an Amazon S3 Arn to its s3: URL - the way it roots a bare path; Url.from_uri is the strict door and refuses one.
  Urn: clear_extensions, extension, extensions, file_name, from_json, from_str, from_uri, from_value, into_uri, locator, locator_path, media_type, mime_type, namespace, namespace_specific, remove_extension, resolve, set_extension, set_extensions, set_file_name, set_media_type, set_mime_type, set_query, set_stem, stem
    The filename accessors read the namespace-specific string rather than the whole path, and their setters leave the namespace alone. locator_path spells the relative path the name reads as, resolve(base) joins it onto a location, locator() joins it onto the working directory.
  Arn: account, bucket, clear_extensions, extension, extensions, file_name, from_json, from_parts, from_str, from_uri, from_value, into_uri, key, locator, media_type, mime_type, partition, region, remove_extension, resource, resource_id, resource_separator, resource_type, service, set_extension, set_extensions, set_file_name, set_media_type, set_mime_type, set_query, set_stem, stem, table
    The five AWS fields, and the resource read as a path by the filename accessors. from_parts takes partition, service, region, account, resource - the ARN's own parts, not the URI components Uri.from_parts takes. bucket is the container whichever store names one, key is Amazon S3 only and table Amazon S3 Tables only, and locator() answers the s3: or s3tables: URL those two positions spell, refusing every other service by name.
  Scalar: __bool__, __contains__, __iter__, __len__, absolute, add, as_bool, as_bytes, as_float, as_int, as_py, as_str, as_value_bytes, at, count, decimal, digest, divide, dtype, duration, family, from_, from_enum, from_struct, from_value_bytes, get, get_or, has, id, into_array_field, into_arrow_array, into_arrow_batch, into_arrow_scalar, into_arrow_table, into_field, into_json, into_json_bytes, into_struct_field, into_value_bytes, is_container, is_empty, is_integer, is_null, is_number, items, keys, kind, multiply, negate, path, remainder, remove, scale, set, stable_hash, subtract, unit, unscaled, values, zone
    from_(value): a pyarrow container, a pandas or polars frame or series, a numpy array, an Arrow C exporter or a ChunkedSerie lands as a serie holding the column it is (Serie.from_ reads it, a chunked value joined once), buffers shared, a stream drained and one pyarrow Scalar its row; as_py() is that list; a Term, Filter, Selector, Plan or Expression crosses as its canonical text
  ArrowCastPlan: __init__(source, target, *, safe=True, representation="value"), __repr__, apply, is_identity, preflight, representation, safe, source, target
    source is a Field, a pyarrow.Field, or a pyarrow.Schema read as the record root row; source answers the pyarrow.Field an input must lay out as. apply(serie) casts a Serie of that layout and answers a Serie; a pyarrow.RecordBatch or Array is first the column of its own layout, as Serie.from_arrow_batch / from_arrow_array read it with no field. A ChunkedSerie, a pyarrow.ChunkedArray or a pyarrow.Table is its chunks, as ChunkedSerie.from_ reads it with no field: the plan is applied to every chunk and the answer is a ChunkedSerie of as many chunks.
  Serie: __arrow_c_array__, __arrow_c_schema__, __contains__, __copy__, __deepcopy__, __delitem__, __getitem__, __iter__, __len__, __reduce__, __repr__, __richcmp__, __setitem__, as_py, cast, child, child_at, children, clear, dtype, empty, extend, extend_from_serie, field, from_, from_arrow_array, from_arrow_batch, from_arrow_reader, from_default, from_scalars, get, get_child_by_path, insert, into_arrow_array, into_arrow_batch, into_arrow_reader, into_arrow_scalar, into_arrow_table, into_numpy, into_pandas, into_polars, into_run, into_scalar, is_column, is_empty, is_null, items, null_count, pop, push, remove, resize, rows, scalar, set, set_cell, set_child, slice, splice, truncate, with_capacity
    from_arrow_array(array, field=None, *, safe=True, representation="value"), from_arrow_batch(batch, root=None, *, ...) and from_arrow_reader(reader, root=None, *, ...): no field is the input's own layout (an array named item, a batch or stream the record root row), buffers shared; a field casts, an exact layout sharing the buffers. from_default(field, rows=1) repeats the field's canonical default. cast(field, *, ...) takes a Field, or a DataType as its required column named value, and refuses a run. into_arrow_scalar() is the one row as a pyarrow.Scalar. The Arrow PyCapsule Interface: __arrow_c_schema__() and __arrow_c_array__(requested_schema=None) hand the column over as capsules, buffers shared - a record column under the exchange schema of the root SerieReader.root_of names (its metadata and dictionary-ID sidecar kept), any other column under its own field - and a StructSerie adds __arrow_c_stream__(requested_schema=None), the one-batch stream SerieReader.from_serie reads, so pa.array, pa.record_batch, pa.table, pa.RecordBatchReader.from_stream and polars read a Serie directly; a requested_schema capsule is imported by pyarrow and applied by the one cast (Serie.cast's default safe cast, so a value a nullable target cannot hold is null and a required one refuses it), and a run raises as into_arrow_array does. into_arrow_array, into_arrow_batch and into_arrow_reader cross as those capsules, never a raw address; every foreign array or batch is validated (ArrayData::validate_full) off the GIL before any row is read, and a malformed one is a ValueError. Whole drains, joins, casts and row-proving landings (from_arrow_array, from_arrow_batch, from_arrow_reader, from_scalars, from_, cast) run off the GIL; cast clones the column out of its borrow first, so another thread writing it waits for the GIL rather than finding it borrowed.
    from_(value, field=None, *, safe=True, representation="value") is the one ingestion door: a pyarrow Scalar (a one-row column named value) / Array / ChunkedArray (its chunks joined once by ChunkedSerie.into_serie) / RecordBatch / Table / RecordBatchReader / Dataset / Scanner, a pandas or polars frame or series, a numpy array (a record dtype is rows), an Arrow C data or stream exporter (a stream whose schema is no record is refused, naming ChunkedSerie.from_(pyarrow.chunked_array(obj))), a Serie (shared), a ChunkedSerie (joined by into_serie) or a SerieReader (taken) - a native value read as what it holds, never through its own capsule; a stream is drained into one column; any other value is read as a Scalar, a sequence as its rows and anything else as one row. into_arrow_table/into_pandas/into_polars read the column as one batch; into_numpy is pyarrow's own to_numpy(zero_copy_only=False).
  ChunkedSerie: __arrow_c_stream__, __contains__, __copy__, __deepcopy__, __getitem__, __iter__, __len__, __reduce__, __repr__, __richcmp__, as_py, cast, child, child_at, children, chunk, chunks, dtype, empty, field, from_, from_arrow_chunked_array, from_arrow_reader, from_serie, from_series, get, get_child_by_path, into_arrow_chunked_array, into_arrow_reader, into_arrow_table, into_numpy, into_pandas, into_polars, into_serie, is_empty, is_null, items, null_count, num_chunks, push_chunk, rows, scalar, slice
    Many Serie columns under one field, held apart: a pyarrow.ChunkedArray, or a pyarrow.Table of one batch per chunk. No constructor; mutable through push_chunk, so __hash__ is None. from_series(chunks, field=None, *, safe=True, representation="value") holds Serie columns - with no field one datatype in pieces, a chunk of another datatype refused naming it; with one, any chunk of another field cast into it; from_arrow_chunked_array(chunked, field=None, *, ...) takes a pyarrow.ChunkedArray, an Array or any iterable of arrays, each chunk crossing the C Data Interface with its buffers shared; from_arrow_reader(reader, root=None, *, ...) takes what a record stream is read from - a RecordBatchReader, Table, Dataset, Scanner, frame or iterable of batches - one chunk per batch. from_(value, field=None, *, ...) reads Serie.from_'s recognition ladder as chunks: a ChunkedArray is its chunks; a Table, RecordBatchReader, Dataset, Scanner or frame one chunk per batch; a RecordBatch, Array, pyarrow Scalar (a one-row column named value), series or numpy array one chunk; a Serie one chunk, shared; a SerieReader taken and drained, one chunk per batch; a ChunkedSerie shared, or cast when a field is given; any other value read as Serie.from_ reads it, one chunk.
    chunks hands each chunk out as its leaf class; rows cross as Scalar (__iter__, rows, get, __getitem__ with an int, negative from the end) and as Python values (as_py, a record as a dict), and a step-1 slice is a ChunkedSerie; child, child_at, children, items and get_child_by_path(path: str) are the same selection in every chunk; into_serie() is the one join, as its leaf class; cast(field, *, ...) is one plan over every chunk, a DataType its required column named value. into_arrow_chunked_array() answers one pyarrow array per chunk, buffers shared, typed by the field even with no chunk; into_arrow_reader() one batch per chunk and into_arrow_table() its read_all(); into_numpy is pyarrow's ChunkedArray.to_numpy(zero_copy_only=False). __arrow_c_stream__(requested_schema=None) is the PyCapsule stream, one array per chunk and buffers shared: a record's chunks as the batches SerieReader.from_chunked reads, any other field's as the column pyarrow.ChunkedArray streams; a requested_schema capsule is applied by the one cast. from_, from_arrow_chunked_array, from_arrow_reader, cast and into_serie run off the GIL, every foreign chunk validated before a row is read. Equality and order are the rows, against a ChunkedSerie or a Serie; repr is the ChunkedSerie.from_series call over each chunk's Serie repr and the field - rebuilding it wherever those do, which a record's rows, spelled as mappings, do not - and a pickle keeps the chunks and the field.
  SerieReader: __arrow_c_stream__, __iter__, __next__, __repr__, cast, field, from_, from_arrow_reader, from_chunked, from_serie, into_arrow_reader
    from_arrow_reader(reader, root=None, *, safe=True, representation="value") compiles one plan before a batch is pulled and yields one record Serie per batch; into_arrow_reader() hands the batches not yet pulled over as a pyarrow.RecordBatchReader under the root's exact exchange schema - one C stream capsule, pulled C to C off the GIL from any thread, a failure raised as the pyarrow exception of its Arrow category with the core's message - after which the reader is spent; __arrow_c_stream__(requested_schema=None) is the same stream as a PyCapsule, cast into a requested_schema capsule by SerieReader.cast first, and spends the reader too. from_(value, root=None, *, ...) shares Serie.from_'s columnar recognition; a held column, scalar or concrete container crosses through Serie.from_'s scalar boundary and becomes one held item. A Python sequence recognized as mapping records or columnar batches remains an incremental record stream, reusing its first batch import; an iterator or generic reusable iterable remains an incremental record-row stream, and resolving either schema may pull its first item. A non-record held value is wrapped as one record child before root is applied; a root over a held column, held chunks or a native reader casts what is held by one plan (SerieReader.cast: a root equal to the reader's own hands it back), never re-exporting and re-landing it. from_serie(serie) is one held column as that stream - a record column the batch it is, any other the one child of a row - refusing a record column with an absent row, and compiling no plan; from_chunked(chunked) is a held ChunkedSerie as the stream of its chunks by the same rule, one record column per chunk, and from_ reads a ChunkedSerie so. cast(field, *, safe=True, representation="value") spends the reader and answers a new one yielding every record cast into field by one plan - a Field, or a DataType as its required column named value - the options and the target resolved before the reader is taken so a refused one leaves it usable; held records are cast at the call and refused there, a stream's batches as they are pulled, and into_arrow_reader afterwards hands batches on under the cast schema. IOBase.read_arrow answers a SerieReader and IOBase.write_arrow takes anything SerieReader.from_ reads.
  Bounds: column, from_partitions, with_column
  Digester: algorithm, apply_arrow_batch, as_digest, as_int, clear, write_bytes, write_reader, write_scalar
  TxHash: __bytes__, __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __le__, __len__, __lt__, __ne__, __reduce__, __repr__, __str__, algorithm, digest, dtype, from_bytes, from_parts, into_datetime, into_scalar, into_sequenced_uuid, into_uuid, stable_hash, unit, unix, width, with_unit
    into_uuid() -> Scalar: the lossless RFC 9562 UUIDv7 projection, with the Unix millisecond first, the microsecond within it in rand_a's high ten, then the digest stored whole - its top two bits above the variant and its low sixty-two in rand_b; it takes no arguments and hashes nothing; every digest bit survives, so two values project to one identifier only where their microsecond and all 64 digest bits agree; neither unit nor algorithm survives, and sub-microsecond time cannot be recovered; returned as a uuid-typed Scalar because the binding has no Uuid wrapper class - as_py() answers the hyphenated str; a digest that is not 64 bits wide, a microsecond restatement overflow, or an instant a UUIDv7 cannot hold, is ValueError
    into_sequenced_uuid(sequence: int, seed: int) -> Scalar: the event-oriented UUIDv7 projection, with Unix milliseconds first, sequence.min(4095) in the ordered twelve-bit lane, and the low 62 bits of XXH3 over the digest and whole u64 sequence with the u64 seed; returned as a uuid-typed Scalar; an integer outside u64 is OverflowError, while a non-64-bit digest, clock-restatement failure or invalid UUIDv7 timestamp is ValueError
  TxHasher: algorithm, apply_arrow_batch, column_txhashes, digest, digest_scalar, dtype, from_state, row_txhashes, unit, unix_of, width
  Version: Version(major: int, minor: int = 0, patch: int = 0); from_str(value: str) -> Version; readonly major/minor/patch: int; stable_hash() -> int; numeric comparison, hash, copy, deepcopy, pickle (major, minor, patch); widths u8/u8/u16, exactly four native bytes

  expression: COMPARISONS, FUNCTIONS, VERBS, Bound, Bounds, BoundSelector, Expression, Filter, Plan, Records, Selector, Term, UserFunction, needs_quoting, unregister_user_function, user_defined_filter, user_defined_function, user_function_signature, user_functions
    user functions: @user_defined_function(namespace="py", name=None, parameters=None, returns=None, vectorized=False) and @user_defined_filter register a callable as namespace.name from its type hints and defaults; the wrapper stays callable and answers term(*args), where(*args), signature (the struct Field), unregister(); Term.call takes the qualified name
    native: register_user_function(namespace, name, parameters, returns, callable, *, defaults=None, vectorized=False) -> Field, unregister_user_function(name) -> bool, user_functions() -> list[str], user_function_signature(name) -> Field | None
  root constants: AVRO_MAX_SCHEMA_DEPTH, DEFAULT_FETCH_BYTE_SIZE, DEFAULT_STREAM_BATCH_SIZE, IPC_DICTIONARY_IDS_KEY
  __version__: (module or value)
  charset: CHARSETS, bom, canonical_name, decode, decode_lossy, encode, from_bom
  enums: AsciiCode, CFI, CHARSETS, CODECS, COMPATIBILITY_SCHEMES, CfiCode, Country, CountryCode, Ccy, CcyCode, DATA_TYPE_IDS, DATA_TYPE_KINDS, DIGEST_ALGORITHMS, EDGE_ALGORITHMS, EVENT_COLUMNS, FORMATS, IO_KINDS, IO_MODES, IO_WRITE_MODES, LEADING_FRAGMENTS, LEVELS, MARKET_COLUMNS, MARKET_KINDS, MARKET_VIEWS, MD_UPDATE_ACTIONS, MIC, MicCode, OPERATION_COLUMNS, PYTHON_KINDS, REPRESENTATIONS, TIME_UNITS, UNION_MODES, fixed_ascii
  enums.AsciiCode: the base of the fixed US-ASCII widths fixed_ascii(width) builds (at most sixteen bytes) and the four registered code bases (CountryCode, CcyCode, MicCode, CfiCode); lives in yggdryl.enums.string, declares through StringEnum / Field.set_string_enum
    classmethods: as_enum, dtype, from_code, from_field, from_str, into_field
    instance: into_str
    protocol: an open IntEnum whose member is its packed ASCII value and whose str/format is that value's text; a subclass body is the caller's vocabulary, so a member spelled as any of the seven names listed above is refused at class creation rather than shadowing it, and a stored declaration naming one is a ValueError
  enums.Country / enums.Ccy / enums.MIC / enums.CFI: the shipped vocabularies, over CountryCode, CcyCode, MicCode, and CfiCode
  enums.MARKET_KINDS / enums.MARKET_VIEWS / enums.MD_UPDATE_ACTIONS / enums.EVENT_COLUMNS / enums.MARKET_COLUMNS / enums.OPERATION_COLUMNS: the graph vocabulary's own listings - a MarketData.kinds spelling, a MarketData.plan view name (orders, quotes, executions, trades, book_sides, books, lifecycle), a BookRef.action spelling, and the sixteen/nineteen/eight column names EventColumn/MarketColumn/OperationColumn state, each in schema order
  scalar classes: into_field (cached staticmethod; the class's non-null Struct root, not the leaf conversion Scalar.into_field names)
  combined: (module or value)
  refresh_logging: drops the cached Python log levels, so a level set after import applies
  coding: Coded, Gzip, Identity, Zlib, Zstd; gzip, zlib and zstd are modules of their own at the root
  coding roles: Coded is the IOBase subclass presenting the decoded bytes of a coding; Identity, Gzip, Zlib and Zstd sit under it, and Codec.Deflate folds into Zlib. Coded: into_handle, level
  holder: Buffer, Buffered, FsFile, FsFolder, FsPath, IOBase, IOCursor, LocalFile, LocalFolder, LocalPath, S3File, S3Folder, S3Path
  holder roles: every name above but IOBase and IOCursor is an IOBase subclass naming one core holder; construction answers the composition a name declares, so type(handle) says which implementation answers the contract
    Buffer: in-memory bytes; built with IOBase.from_bytes
    LocalPath / LocalFile / LocalFolder: the local roles, each constructible from a location; LocalPath is what a name declaring nothing takes, and what addresses the stored bytes of a coded name
    LocalFolder: classmethods temporary, home, config
    FsPath / FsFile / FsFolder: the same three roles on a foreign filesystem, each constructible as (filesystem, path, *, uri=None)
    S3Path / S3File / S3Folder: the same three roles on Amazon S3, Google Cloud Storage, and Azure Blob Storage, each constructible as (location) or as (container, key, provider=) for the raw name a store uses - a raw name, unlike a location, does not say which store holds it - with an optional options= mapping in PyIceberg's s3./gcs./adls. names, PyArrow's, or each store's own environment names
    Buffered: cached_bytes, cached_pages, has_cached_page, clear_cache, into_handle, and the normalized
      configuration the core clamped to: page_size, max_bytes, ttl, page_index, page_start
  media: Avro, DEFAULT_RECORD_BATCH_ROW_SIZE, Ipc, Media, NULL_PARTITION, Parquet, RecordOptions, Text, partition_text, with_partitions, without_partitions; what every medium shares - avro and iceberg are modules of their own at the root
  media roles: Media is the IOBase subclass retaining a record implementation, with Ipc, Parquet and Avro under it; Text is its own holder variant and sits directly under IOBase. Media, Text: into_handle
  text: TextEntries, TextEntry, TextLine, TextLines, TextOptions, codec; json, toml, yaml and xml are modules of their own at the root
  IOBase text decode: read_text_lines(*, options) answers a lazy TextLines iterator; every record method routes through the same decode
  field: pure Field builder for native/Arrow/dataclass shapes, with optional name
  root types: ArrowCastPlan, BigDecimalField, BloombergCodeField, FIGICodeField, BooleanField, BytesField, BytesParameters, CfiCodeField, ChunkedSerie, CountryField, CcyField, CusipCodeField, DataType, Date32Field, Date64Field, DateTime64Field, Decimal128Field, Decimal256Field, Decimal32Field, Decimal64Field, DecimalField, DecimalWidthField, DenseUnionField, DictionaryField, Duration32Field, Duration64Field, Field, FieldPath, FixedSizeSerieField, FixedSizeSerieSerie, Float16Field, Float32Field, Float64Field, GeographyField, GeometryField, Int16Field, Int32Field, Int64Field, Int8Field, IntervalField, IsinCodeField, LargeSerieField, LargeSerieSerie, LargeSerieViewField, LargeSerieViewSerie, MapField, MapSerie, MicCodeField, NullField, ProtocolField, PythonMetadata, RunEndEncodedField, Scalar, SedolCodeField, Serie, SerieField, SerieReader, SerieSerie, SerieViewField, SerieViewSerie, SideField, StateField, StringEnum, StringField, StringParameters, StructField, StructSerie, Time32Field, Time64Field, TimeField, TimeInForceField, Timezone, UnitField, TypedDataType, TypedField, UInt16Field, UInt32Field, UInt64Field, UInt8Field, UnionField, UrlField, UrnField, UuidField, VariantField, Version, VersionField, ascii, ascii_view, bigdecimal, binary, binary_view, bloomberg, boolean, bytes, cfi, country, cp1252, cp1252_view, ccy, cusip, date32, date64, datetime64, decimal, decimal128, decimal256, decimal32, decimal64, dense_union, dictionary, duration32, duration64, field, fixed_ascii, fixed_cp1252, fixed_size_binary, fixed_size_serie, fixed_utf8, figi, float16, float32, float64, geography, geometry, int16, int32, int64, int8, interval, isin, large_ascii, large_ascii_view, large_binary, large_binary_view, large_cp1252, large_cp1252_view, large_serie, large_serie_view, large_utf8, large_utf8_view, map, map_of, mic, null, run_end_encoded, scalar, sedol, serie, serie_view, side, sized_ascii, sized_binary, sized_cp1252, sized_utf8, state, string, struct, time, time32, time64, timeinforce, uint16, unit, uint32, uint64, uint8, union, url, urn, utf8, utf8_view, uuid, variant, version; re-exported from `yggdryl` itself, and each type is also a module of its own at the root - yggdryl.integer, yggdryl.string, yggdryl.temporal and the rest
  VersionField: TypedField[Literal["version"], Version]; version(name: str, *, nullable: bool = True, metadata: MetadataInput = None) -> VersionField; scalar conversion uses the existing Field/Scalar boundary
  UrlField: TypedField[Literal["url"], str]; url(name: str, *, nullable: bool = True, metadata: MetadataInput = None) -> UrlField; UrnField: TypedField[Literal["urn"], str]; urn(name: str, *, nullable: bool = True, metadata: MetadataInput = None) -> UrnField; the two leaves of the uri family; a str in, the canonical text out through as_py
  StringField: TypedField[Literal["utf8", "large_utf8", "utf8_view", "large_utf8_view", "fixed_utf8", "sized_utf8", "ascii", "large_ascii", "ascii_view", "large_ascii_view", "fixed_ascii", "sized_ascii", "cp1252", "large_cp1252", "cp1252_view", "large_cp1252_view", "fixed_cp1252", "sized_cp1252"], str]; string(name, *, layout="string", charset=None, fixed=None, max=None, nullable=True, metadata=None) -> StringField (fixed and max together: TypeError; a charset only beside a charset-free layout); one factory per leaf: utf8/large_utf8/utf8_view/large_utf8_view/ascii/large_ascii/ascii_view/large_ascii_view/cp1252/large_cp1252/cp1252_view/large_cp1252_view(name, *, nullable, metadata), fixed_utf8/fixed_ascii/fixed_cp1252(name, width, *, nullable, metadata), sized_utf8/sized_ascii/sized_cp1252(name, max, *, nullable, metadata)
  BytesField: TypedField[Literal["binary", "large_binary", "binary_view", "large_binary_view", "fixed_binary", "sized_binary"], bytes]; bytes(name, *, layout="binary", fixed=None, max=None, nullable=True, metadata=None) -> BytesField (fixed and max together: TypeError); binary/large_binary/binary_view(name, *, nullable, metadata) are it with a layout picked once; fixed_size_binary(name, byte_width, *, nullable, metadata) the fixed layout
  fix: FixCapture, FixCodec, FixHeader, FixMessages, FixMsg, FixRegistry, MsgType, fix_crate_fields, fix_schema, fix_schema_carrying, fix_schema_tags, global_registry, install_global_registry
  fix constants
    ULBRIDGE_ROWHEADER: str, the row header a ULBridge log writes in front of every line, as a rowheader for a text read - the crate's own text rather than a second copy of it; msgsessionid, msgctxid, msgseqnum and msgpluginid are named for the fields they fill, and timestamp, msgthreadid and level name none and are the capture's own columns, so the header dates neither its line nor its message - a message stating no SendingTime(52) is read against its line's currunix, through this header the handle's time where it has one
  fix.FixRegistry
    statics: from_fields(fields: Iterable[FieldLike]) -> FixRegistry; from_handle(location: IOBase | Url | str | PathLike[str]) -> FixRegistry; from_cfb_file(location: IOBase | Url | str | PathLike[str], dialect: str | None = None) -> tuple[FixRegistry, list[Field]], the dialect stamped as FIX:branches on every field and definition the file produces; from_json(document: str) -> FixRegistry
    mutations: add_field(field: FieldLike) -> bool; add_fields(fields: Iterable[FieldLike]) -> tuple[int, int]; merge_with(other: FixRegistry) -> tuple[int, int]; add_cfb_file(location: IOBase | Url | str | PathLike[str], dialect: str | None = None) -> tuple[int, int], the dialect or the location's stem; add_cfb_files(location: IOBase | Url | str | PathLike[str], pattern: str, dialect: str | None = None) -> tuple[int, int, int], (files, added, merged) over the core glob walk; add_json_file(location: IOBase | Url | str | PathLike[str]) -> tuple[int, int]; insert(field: FieldLike) -> Field | None; update(field: FieldLike) -> None; remove(key: int | str) -> Field | None; remove_by_id(id: int) -> Field | None
    one namespace: insert files a definition by the shape it has - a Struct is a component, a Serie of Structs or a Map a group, a message a component carrying FIX:msgtype - and there is no category argument and no definition door of its own: insert/update/remove and the field_by_* pairs reach scalar fields, components and groups alike, a definition answering the tag door under the identity the catalog derived for it
    fields: get_field(key: int | str) -> Field | None; field(key: int | str) -> Field; get_field_by_id(id: int) -> Field | None; field_by_id(id: int) -> Field; get_field_by_tag(tag: int) -> Field | None; field_by_tag(tag: int) -> Field; get_field_by_counter(tag: int) -> Field | None; field_by_counter(tag: int) -> Field, the repeating group one counter tag opens (two groups on one counter name nothing); get_field_by_name(name: str) -> Field | None; field_by_name(name: str) -> Field, a scalar before a component before a group; get_field_by_path(path: str | FieldPath) -> Field | None; field_by_path(path: str | FieldPath) -> Field
    messages: register_msgtype(spelling: str, name: str | None = None, description: str | None = None) -> MsgType; get_msgtype(spelling: str) -> MsgType | None; msgtype(spelling: str) -> MsgType, an exact wire code, then a folded canonical name, then an alias of tag 35's code set
    membership: dialects() -> list[str], the distinct FIX:branches names any field or definition carries, sorted; membership is provenance a caller filters on and no lookup consults it
    code sets: one vocabulary is named once and read by as many fields as name it, so the members live here and a field's FIX:codeset states the name; a dictionary refuses a field naming a set it does not hold, at insert, update, from_fields, from_json and store load alike; `msgcatcodeset` is intrinsic and set/merge/remove may not change its stable integer market-operation IDs. Python: get_codeset(name: str) -> list[FixCode] | None; codeset(name: str) -> list[FixCode]; codeset_of(field: FieldLike) -> list[FixCode] | None; codeset_names() -> list[str], the names held; idmap_sources() -> list[FixIdMapSource] (FixIdSource plus tag: int), every field's FIX:idmap entries beside its tag, in tag order; set_codeset(name: str, codes: Iterable[Mapping[str, object]]) -> None, an empty list removing an ordinary set; merge_codeset(name: str, codes: Iterable[Mapping[str, object]]) -> None, folded by wire value, the reading the dictionary holds winning a shared one and every spelling either side declared kept as an alias; remove_codeset(name: str) -> list[FixCode] | None, refused while a held field reads by it. JavaScript: getCodeset(name)/codeset(name)/codesetOf(field) -> {name, codes} | null; codesetNames() -> string[]; idmapSources() -> {tag, map, key, follow, role?}[], every field's FIX:idmap entries beside its tag, in tag order; codeName(name, value)/codeValue(name, text) -> string | null; setCodeset(name, codes)/mergeCodeset(name, codes)/removeCodeset(name)
    snapshots: into_json() -> str; stable_hash() -> int; write_into(location: IOBase | Url | str | PathLike[str]) -> None, `commit` with its report discarded; commit(location: IOBase | Url | str | PathLike[str]) -> dict[str, Any] naming `written`/`removed` and counting `skipped`, each document digested where it lies so a second commit of one registry writes nothing, shards named by their tag's hundred nine digits wide (tag 55 -> fields/000000000.json, tag 65003 -> fields/000000650.json) beside components/, groups/ and codesets/ - one <name>.json per code set, stating its name and its members - the crate's own dump among them as components/fixmsg.json and groups/metadata.json; __copy__()/__deepcopy__(memo: Any) -> FixRegistry; __reduce__() -> tuple[object, tuple[str]]
    keys: an int tag fits i32 and excludes bool; a str is a name/dotted path; a counter is the tag of the int32 counting a group's occurrences; bare queries use the core deterministic match - canonical tag before alternate, canonical name before alias, one fold; an identifier is the int field.fix.id answers and is read only by the *_by_id methods (bool refused, outside i32 is OverflowError); absent required lookup raises KeyError, invalid mutation raises ValueError
    contents: every registry holds the crate's own definitions from construction - fix_crate_fields() in tag order from 65003, the Map group metadata (65049) among them - and seeds the standard clocks SendingTime (52) and TransactTime (60) beside them; len() counts the scalar fields, the components and the groups, while __iter__ walks the scalars alone
    protocol: mutable and unhashable; equality includes every definition; __len__/__bool__/__contains__/__getitem__ operate on the one namespace; __iter__ -> FixFieldIterator lazily follows canonical identifiers, tag-major then id; mutations refuse while a message, a MsgType view, an unfinished iterator or the process default shares the registry
  fix.MsgType
    readonly: name: str (the folded canonical name), value: str (the exact wire code), msgcat: str | None (symbolic four-character `FIX:msgcat` metadata; `FixMsg.msgcat` is its resolved `int32`), field: Field (read-only); get_group_by_tag(tag: int) -> Field | None; stable_hash() -> int
    selection: identifier_values(message: FixMsg) -> list[tuple[Field, Scalar]], the compiled native selection of non-null direct members in declaration order; declaration Fields are read-only, Scalars retain their native types; no nested-group traversal
    protocol: an immutable copy of the definition beside the registry it came from, which it keeps shared; native equality/order/hash, __str__() -> str, copy/deepcopy -> MsgType, __reduce__() -> tuple[object, tuple[str, str]] carrying the registry snapshot and the definition's name
  native FIX iterators
    FixFieldIterator: Iterator[Field]; FixMsgIterator: Iterator[tuple[str, Scalar]]; FixMessages: Iterator[FixMsg]; each returns self from __iter__, yields one native item from __next__, retains bounded native cursor state and is unhashable
    FixMessages is what every FixCodec stage answers - one line's messages, none, one or many a row, lines or records parsed, messages chained by the lifecycle, a batch read back; a Python iterable behind it is pulled one item at a time; a refused line raises ValueError where it is met and the stream continues, a failure of the iterable raises as itself and ends it
  fix.FixMsg
    constructor: FixMsg(field: FieldLike, value: Scalar | Mapping[str, Any] | Sequence[Any], registry: FixRegistry | None = None); a child stating a typed fact fills the holder that owns it and leaves the row, and SendingTime is the stated one else UTC now
    statics: from_row(schema: FieldLike, row: Scalar | Mapping[str, Any] | Sequence[Any], registry: FixRegistry | None = None) -> FixMsg, the inverse of into_row with the content rebuilt from the fixentries column and no clock read
    readonly: registry: FixRegistry, field: Field (the content row's schema), value: Scalar (the content row)
    holders: header() -> FixHeader (the frame, the trailer's signaturelength/signature/checksum included); capture() -> FixCapture; the FIX numbers and identifiers the message lifted are reached by their own tags - by_tag(44), by_tag(11) - and there is no lifted() view of them; msgcat: int | None (the stable int32 market-operation category, also exposed as marketoperationid); text: str | None (tag 58); metadata: dict[str, str] (what a bridge stated under its own namespaces, folded, sorted)
    graph facts, each the message's own property: curruuid: Scalar (uuid), crossuuid: Scalar (uuid), crosscode: str, currhashcode: int, crosshashcode: int, currunix: int (ns since the epoch, UTC), state: Scalar (state code), seqnum: int, prevuuid: Scalar | None, creaunix / execunix / recdunix / exprtime / prevunix / snapunix: int | None, srcuuids: list[Scalar] (sorted unique provenance; the text line it was parsed out of, none for bytes), carried: list[tuple[str, Scalar]] (the capture's own cells the message carries, each under its column; empty for bytes); the market facts price: Scalar | None, currency: Scalar, quantity: Scalar | None, unit: str (the spelling, empty where none), side: Scalar, securityids: dict[str, str] (one code under each source - ISIN, CUSIP, FIGI - in source order), cficode / miccode: Scalar | None, lastpx / lastqty / avgpx / cumqty / leavesqty / prevpx / prevqty / spotrate / forwardpoints: Scalar | None, the six FX lifts lastspotrate / lastforwardpoints / bidspotrate / bidforwardpoints / offerspotrate / offerforwardpoints: Scalar | None (FIX's own 194, 195, 188-191), anomalies: list[tuple[str, str]] (what the reading could not take as it stands, in arrival order), ticker: str | None; the operation facts marketoperationid: int | None, tif: str | None (the stored code), tradable: bool | None, accountids / userids / altids: dict[str, str] (each value under the field that stated it, in key order), bid / ask: graph.Lane | None
    market_operations() -> list[graph.MarketData], the typed graph leaves the message expands to: an order, a quote, an execution or an initial trade report is one; a book W or X one per NoMDEntries(268) occurrence, or one scoped snapshot control for an empty W; a message of any other type is a ValueError naming its MsgType
    typed tags: a lookup by a frame tag {8,35,49,56,34,52,43,385,93,89,10}, a crate tag (65000..65100) but sourceurl (65026), one of the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003} or 58 answers its holder, typed as its column is (by_tag(35) is text, by_tag(52) a nanosecond UTC datetime64, by_tag(44) an exact decimal128(38,18), a crate identity a uuid); every other key reaches the content row, Side(54) and Currency(15) among it
    writes: set(key: int | str, value: object) -> None, a typed key its holder (None clears it) and any other key the row, typed by the field the key resolves to, KeyError for a key reaching nothing, and ValueError for a refused value or for a key reaching the capture's own column - sourceurl - which no message holds; remove(key: int | str) -> Scalar | None, clearing a typed fact or removing a row child; every write settles the identity again - the cross code, the hash code and the two identities
    lookups: get_by_id(id: int) -> Scalar | None; by_id(id: int) -> Scalar; get_by_tag(tag: int) -> Scalar | None; by_tag(tag: int) -> Scalar; get_by_name(name: str) -> Scalar | None; by_name(name: str) -> Scalar; get_by_path(path: str | FieldPath) -> Scalar | None; by_path(path: str | FieldPath) -> Scalar; get(key: int | str, default: object = None, /) -> object; each answers an owned Scalar
    views: stable_hash() -> int; digest() -> bytes, sixteen big-endian bytes over the wire entries; entries() -> list[FixEntryTuple], the content row as a tree of (tag, name, value | None, entries) tuples - a group one tuple under its counter with the count as its value and an occurrence tuple per member set, a component a tuple with no value, tag 0 for a key no dictionary resolved, and never a typed fact; into_row(schema: FieldLike) -> Scalar; into_bytes(separator: int = 1) -> bytes; into_text(separator: str = "\x01") -> str, both emitting the header and the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003} in front of the content (SendingTime only when the message stated it) and the trailer 93, 89, 10 behind it, a coded fact as its wire code
    protocol: native facts+row equality/hash against the registry, and a hashed message is frozen - set/remove after hash() raise TypeError, a copy takes writes again; copy/deepcopy -> FixMsg; __reduce__() -> tuple[object, tuple[str, str, str]] carrying the row with the typed facts appended as their own columns, the value and the registry snapshot; __iter__ -> FixMsgIterator of (name, Scalar) pairs over the content row; __len__ counts its children
  fix.FixHeader
    readonly: beginstring: str, msgtype: str, sendercompid: str | None, targetcompid: str | None, msgseqnum: int | None, sendingtime: int (ns since the epoch, UTC, as the intake settled it), stated_sendingtime: bool (only a stated one goes back on the wire), possdupflag: bool | None, msgdirection: str | None, and the trailer's signaturelength: int | None, signature: bytes | None, checksum: str | None
    protocol: an immutable copy of the message's header; equality and hash over its facts, copy/deepcopy -> FixHeader, __repr__
  fix.FixCapture
    readonly: msgpluginid: str | None, msgctxid: str | None, msgsessionid: str | None, msgsesseventid: str | None - the four of MsgType, msgsessionid, msgctxid and MsgSeqNum joined by `:` with no length prefixes, `8:e7256476:9effef3e6a:1094`, where all four are stated, derived whenever the message settles; also by_tag(65065); msgoriginator: str | None, the plugin a bridge's log line names the message as coming through, also by_tag(65066); conversationid: str | None, a stated CONVERSATIONID else the line's `{conversationId: ...}`, also by_tag(65067)
    protocol: an immutable copy of what the line's own bridge row header stated about the capture it was written for. What a reader said about the line - the object it came out of - is the capture's own column, carried by the message and stated again by into_row; none of it is FIX content, an entry or wire. Where message type, session, context and sequence are present, `msgsesseventid` is the session event the message was delivered as, the key two observations of one delivery merge on; that capture-derived key is excluded from the FIX content identity; equality and hash over its facts, copy/deepcopy -> FixCapture, __repr__
  fix.FixCodec
    constructor: FixCodec(registry: FixRegistry | None = None, *, default_sending_time: Scalar | datetime.datetime | None = None, separator: int | None = None, payload_column: str = "body", capture_names: Sequence[str] | None = None, null_values: Sequence[str] | None = None, direction: str | None = None, batch_byte_size: int | None = None, batch_row_size: int | None = None, include_msgtypes: Sequence[str] | None = None, exclude_msgtypes: Sequence[str] | None = None, threads: int | None = None, snapshot_ns: int | None = None, official_time_delay_ms: int | None = None, market_metadata: bool = True); default_sending_time redirects to try_with_default_sending_time - a Scalar crosses as itself and must already be a nanosecond UTC datetime64, a datetime is cast once through that clock, any other layout is the core's ValueError; direction is any spelling of a code of tag 385's set; a pin is on the codec, a stage is a call
    readonly: registry: FixRegistry; default_sending_time: Scalar | None, the SendingTime a new message takes when neither it nor its carrier states one - a row cell reaching tag 52, else the currunix of the TextLine it was read out of - None reading UTC now once per such message; separator: int | None; payload_column: str; null_values: list[str]; direction: str | None; batch_byte_size: int, the core's 128 MiB when unstated; batch_row_size: int, the core's 32,768 when unstated, a batch closing on whichever bound it reaches first; threads: int, available CPUs when unstated, zero reading as one and one pulling lazily; line, message-row and write doors use 64-item chunks at lane depth two in input order, while Arrow capture parse doors hold at most one whole input batch per thread in flight in batch order; snapshot_ns: int | None, an epoch-aligned lifecycle grid where positive and disabled otherwise; official_time_delay_ms: int, how far from SendingTime(52) an official transaction clock may stand and still date the message, the core's one second when unstated: the parse takes the TransactTime(60) the message states, else the TrdRegTimestamp(769) of its TrdRegTimestamps(768) group whose TrdRegTimestampType(770) stamps the event or a hop it crossed, nearest the sending clock, and the sending clock dates the message where none stands that near; a nonpositive delay admits only a clock equal to it; include_msgtypes: list[str]; exclude_msgtypes: list[str], the core's Heartbeat, TestRequest and "unknown" when unstated, each read before a frame is built and spelled as a code or a name, and an empty exclude_msgtypes keeps every type; market_metadata: bool, whether a market operation the codec builds carries in its metadata what its message states that no typed column reads - on by default, part of the leaf's curruuid and currhashcode, honoured by market_operations, market_arrow_reader and book_arrow_reader; FixMsg.market_operations always carries it
    statics: infer_msgtype_bytes(body: bytes | bytearray | memoryview) -> bytes | None; infer_msgtype_text(body: str) -> str | None
    parsing, streams: parse_line(row: bytes | bytearray | memoryview) -> FixMessages; parse_lines(lines: Iterable[bytes | bytearray | memoryview]) -> FixMessages, lazy; parse_text_line(line: TextLine) -> FixMessages; parse_text_lines(lines: Iterable[TextLine]) -> FixMessages, lazy
    parsing, one message: parse_fix_line(body: bytes | bytearray | memoryview) -> FixMsg; parse_ullink_line(body: bytes | bytearray | memoryview) -> FixMsg; parse_fixml_line(body: bytes | bytearray | memoryview) -> FixMsg; parse_pairs(pairs: Sequence[tuple[str, str]]) -> FixMsg; each takes one frame, and a body holding a second is ValueError naming the byte that second opens at
    what a parse does: builds the message, lifts its typed facts onto their holders, explodes a nested XmlData into it, restates deprecated fields to their latest aliases, runs the dictionary's FIX:derivation rules, reads the identifier maps off the fields that state them, fills an order's lanes, then settles the identity - there is no enrich step and no enrich door
    chaining: lifecycle(messages: Iterable[FixMsg]) -> FixMessages, lazy, the one finite walk: messages are collected; raw observations with the same complete nonempty `(msgtype, msgsessionid, msgctxid, msgseqnum)` - one capture msgsesseventid - are coalesced first, the latest recdunix, a stated one before an unstated one, then currunix selecting the reference once over all of them, for a full FIX-content and graph merge: reference scalar conflicts lead, older facts fill absences, and repeating-group occurrences merge recursively at equal indexes in FIX tag order, while duplicate execunix and recdunix observations retain their earliest facts; the retained messages are sorted by instant, each stated as the one after the live message it follows under its cross identity, carrying prevuuid, prevunix, seqnum, the lifecycle's creaunix and latest execunix, plus a missing parentorderid from a different predecessor orderid and a missing clordid from the predecessor; stated links win, and the message is settled again around them; srcuuids never travel along a chain
    formatting: format_messages(messages: Iterable[FixMsg], field: FieldLike) -> list[Scalar], each message as the row one message field of the registry holds it, a column that field carries and the message does not lifted off its content record first
    Arrow twins, over pyarrow.RecordBatchReader / Table / RecordBatch / any Arrow C stream exporter, answering pyarrow.RecordBatchReader: parse_text_arrow_reader(source) -> capture batches to batches of FIX rows, carried columns leading, batches closed on the bytes each row lands as against batch_byte_size; with several threads, whole input batches are at most one job per thread in flight and answer in batch order; lifecycle_arrow_reader(source) -> the same schema in and out, the rows walked as one ordered lifecycle with the record untouched, each row's capture cells carried by its message through the walk's own ordering and stated again at their own columns; format_arrow_reader(source, field: FieldLike) -> the format twin, batches under field; messages(source) -> FixMessages, lazy: one thread holds one batch at a time, while several retain bounded 64-row chunks that can span source batches and yield source order; each row's own cells carry into its message, so an arrow_reader over them states the capture's own columns again; arrow_reader(schema: FieldLike, messages: Iterable[FixMsg]) -> pyarrow.RecordBatchReader; book_arrow_reader(messages: Iterable[FixMsg], snapshot_millis: int = 0, global_: bool = False) -> pyarrow.RecordBatchReader, sorted messages through native operations and per-symbol books (or one GLOBAL book), written as lifted graph.MarketData.field() rows of kind book_event, only an initial `35=AE` with `487` absent/New and `150` absent or execution-like (`F`, legacy `1`/`2`) projected natively from NoSides(552) into one composite Trade whose canonical children are flattened into the BookEvent execution list; non-New/cancel/correct/reverse/status AE remain named refusals; the native book boundary ignores administration, requests including AD/AQ, acknowledgements including AR, and non-executing EXEC, while preserving source errors and all malformed admitted records; standalone operation conversion remains strict; children state Side and side IDs but no TradeSideIndex, and anonymous sides use their canonical 128-bit occurrence-content digest rather than group index; a supplied snapshot rebases root and every child currunix while retaining child execunix, epoch-aligned snapshots are enabled when positive, and lifecycle enrichment remains explicit; no Python Trade wrapper or binding-side side expansion; write_arrow_reader(source, sink: IO[bytes]) -> int lines written, refused before a row when the fixentries column is absent
    market doors: market_operations(messages: Iterable[FixMsg]) -> graph.MarketDataRowIterator, the capture collected at the call, admitting what book_arrow_reader admits, each admitted message expanded as FixMsg.market_operations under market_metadata and the operations stably sorted by snapunix else currunix; an admitted message whose expansion is refused raises ValueError first, in source order, dropping only its own leaves, and an item that is not a FixMsg or the iterable's own failure ends the pull and raises as itself once the operations it reached are read; neither lifecycle nor a msgtype filter runs; market_arrow_reader(messages: Iterable[FixMsg]) -> pyarrow.RecordBatchReader, those operations as graph.MarketData.field() rows closing on batch_row_size and batch_byte_size, one refusal (an expansion, a bad item, the iterable's own failure) being the reader's only item; market_operations_arrow_reader(source) -> pyarrow.RecordBatchReader, its twin over batches of FIX rows, each row read as its own message, answering over rows no walk wrote what market_arrow_reader answers for their messages; a walked capture reaches the sorted door as messages - market_arrow_reader(codec.lifecycle(messages)) - never as the rows lifecycle_arrow_reader writes, whose walked facts (prevpx, prevqty, a carried side or ticker, an execution instant) are no cell of the row; a schema making no FIX row refused before a row is read exactly as lifecycle_arrow_reader refuses it
    protocol: copy/deepcopy preserves the shared registry; unhashable
  fix functions
    fix_schema(registry: FixRegistry | None = None, name: str = "fix") -> Field, the one fixed row a whole capture lands in - the crate's own columns in bands (when, which event, which message and session, the instrument, the order chain, the values, how it went, the groups, the frame), each spelled by the dictionary's folded canonical name with its tag on FIX:tag, one fixentries serie closing the row under the nofixentries that counts it; beginstring, currunix, creaunix, currhashcode, crosshashcode, curruuid and crossuuid are the non-null columns, and a column is read by name rather than by position
    fix_schema_carrying(carrier: FieldLike, read: FieldLike) -> Field, a capture's own columns in front, dropping a capture column whose folded name a FIX column already takes; fix_schema_tags() -> list[int]; fix_crate_fields() -> list[Field], what the crate adds beside the specification in tag order from 65003; global_registry() -> FixRegistry; install_global_registry(registry: FixRegistry) -> None
  graph: BookEvent, BookIterator, BookRef, BookSide, EventIterator, Execution, ExecutionEvent, Lane, MarketData, MarketDataRowIterator, Order, OrderEvent, Quote, QuoteEvent, SnapshotEvent, SnapshotPartition, TradeEvent, ENTRY_ID, ENTRY_REF_ID, FOLLOWED_ALTIDS, GLOBAL_SYMBOL; the typed market leaves and MarketData over every one of them, what FixMsg.market_operations() and FixCodec.market_operations() answer; nothing here resolves, folds, merges or validates a fact - a named fact is resolved by the column enums' own of_name, checked by its column's own field and stated through its column's own record, every other constructor redirects to the native core door named beside it, and a value crosses the boundary through the one generic scalar door (`Scalar.from_`/`Scalar` intake), which recognizes a `Lane` itself rather than a door branching on a key name
    shared facts (one set, answered by every class whose traits name them): element - curruuid / crossuuid: Scalar (uuid); crosscode: str; currhashcode / crosshashcode: int; srcuuids: list[Scalar]; event - currunix: int; state: Scalar; seqnum: int; creaunix / execunix / recdunix / exprtime / prevunix / snapunix: int | None; prevuuid: Scalar | None; is_execution: bool; market - price / quantity / lastpx / lastqty / avgpx / cumqty / leavesqty / prevpx / prevqty / spotrate / forwardpoints: Scalar | None; currency / side: Scalar; unit: str; securityids / metadata: dict[str, str]; cficode / miccode: Scalar | None; ticker: str | None; operation - marketoperationid: int | None; tif: str | None; tradable: bool | None; accountids / userids / altids: dict[str, str]; bid / ask: Lane | None
    shared verbs: with_previous(previous: Self) -> Self | None and merge_with(other: Self) -> Self | None (same class only; None where it cannot follow or nothing changes); is_after(other) / is_before(other) -> bool; restating(live: Self) -> Self on the dated classes
    shared protocol: immutable; native equality (the core's ==), hash by currhashcode, repr `<Class>(<curruuid>, ...)`, copy/deepcopy, pickle carrying the IPC bytes of the value's one MarketData.arrow_reader row and rebuilt through MarketData.from_arrow_reader
  graph.Order
    constructor: Order(**facts), each keyword a market or operation column name or crosscode or srcuuids, folded as the column enums read it, then finalized; Ellipsis skipped, None clears; an unknown name is refused naming the fact, a derived identity (curruuid, crossuuid, currhashcode, crosshashcode) refused as derived, any other event column refused as a clock, state or chain an undated element has none of
    readonly: the element, market and operation facts; kind: "order"
    at(unix: int) -> OrderEvent, the element dated and finalized
  graph.Quote
    as graph.Order, kind "quote"; at(unix: int) -> QuoteEvent
  graph.Execution
    as graph.Order, kind "execution"; at(unix: int) -> ExecutionEvent
  graph.OrderEvent
    constructor: OrderEvent(currunix: int, book: BookRef | None = ..., **facts), each keyword an event, market or operation column name, folded, then finalized; Ellipsis skipped, None clears; an unknown name refused naming the fact, a derived identity (curruuid, crossuuid, currhashcode, crosshashcode) refused as derived, currunix under a folded spelling refused as stated once positionally; book a BookRef else TypeError
    readonly: the element, event, market and operation facts; kind: "order"; book: BookRef | None; action: str | None; scope: str; is_full_snapshot: bool
    with_book(book: BookRef) -> OrderEvent, refinalized; into_element() -> Order, the clocks and book control dropped and finalized
  graph.QuoteEvent
    as graph.OrderEvent, kind "quote"; into_element() -> Quote
  graph.ExecutionEvent
    as graph.OrderEvent, kind "execution", is_execution always True; into_element() -> Execution
  graph.TradeEvent
    not directly instantiable; statics from_parts(root: OrderEvent | QuoteEvent | ExecutionEvent | TradeEvent | MarketData, executions: Sequence[ExecutionEvent]) -> TradeEvent, the root's facts copied; an undated root is a TypeError naming its kind
    readonly: the element, event, market and operation facts; executions: list[ExecutionEvent], in canonical side, cross code and identity order
  graph.Lane
    constructor: Lane(price=..., spotrate=..., forwardpoints=..., currency=..., quantity=..., unit=...), every slot Ellipsis by default (not given); None and not-given both read back None
    readonly: price / spotrate / forwardpoints / quantity: Scalar | None; currency: Scalar | None; unit: str | None
    is_stated() -> bool
    protocol: native equality/hash (over the lane's own record)/repr/copy/deepcopy/pickle
  graph.BookRef
    constructor: BookRef(action=..., scope=..., position=..., entry_px=..., entry_size=...), every slot Ellipsis by default; action reads through the MdUpdateAction vocabulary, refusing an unknown spelling; entry_px/entry_size cross the same Field.scalar door every other decimal slot in the graph binding does, so a float or an out-of-range value is refused exactly as Lane's and the leaves' are
    readonly: action: str | None; scope: str | None; position: int | None; entry_px / entry_size: Scalar | None
    is_stated() / is_range_delete() / is_partial() -> bool
    protocol: native equality/hash/repr/copy/deepcopy/pickle
  graph.SnapshotPartition
    constructor: SnapshotPartition(scope: str, symbol: str | None = None)
    readonly: scope: str; symbol: str | None
    protocol: native equality/order/hash/repr/copy/deepcopy/pickle, ordered by (symbol, scope) with None first
  graph.BookSide
    constructor: BookSide(side: str), read through the core Side vocabulary
    readonly: the element and market facts (side the side it holds); live / deltas: list[MarketData] (each an order or quote event); is_empty: bool; best_price / best_quantity: Scalar | None (the first priced level's; None for an empty side or one of unpriced entries only); limits: list[Scalar], one struct Scalar per level {price: Decimal | None, quantity: Decimal, uuids: list[str]}, best first, the limit of every unpriced entry last, uuids in live order
    __len__() -> int; with_operation(operation: OrderEvent | QuoteEvent | MarketData) -> BookSide, a new side with the operation atomically applied; an order or quote stating no price rests at the unpriced level after every priced one; depth(levels: int) -> Scalar | None, the exact sum of the first levels limits' quantities, the unpriced limit counted where reached, 0 for an empty side or levels=0, None only past decimal, a negative levels an OverflowError
  graph.BookEvent
    constructor: BookEvent(currunix: int, symbol: str), an empty book
    readonly: the element, event and market facts; bid / ask: BookSide; executions: list[ExecutionEvent]; snapshot_partitions: list[SnapshotPartition]; is_crossed: bool; is_locked: bool (both bests stated and equal); spread: Scalar | None (best ask less best bid, negative when crossed, None where a side states no best); bbo_midpoint / median_quantity: Scalar | None
    with_operations(operations: Iterable[leaf | MarketData]) -> BookEvent, a new book with one atomic group applied: order, quote, execution and trade events and snapshot controls fold, any other kind is refused by name, a non-leaf item is a TypeError naming its index; imbalance(levels: int) -> Scalar | None, (bid - ask) / (bid + ask) over the sides' depth(levels), 1 bid-only, -1 ask-only, None where the total is zero
  graph.SnapshotEvent
    not directly instantiable; statics snapshot(event: any dated leaf | MarketData, scope: str | None = None) -> SnapshotEvent, the event and market facts copied; an undated leaf is a TypeError naming its kind
    readonly: the element, event and market facts; book: BookRef, action always "snapshot"
  graph.MarketData
    constructor: MarketData(leaf), any of the ten leaves or a MarketData, through the core's own From; anything else a TypeError
    classattr: kinds: tuple[str, ...], every MarketKind spelling in declaration order (order, quote, execution, book_side, order_event, quote_event, execution_event, trade_event, book_event, snapshot_event)
    readonly: the element and market facts, delegated to the leaf; kind: str; is_event: bool; book: BookRef | None (the control of an operation event or a snapshot control)
    as_order() / as_quote() / as_execution() / as_book_side() / as_order_event() / as_quote_event() / as_execution_event() / as_trade_event() / as_book_event() / as_snapshot_event() -> the leaf or None; into_leaf() -> the leaf as its own class
    statics: field() -> Field, the lifted `marketdata` struct every leaf is written under; arrow_reader(items: Iterable[leaf | MarketData], batch_row_size: int | None = None, batch_byte_size: int | None = None) -> pyarrow.RecordBatchReader, pulling the items lazily, a bad item's TypeError and a source's own failure arriving as the reader's error; from_arrow_reader(reader) -> MarketDataRowIterator, tolerant: any subset of the lifted columns in any order, names folded, foreign columns ignored, a castable type cast once, a missing kind or a missing currunix on a dated row refused by name at its row; plan(view: str, lifts: Sequence[str | FieldPath] | None = (), *, crosscode: str | None = None) -> Plan, the one plan a named view (enums.MARKET_VIEWS, ASCII case-insensitive) is over a marketdata stream, each lift a FieldPath or its text (securityids['ISIN'] as isin) resolved once and appended as a projection, None no lifts as Node reads null; only lifecycle takes, and needs, a crosscode; a refused view or path a ValueError, a lift that is no path (or a bare str for lifts) a TypeError; apply_view(view: str, source, lifts: Sequence[str | FieldPath] | None = (), *, crosscode: str | None = None) -> pyarrow.RecordBatchReader, exactly plan(...).apply_arrow_reader(source) bound once, a lift naming a column the rows do not hold refused there
  graph.MarketDataRowIterator
    Iterator[MarketData], what MarketData.from_arrow_reader and FixCodec.market_operations answer: lazy; a core refusal raised at its item (from_arrow_reader fused after it); a Python source's own failure raised once the operations it reached are read; unhashable
  graph.BookIterator
    constructor: BookIterator(items: Iterable[leaf | MarketData], snapshot_millis: int = 0, global_: bool = False)
    readonly: global_: bool
    protocol: Iterator[BookEvent], unhashable, pulls its items lazily; a leaf a book does not fold is a ValueError naming its kind, a bad item's own TypeError and a source iterable's own failure both raise as themselves
  graph.EventIterator
    constructor: EventIterator(items: Iterable[leaf | MarketData], sorted: bool = True, snapshot_ns: int | None = None); sorted=False collects and sorts the whole source inside the constructor, so a source failure raises there rather than after a partial sorted walk looks complete
    readonly: snapshot_ns: int | None
    alive() -> list[MarketData], the elements still alive after what the walk has read so far
    protocol: Iterator[MarketData], unhashable, pulls its items lazily; the order, quote, execution and trade events walk and every other leaf is yielded unchanged, in place
  iceberg: Catalog, Compaction, DataFile, IcebergOptions, ManifestFile, Namespace, Namespaces, PartitionField, PartitionSpec, ScanPlan, SchemaUpdate, Snapshot, Table, Tables, assign_field_ids, can_promote, schema_from_json, schema_into_json
  scalar: decorator installing cached static into_field()
  avro
    exports: Block, BlockIterator, Container, Schema, blocks, dumps, dumps_single, loads, loads_single
    decode limits: max_depth, max_input_bytes, max_nodes; blocks is a fused lazy iterator
  avro.Schema
    statics: from_value
    instance: fingerprint, into_canonical_form, into_json, kind, stable_hash
  avro.Container
    instance: get, metadata, rows, schema, stable_hash
  avro.BlockIterator
    instance: get, metadata, schema, __iter__, __next__
  avro.Block
    instance: count, rows, size
    protocol: intentionally unhashable lazy block with resolution context
  iceberg.IcebergOptions
    instance: commit_max_backoff_ms, commit_min_backoff_ms, commit_retries, compact_after_commits, data_mime_type, read_parallel_min_file_size, read_parallel_min_files, read_parallelism, stable_hash, target_file_size, write_parallelism, write_staging
    protocol: native equality/order; hashing freezes all setters; copy/pickle unlock
  iceberg.Compaction
    instance: bytes_rewritten, files_after, files_before, stable_hash
  iceberg.PartitionField
    statics: from_json
    instance: field_id, into_json, name, source_id, stable_hash, transform
  iceberg.PartitionSpec
    statics: from_json, identity, unpartitioned
    instance: fields, into_json, is_unpartitioned, spec_id, stable_hash
  iceberg.Snapshot
    statics: from_json
    instance: into_json, manifest_list, operation, parent_snapshot_id, schema_id, sequence_number, snapshot_id, stable_hash, summary, timestamp_ms
  iceberg.ManifestFile
    instance: added_files_count, added_rows_count, added_snapshot_id, deleted_files_count, existing_files_count, existing_rows_count, is_data, length, partition_spec_id, path, sequence_number, stable_hash
  iceberg.DataFile
    instance: column_sizes, content, file_size_in_bytes, lower_bounds, mime_type, null_value_counts, partition, path, record_count, sort_order_id, split_offsets, stable_hash, upper_bounds, value_counts
  iceberg.ScanPlan
    instance: files_planned, files_skipped, manifests_read, manifests_skipped, record_count, stable_hash
  text.codec
    exports: from_io, from_stream, into_io, into_stream
    inference: explicit format -> source/destination suffix -> one native content sniff; anonymous output defaults to JSON
    parse options: field, cls, safe, errors, max_depth, max_input_bytes, max_nodes, max_documents
    output options: format, utf8 (into_io), indent; destination=None returns bytes or str, otherwise writes and returns None
  json
    exports: Buffer, Destination, Readable, Source, Writable, dump, dump_all, dumps, dumps_all, load, load_all, loads, loads_all
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes; dump_all requires a destination
    parse: load/loads/load_all/loads_all(source, *, cls=None, field=None, safe=True, errors="raise", max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None)
  toml
    exports: Buffer, Destination, Readable, Source, Writable, dump, dumps, load, loads
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes
    parse: load/loads(source, *, cls=None, field=None, safe=True, errors="raise", placeholders=None, environment=False, max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None)
  xml
    exports: Buffer, Destination, Readable, Source, Writable, dump, dumps, loads
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes; the value is the record naming the document element, `@name` an attribute, `#text` the element's own text, a list a repeated element, None the empty element
    parse: loads(source, *, cls=None, field=None, safe=True, errors="raise", placeholders=None, environment=False, max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None); a field types the root element's value, and a dataclass cls is that field when none is given
  yaml
    exports: Buffer, Destination, Readable, Source, Writable, dump, dump_all, dumps, dumps_all, load, load_all, loads, loads_all
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes; dump_all requires a destination
    parse: load/loads(source, *, cls=None, field=None, safe=True, errors="raise", placeholders=None, environment=False, max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None); load_all/loads_all use the same native limits
  xxhash
    exports: SECRET_MINIMUM_LENGTH, Digest, Digester, Xxh32, Xxh64, Xxh3, Xxh128, bits, column_digests, digest, is_secretable, is_seedable, row_digests, width, xxh32, xxh64, xxh3, xxh128
    input: str | bytes | bytearray | memoryview | any buffer; bytes and str are borrowed, every other buffer is read through one bounded window
    one-shot: xxh32(data, seed=0), xxh64(data, seed=0), xxh3(data, seed=0, secret=None), xxh128(data, seed=0, secret=None) -> int
    redirects: IOBase.read_digest / read_range_digest and Scalar.digest, algorithm defaulting to "xxh3-64"
    Rust-only: the reader/writer pair and the Hashed<H> handle
  txhash
    exports: DEFAULT_UNIT, UNIX_WIDTH, TxHash, TxHasher, column_txhashes, compose, decompose, digest, dtype, restate_unix, row_txhashes, txh32, txh64, txh3, txh128, unix_array, unix_now, unix_of, width
    instant: every unix argument is int | datetime | date | str | Scalar; an int is the count already, everything else crosses into a Scalar and takes the core's one intake, so a zoned datetime already counts from the epoch and a naive one reads as UTC
    one-shot: txh32(data, unix, seed=0), txh64(data, unix, seed=0), txh3(data, unix, seed=0, secret=None), txh128(data, unix, seed=0, secret=None) -> TxHash at microseconds
    columns: row_txhashes / column_txhashes / compose / decompose / unix_array take and answer pyarrow arrays, unit defaulting to "us" and the algorithm to "xxh3-64"
    redirects: field.digest.time / unit / is_coupled / remove_time / remove_unit on the digest view; TxHasher.apply_arrow_batch is Digester.apply_arrow_batch under the hasher's state
### javascript: yggdryl  [node]
  top-level
    statics: intoField
  field-class protocol
    statics: intoStructField (getter only; memoized; stored fields and methods rejected)
  Uri
    statics: from, fromPath, fromString, fromJSON
    instance: account, at, authority, bucket, clearExtensions, clone, compare, equals, extension, extensions, fileName, fragment, hostname, intoArn, intoPath, intoUrl, intoUrn, isVirtualHosted, joinPath, key, length, locator, mediaType, mimeType, password, path, pathSegments, query, region, removeExtension, scheme, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, storeEndpoint, toJSON, toString, user
    JavaScript has no class inheritance here, so `new Uri(...)` and `Uri.from(...)` answer a Uri and intoUrl / intoUrn / intoArn narrow it; the narrowing constructor is Python-only. locator answers the Url an identifier names, resolving a name.
  MediaType
    statics: from, fromContentHeaders, fromExtension, fromFileName, fromPath, fromString, fromJSON, fromParts, fromExtensions
    instance: at, base, charset, clearEncodings, clone, compare, contains, encoding, encodings, equals, extension, extensions, getEncoding, isApplication, isArchive, isAudio, isBinary, isEncoded, isEncoding, isFont, isHaptics, isImage, isIo, isMessage, isModel, isMultipart, isStructured, isTabular, isText, isTextual, isVideo, length, pushEncoding, setBase, setCharset, stableHash, toJSON, toString, setEncodings
  Url
    statics: from, fromPath, fromString, fromUri, fromJSON
    instance: account, asPosix, asUri, at, authority, bucket, clearExtensions, clone, compare, equals, exists, extension, extensions, fileName, fragment, fullMatch, hostname, intoPath, intoUri, isAbsolute, isDir, isFile, isGlob, isPrivate, isRelativeTo, isVirtualHosted, joinpath, key, length, locator, match, mediaType, mimeType, name, parent, parents, partition, partitions, parts, password, path, pathSegments, query, region, relativeTo, removeExtension, scheme, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, storeEndpoint, suffix, suffixes, toJSON, toString, user, withName, withStem, withSuffix
    The constructor and from are the location door and resolve a name; fromUri is the strict door and refuses one.
  DataType
    statics: from, fromString, fromRegex, time, string, bytes, utf8, largeUtf8, utf8View, ascii, fixedUtf8, fixedAscii, binary, largeBinary, binaryView, fixedSizeBinary, fromLogicalName, logicalNames, fromJSON, fromJSONBytes, fromFields, variant, geometry, geography, fromArrow
    instance: asciiPacked, asciiValue, bytesParameters, charset, clone, codeWidth, compare, contains, defaultJSHint, defaultJSValue, equals, explodeFields, field, fieldAt, fieldByPath, fixedByteWidth, get, getField, getFieldAt, getFieldByPath, id, intoSchemeCompat, keys, kind, length, mergeWith, nested, removeField, removeFieldAt, removeFieldByPath, setField, setFieldAt, setFieldByPath, scalar, showDiff, showDiffs, stableHash, stringParameters, toJSON, toJSONBytes, toString, unnestFields, values
    stringParameters / bytesParameters: plain objects {layout, charset?, bound?, fixed?, max?} where layout is the leaf's canonical name (= the datatype id) and a key is absent when the leaf gives no such reading; DataType.string / DataType.bytes read the same shape as input, with at most one of bound/fixed/max
  StringEnum
    statics: fromJson, fromLogicalName, memberName, prebuilt
    instance: clone, equals, get, getMember, insert, intoEnum, intoJson, intoMembers, length, members, name, remove, toString
  Term
    statics: all, alwaysFalse, alwaysTrue, any, call, column, fromJson, literal, parameter, parse, typedLiteral
    instance: add, and, asColumn, at, between, bind, cast, checkBudget, child, clone, columns, compare, comparison, conjuncts, depth, divide, eq, equals, explain, field, ge, glob, gt, ilike, intoJson, isAlwaysFalse, isAlwaysTrue, isIn, isLiteral, isNotNull, isNull, key, le, like, lt, multiply, ne, negate, nodeCount, not, or, parameters, remainder, simplify, slice, stableHash, subtract, toJSON, toString, tryCast
  Bound
    instance: columnIndices, columns, eval, explain, field, filterArrow, filterArrowBatch, filterArrowReader, filterArrowTable, isPredicate, matches, partitionSplit, readsRows, schema, term, toString
  Filter
    statics: all, alwaysFalse, alwaysTrue, any, fromJson, parse
    instance: and, applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyField, applyRecords, bind, checkBudget, clone, columns, compare, conjuncts, equals, explain, intoJson, isAlwaysFalse, isAlwaysTrue, not, or, parameters, simplify, stableHash, term, toJSON, toString
  Selector
    statics: all, allExcept, fromColumns, fromField, fromJson, parse
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyField, applyRecords, bind, checkBudget, clone, columns, compare, equals, excluded, explain, hasStar, intoField, intoJson, isAll, isColumns, length, names, parameters, projections, simplify, stableHash, toJSON, toString, withoutColumns, withProjection
    hasStar: the selector opens with * - it reads every stored column it does not exclude, whatever it appends after (select * exclude (a), upper(b) as c); isAll only for a bare * that excludes and appends nothing; names and length count the appended projections
  BoundSelector
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyRecords, applyRow, explain, isAll, isIdentity, output, projections, schema, toJSON, toString
  Plan
    statics: fromField, fromJson, parse
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyRecords, checkBudget, clone, columns, compare, createTarget, equals, execute, explain, field, fieldFrom, filter, intoExpression, intoJson, isEmpty, isIdentity, limit, mergeBy, offset, ordering, parameters, readColumns, readSections, rootMetadata, rootName, schema, selector, simplify, source, sourcePlan, stableHash, toJSON, toString, verb, withCreate, withFilter, withLimit, withMergeBy, withOffset, withOrdering, withSelect, withSource, withWrite, writeTarget
  Expression
    statics: filter, fromJson, parse, plan, select, sequence
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyField, applyRecords, asFilter, asPlan, asSelector, checkBudget, clone, columns, compare, equals, explain, intoJson, isFilter, isIdentity, isPlan, isSelector, isSequence, kind, parameters, simplify, stableHash, steps, toJSON, toString
  Records
    statics: fromArrowReader
    instance: collect, field, intoArrowReader, next, Symbol.iterator, toString
    inputs: every Either-typed parameter is spelled out in the typings - a Term | string operand, a Filter | Term | string predicate, a Selector | Term | string | Array<Term | string> selector, a Plan | Selector | Filter | Field | string plan; the loader widens bind parameters and the Arrow holders (BatchReader, Table, RecordBatch) and applyRecords rows, and Records iterates as JavaScript does
    user functions: Term.call takes a qualified namespace.name and the parser reads it; nothing registers in JavaScript, so a bind refuses the function by name
  Urn
    statics: from, fromString, fromUri, fromJSON
    instance: at, authority, clearExtensions, clone, compare, equals, extension, extensions, fileName, fragment, intoUri, length, locator, locatorPath, mediaType, mimeType, namespace, namespaceSpecific, path, pathSegments, query, removeExtension, resolve, scheme, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, toJSON, toString
    locatorPath spells the relative path the name reads as, resolve(base) joins it onto a location, locator() joins it onto the working directory.
  Arn
    statics: from, fromString, fromParts, fromUri, fromJSON
    instance: account, at, authority, bucket, clearExtensions, clone, compare, equals, extension, extensions, fileName, intoUri, key, length, locator, mediaType, mimeType, partition, path, pathSegments, region, removeExtension, resource, resourceId, resourceSeparator, resourceType, scheme, service, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, table, toJSON, toString
    The five AWS fields, and the resource read as a path by the filename accessors. fromParts takes partition, service, region, account, resource. bucket is the container whichever store names one, key is Amazon S3 only and table Amazon S3 Tables only, and locator() answers the s3: or s3tables: URL those two positions spell, refusing every other service by name.
  Field
    statics: from, fromString, fromJSON, fromJSONBytes, fromArrow
    instance: accept, acceptEncoding, acceptLanguage, acceptRanges, alias, arrow, az, cacheControl, clear, clearProperties, clone, comment, compare, contentDisposition, contentEncoding, contentLanguage, contentLength, contentLocation, contentRange, contentType, defaultJSHint, defaultJSValue, delete, dictionaryId, dictionaryIsOrdered, digest, digestFieldLen, digestFieldNames, digestFields, display, dtype, entries, equals, etag, expires, explodeFields, field, fieldAt, fieldByPath, fieldLen, fieldProperties, file, fix, get, getField, getFieldAt, getFieldByPath, getProperty, glue, gs, has, hasPartitionFields, hasProperty, http, httpLocation, iceberg, identity, indexOf, intoSchemeCompat, isPartition, keys, lastModified, location, mediaType, mergeWith, mimeType, mysql, name, nullable, onlyDigestFields, onlyPartitionFields, pandas, parquetFieldId, partition, partitionFieldLen, partitionFieldNames, partitionFields, polars, postgres, postgresql, propertyIter, protocol, python, range, removeAccept, removeAcceptEncoding, removeAcceptLanguage, removeAcceptRanges, removeAlias, removeCacheControl, removeComment, removeContentDisposition, removeContentEncoding, removeContentLanguage, removeContentLength, removeContentLocation, removeContentRange, removeContentType, removeDisplay, removeEtag, removeExpires, removeField, removeFieldAt, removeFieldByPath, removeHttpLocation, removeLastModified, removeLocation, removeMediaType, removeMimeType, removeParquetFieldId, removeProperty, removeRange, removeStringEnum, removeVary, s3, set, setAccept, setAcceptEncoding, setAcceptLanguage, setAcceptRanges, setAlias, setCacheControl, setComment, setContentDisposition, setContentEncoding, setContentLanguage, setContentLength, setContentLocation, setContentRange, setContentType, setDictionaryOptions, setDisplay, setDtype, setEtag, setExpires, setField, setFieldAt, setFieldByPath, setHttpLocation, setLastModified, setLocation, setMediaType, setMimeType, setName, setNullable, setParquetFieldId, setPartition, setProperty, setRange, setStringEnum, setVary, scalar, showDiff, showDiffs, size, spark, sql, stableHash, stringEnum, toJSON, toJSONBytes, toString, unnestFields, update, urn, values, vary, withPartition, withPartitionFields, withoutPartitionFields
  ProtocolField
    instance: addBranch, branches, clear, codeset, comment, delete, derivation, description, directions, display, entries, get, has, hasBranch, id, identifiers, idmap, key, keys, mergeWith, names, nulls, prefix, scheme, set, size, tag, tags, toJSON, toString, update, values
    typed fix vocabulary: branches, tag, tags, names, identifiers, description, nulls, directions, idmap, derivation are accessor pairs, id a read-only accessor (number | null, the i32 digest of the tag and the folded name, derived on every read), addBranch(name)/hasBranch(name) methods, all answered only by the `fix` view; every other scheme throws a TypeError naming its own scheme; branches crosses as string[] (sorted, lowercase, [] when absent; assigning [] removes FIX:branches); directions crosses as FixDirection[] ({code: string, patterns: string[]}, [] when absent; assigning [] removes FIX:directions); idmap crosses as FixIdSource[] ({map: string, key: string, follow?: boolean, role?: string}, follow always stated and role only where one is on the way out, [] when absent; assigning [] removes FIX:idmap, a refused key, follow flag, role or repeated key throws leaving the field unchanged); derivation crosses as string | null (the FIX:derivation term in its canonical text, null when absent; assigning text parses it through the expression grammar and stores it canonical, a text that is not a term throws leaving the field unchanged; assigning null removes FIX:derivation)
    FIX tags: tag, tags and counter admit positive i32 tags only - 0 (what an unresolved arrival entry records) or a negative throws the core's refusal, written or read back from stored metadata
  MimeType
    statics: from, fromContentCoding, fromContentType, fromExtension, fromPath, fromString, fromJSON, OCTET_STREAM, JSON, JSON_LINES, YAML, TOML, CSV, TSV, PARQUET, ARROW_FILE, ARROW_STREAM, AVRO, ORC, PUFFIN, PLAIN_TEXT, MARKDOWN, HTML, CSS, JAVASCRIPT, XML, PDF, CBOR, MESSAGE_PACK, PROTOBUF, SQLITE3, PNG, JPEG, GIF, WEBP, SVG, MP3, WAV, OGG, FLAC, MP4, WEBM, WOFF, WOFF2, TTF, OTF, XLS, XLSX, ODS, DOC, DOCX, GZIP, ZSTD, BROTLI, ZLIB, COMPRESS, BZIP2, XZ, LZ4, SNAPPY, ZIP, SEVEN_ZIP, RAR, TAR
    instance: clone, compare, contentCoding, equals, extension, format, isApplication, isArchive, isAudio, isBinary, isEncoding, isFont, isHaptics, isImage, isIo, isKnown, isMessage, isModel, isMultipart, isStructured, isTabular, isText, isTextual, isVideo, stableHash, structuredSuffix, subtype, toJSON, toString, topLevel
  IOBase
    statics: from, fromBytes, fromFs, fromUri
    instance: appendArrowBatch, appendArrowReader, appendArrowTable, appendBytes, appendRecords, buffered, childrenWhere, clear, close, closed, codec, columnSize, compressInto, copyInto, createDir, cursor, decompressInto, deleteDir, deleteDirContents, deleteFile, deleteRootDirContents, exists, filesystem, flush, glob, info, intoPath, intoText, isAtomic, isDir, isFile, isIo, isTabular, iterdir, joinpath, kind, ls, boundUri, maskedUri, mediaType, mergeArrowBatch, mergeArrowReader, mergeArrowTable, mergeRecords, mkdir, moveInto, name, normalizePath, open, openAppendStream, opened, openInputFile, openInputStream, openOutputStream, overwriteArrowBatch, overwriteArrowReader, overwriteArrowTable, overwriteRecords, parent, partitions, path, pstreamBytes, pwrite, readArrowField, readArrowReader, readBytes, readDigest, readParquetGeospatialStatistics, readParquetStatistics, readRange, readRangeBytes, readRangeDigest, readRecords, readScalar, readText, readTextLines, recordOptions, remove, rglob, rowSize, sameLocation, size, toString, touch, truncate, unlink, uri, url, writeArrowBatch, writeArrowReader, writeArrowTable, writeBytes, writeRecords, writeScalar, writeText
    uri is the identifier a handle is addressed by and url that identifier when it names a location, while boundUri and maskedUri are the bound filesystem's own spelling; every location parameter takes IOBase | Url | Uri | Urn | Arn | string, and a name resolves through the core's locator
    properties: every record read and write takes an options value and, beside it, a plain object of option properties - or the plain object alone in the options position - set on a copy by their own setters; undefined is skipped; readRecords(cls?, options?, properties?); recordOptions() answers TextOptions for a plain-text handle
  IOCursor
    instance: flush, position, read, seek, streamBytes, tell, write
  ByteIterator
    instance: next, Symbol.iterator
  BatchReader
    statics: fromIpc, from
    instance: combined, consumed, field, intoIpc, intoTable
  RecordOptions
    statics: forMediaType, forMimeType, from
    instance: batchRowSize, blockCodec, clone, commitRowSize, compare, compression, equals, field, filter, keyValueMetadata, level, maxByteSize, maxRowGroupSize, maxRowSize, mergeBy, mimeType, name, partitionPairs, plan, safe, select, stableHash, syncMarker, timezone, toString, withBatchRowSize, withBlockCodec, withCommitRowSize, withCompression, withField, withFilter, withKeyValue, withLevel, withMaxByteSize, withMaxRowGroupSize, withMaxRowSize, withMergeBy, withName, withPlan, withSafe, withSelect, withSyncMarker
    sections: field, filter, select, mergeBy and plan are read/write properties, the setters taking a Selector | Term | string | Array<Term | string> (filter: Filter | Term | string; plan: Plan | Selector | Filter | Field | string); partitionPairs() answers the equalities the filter pins
  TextOptions
    instance: autotype, batchRowSize, clone, commitRowSize, compare, equals, field, filter, framing, leadingFragment, level, linesep, lstrip, maxByteSize, maxRecordByteSize, maxRowSize, mergeBy, mimeType, name, parseMtime, partitionPairs, plan, renameColumns, rowheader, rstrip, safe, select, sourceField, stableHash, startRownum, timezone, toString, withBatchRowSize, withCommitRowSize, withField, withFilter, withLevel, withMaxByteSize, withMaxRowSize, withMergeBy, withName, withPlan, withSafe, withSelect
  FieldPath
    statics: root
    instance: equals, isRoot, join, length, name, parent, segments, stableHash, toString
  TextEntries
    instance: at, entryByPath, getEntryByPath, length, toArray, toString
  TextEntry
    instance: entries, key, keyBytes, toString, value, valueBytes
  TextLine
    constructor: new TextLine(index: bigint, body: string | Buffer, captures?: Array<string | null> | null, options?: TextOptions), the unsigned full-width physical row number and the whole line, row header included, read under the options, and never empty: an empty body, negative index or index wider than uint64 throws
    instance: body, bodytype: string, captures, crosscode: string, crosshashcode: bigint, crossuuid: string, currhashcode: bigint, currunix: bigint, curruuid: string, decodedByteSize, droppedByteSize, entries, entryByPath, getEntryByPath, index: bigint, mtime: bigint | null (the captured instant, else the handle's modification time), removeEntryByPath, seqnum: bigint (the full uint64 row number under startRownum, else index; an invalid configured row number throws), setEntryByPath, sourceuri: string | null (the identifier the line was read under, as its canonical text; what the cross code spells, where sourceurl answers where that identifier is - itself where it is a location, which a located read spells with one text, and where the name resolves to under the working directory where it is a name), sourceurl, toString
    Rust-only: the TextLine Arrow converters media::text::into_arrow_batch, into_arrow_reader, from_arrow_batch, from_arrow_reader (no binding)
  TextLineIterator
    instance: next; IOBase.readTextLines(options?) answers one
  Timezone
    statics: UTC, aliases, from, fromOffset, fromString, registered
    instance: abbreviationAt, clone, compare, equals, getTimezoneOffset, intoLocal, intoUtc, isFixed, isKnown, isSavingAt, isUtc, key, observesSaving, offsetAt, stableHash, standardOffset, toJSON, toString
  Version
    constructor: Version(major: number, minor?: number | null, patch?: number | null); numeric widths u8/u8/u16
    statics: fromStr(text: string) -> Version
    readonly: major: number, minor: number, patch: number
    instance: equals(other: Version) -> boolean; compare(other: Version) -> number; stableHash() -> bigint; clone() -> Version; toString()/toJSON() -> string
    typed field: fields.version(name, options?) -> VersionField; native JavaScript value Version, native Arrow value canonical version text
  Scalar
    statics: decimal, duration, float, from, fromEnum, fromValueBytes
    instance: absolute, add, asBytes, asJs, asSerie, asStr, at, clone, compare, count, digest, divide, dtype, equals, family, get, has, id, intoArrayField, intoField, intoJson, intoJsonBytes, intoStructField, intoValueBytes, isEmpty, isTruthy, kind, length, multiply, negate, path, remainder, remove, scale, set, stableHash, subtract, Symbol.iterator, toJSON, toString, unit, unscaled, zone
    from(value, options?): optional options.field redirects to native Field.scalar once for validation and canonicalization; a Term, Filter, Selector, Plan or Expression crosses as its canonical text
  Serie
    constructor: new Serie(rows?: Iterable<unknown> | null), every row converted through Scalar; omitted or null is an empty schema-free run
    statics: empty, fromArrowArray, fromArrowBatch, fromArrowReader, fromDefault, fromScalars, withCapacity
    instance: asJs, at, cast, child, childAt, children, clear, clone, compare, dtype, equals, extend, extendFromSerie, field, getChildByPath, insert, intoArrowArray, intoArrowBatch, intoArrowReader, intoArrowScalar, intoRun, intoScalar, isColumn, isEmpty, isNull, items, nullCount, pop, push, remove, resize, rows, scalar, set, setCell, setChild, slice, splice, Symbol.iterator, toJSON, toString, truncate
    equals(other) and compare(other) read the rows against a Serie or a ChunkedSerie, and refuse anything else by name; intoArrowReader names its BatchReader root as the core's SerieReader::root_of does - a record's own name, any other column under row. Arrow doors: fromArrowArray(vector, field?, options?), fromArrowBatch(batchOrTable, root?, options?), fromArrowReader(reader, root?, options?) and cast(field, options?) take options { safe, representation }, each absent answer skipped and an unknown key refused; fromArrowArray lands the vector once under its own field and casts it once, however many batches it crossed as; fromArrowBatch takes an Apache Arrow JS RecordBatch or Table; fromArrowReader takes a native BatchReader and consumes it; fromDefault(field, rows = 1); intoArrowScalar answers the Apache Arrow JS value of a one-row column; a run has no layout, so cast and the Arrow outputs refuse it
  ChunkedSerie
    statics: empty, fromArrowArray, fromArrowBatch, fromArrowReader, fromSerie, fromSeries
    instance: asJs, at, cast, child, childAt, children, chunk, chunks, clone, compare, dtype, equals, field, getChildByPath, intoArrowArray, intoArrowReader, intoArrowTable, intoSerie, isEmpty, isNull, items, length, nullCount, numChunks, pushChunk, rows, scalar, slice, Symbol.iterator, toJSON, toString
    Many Serie columns under one field, held apart: an Arrow JS Vector's Data, or a Table's batches. No constructor: new ChunkedSerie() is refused and a chunked serie is built through a static. Arrow crosses as copied IPC, never zero copy: fromArrowArray(vector, field?, options?) takes an Apache Arrow JS Vector, one chunk per Data it holds, an empty one included; fromArrowBatch(batchOrTable, root?, options?) a Table, one chunk per batch, or a RecordBatch, one chunk; fromArrowReader(reader, root?, options?) a native BatchReader, consumed, one chunk per batch; fromSeries(chunks, field?, options?) holds Serie columns - with no field one datatype in pieces, a chunk of another datatype refused naming it; with one, any chunk of another field cast into it; options is { safe, representation }, each absent answer skipped and an unknown key refused. chunks hands each chunk out as its leaf class; the row verbs read across the chunks and child, childAt, children, items and getChildByPath are the same selection in every chunk; intoSerie is the one join; cast is one plan over every chunk; intoArrowArray answers a Vector of one Data per chunk, intoArrowReader a native BatchReader and intoArrowTable an Arrow JS Table, each one batch per chunk - Arrow JS holding no vector or table of none, no chunk is a Vector of one empty Data, and a Table whose one batch is Arrow JS's placeholder, which crosses back as no chunk; equals and compare read the rows, against a ChunkedSerie or a Serie
  SerieSerie
    instance: offsets, range, row
  LargeSerieSerie
    instance: offsets, range, row
  SerieViewSerie
    instance: offsets, range, row, sizes
  LargeSerieViewSerie
    instance: offsets, range, row, sizes
  FixedSizeSerieSerie
    instance: range, row, width
  MapSerie
    instance: entries, keys, keysSorted, offsets, range, row, values
  StructSerie
    instance: names, withoutChild
  SerieReader
    statics: fromArrowReader, fromChunked, fromSerie
    instance: cast, field, intoArrowReader, Symbol.iterator
    fromArrowReader(reader, root?, options?): consumes a native BatchReader and compiles one plan from its schema; iteration yields one StructSerie per batch, a batch's failure raised at its pull; intoArrowReader hands the reconciled stream back as a BatchReader; a stream is read once
    fromSerie(serie): one held column as a stream of one record column - a record column the batch it is, any other the one child of a row - refusing a run and a record column with an absent row; the Scalar Arrow doors are retired, so a value crosses Arrow as Serie.fromScalars(field, rows).intoArrow*() and back as Serie.fromArrow*(...).scalar(0) or intoScalar()
    fromChunked(chunked): a held ChunkedSerie as the stream of its chunks by fromSerie's rule, one record column per chunk, nothing cast or copied
    cast(field, options?): consumes the reader and answers a SerieReader yielding every record cast into field by one plan - a record root, or a column (a DataType as its required value field) as the one child of a row, the core's SerieReader::root_of rule - with options { safe, representation } as Serie.cast takes them, each absent answer skipped and an unknown key refused before the reader is taken; the reader's own root answers the reader as it stands, held records are cast at the call and a refusal is raised there naming the column, a stream's batches are cast as they are pulled, and intoArrowReader afterwards hands back batches under the cast schema
  ArrowCastPlan
    statics: compile
    instance: apply, isIdentity, options, preflight, source, target
    compile(source, target, options?): source is a Field, a field expression, or an Apache Arrow JS Schema, Table or RecordBatch imported as the record row; options reads back as { safe, representation } with every answer spelled; apply(serie) casts a Serie and answers one as its leaf class, and apply(chunked) casts every chunk of a ChunkedSerie under the one plan and answers a ChunkedSerie of as many chunks
  Digest
    statics: from, fromBytes
    instance: algorithm, bits, bytes, clone, compare, equals, stableHash, toJSON, toString, value, width
  Xxh32
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, seed, writeBytes, writeScalar
  Xxh64
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, seed, writeBytes, writeScalar
  Xxh3
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, secret, seed, writeBytes, writeScalar
  Xxh128
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, secret, seed, writeBytes, writeScalar
  xxhash namespace
    values: SECRET_MINIMUM_LENGTH, Digest, Xxh32, Xxh64, Xxh3, Xxh128
    functions: digest, xxh32, xxh64, xxh3, xxh128
  TxHash
    statics: from, fromBytes, fromParts
    instance: algorithm, bytes, clone, compare, digest, dtype, equals, intoDatetime, intoScalar, intoSequencedUuid, intoUuid, stableHash, toJSON, toString, unit, unix, width, withUnit
    intoUuid() -> Scalar: the lossless RFC 9562 UUIDv7 projection, with the Unix millisecond first, the microsecond within it in rand_a's high ten, then the digest stored whole - its top two bits above the variant and its low sixty-two in rand_b; it takes no arguments and hashes nothing; every digest bit survives, so two values project to one identifier only where their microsecond and all 64 digest bits agree; neither unit nor algorithm survives, and sub-microsecond time cannot be recovered; returned as a uuid-typed Scalar because the binding has no Uuid wrapper class - asJs() answers the hyphenated string; a digest that is not 64 bits wide, a microsecond restatement overflow, or an instant a UUIDv7 cannot hold, throws
    intoSequencedUuid(sequence: bigint, seed: bigint) -> Scalar: the event-oriented UUIDv7 projection, with Unix milliseconds first, sequence.min(4095) in the ordered twelve-bit lane, and the low 62 bits of XXH3 over the digest and whole u64 sequence with the u64 seed; returned as a uuid-typed Scalar; an argument outside u64, non-64-bit digest, clock-restatement failure or invalid UUIDv7 timestamp throws
  TxHasher
    statics: fromState
    instance: algorithm, applyArrowBatch, digest, digestScalar, dtype, unit, unixOf, width, clone
  txhash namespace
    values: DEFAULT_UNIT, UNIX_WIDTH, TxHash, TxHasher
    functions: digest, dtype, restateUnix, txh32, txh64, txh3, txh128, unixNow, unixOf, width
    instant: every unix argument is bigint | number | Date | string | Scalar; a bigint or an integer number is the count already, a Date is its UTC millisecond instant, a string is timestamp text, and a Scalar holds any of those
    Rust and Python only: the coupled columns (row_txhashes, column_txhashes, compose, decompose, unix_array)
  AvroSchema
    statics: from
    instance: canonicalForm, clone, compare, equals, fingerprint, fromSingleObject, intoCanonicalForm, intoJSON, intoSingleObject, kind, stableHash, toJSON, toString
  AvroDecodeLimits
    fields: maxDepth, maxInputBytes, maxNodes
  AvroDecodeOptions
    fields: maxDepth, maxInputBytes, maxNodes, readerSchema
  AvroBlocks
    instance: get, metadata, next, schema, Symbol.iterator
  AvroBlock
    instance: count, rows, size
  avro
    statics: Schema, blocks, dumps, dumpsSingle, loads, loadsSingle
    decode limits: maxDepth, maxInputBytes, maxNodes; blocks is a fused lazy iterator
  fix
    constants: ULBRIDGE_ROWHEADER: string, the row header a ULBridge log writes in front of every line, as a rowheader for a text read - the crate's own text rather than a second copy of it; msgsessionid, msgctxid, msgseqnum and msgpluginid are named for the fields they fill, and timestamp, msgthreadid and level name none and are the capture's own columns, so the header dates neither its line nor its message - a message stating no SendingTime(52) is read against its line's currunix, through this header the handle's time where it has one
    values: FixRegistry, FixMsg, FixCodec, MsgType, FixMessages
    functions: schema(registry?: FixRegistry | null, name?: string | null) -> Field; schemaCarrying(carrier: Field, read: Field) -> Field; schemaTags() -> number[]; crateFields() -> Field[]; globalRegistry() -> FixRegistry; installGlobalRegistry(registry: FixRegistry) -> void
    plain objects (generated interfaces, every absent fact null): FixHeaderView {beginstring: string, msgtype: string, sendercompid: string | null, targetcompid: string | null, msgseqnum: number | null, sendingtime: bigint, possdupflag: boolean | null, msgdirection: string | null, signaturelength: number | null, signature: Buffer | null, checksum: string | null}; FixCaptureView {msgpluginid: string | null, msgctxid: string | null, msgsessionid: string | null, msgsesseventid: string | null, msgoriginator: string | null, conversationid: string | null}; FixEntryView {tag: number, name: string, value: string | null, entries: FixEntryView[]}; a UUID crosses as its hyphenated text, a hash and an instant (nanoseconds since the epoch, UTC) as a bigint, a price or a quantity as its decimal text, a code as the text it is
  fix.FixRegistry
    constructor: FixRegistry()
    statics: fromFields(fields: Field[]) -> FixRegistry; fromHandle(location: IOBase | Url | string) -> FixRegistry; fromCfbFile(location: IOBase | Url | string, dialect?: string | null) -> [FixRegistry, Field[]], every produced field stamped with the dialect on FIX:branches; fromJson(input: string) -> FixRegistry
    lookup: getFieldById(id: number) -> Field | null; fieldById(id: number) -> Field; getFieldByTag(tag: number) -> Field | null; fieldByTag(tag: number) -> Field; getFieldByCounter(tag: number) -> Field | null; fieldByCounter(tag: number) -> Field, the repeating group a counter tag opens, nothing where two groups share one; getFieldByName(name: string) -> Field | null; fieldByName(name: string) -> Field, a scalar, then a component, then a group; getFieldByPath(path: string | FieldPath) -> Field | null; fieldByPath(path: string | FieldPath) -> Field; getField/get(key: number | string) -> Field | null; field(key: number | string) -> Field; has(key: number | string) -> boolean
    mutation, every definition through the field doors: addField(field: Field) -> boolean; insert(field: Field) -> Field | null, a Struct as a component (a message when it carries FIX:msgtype), a Serie of Structs or a Map as a group, else a scalar; update(field: Field) -> void, into the scalar of the same identity or the component/group of the same folded name; remove(key: number | string) -> Field | null, a name no scalar answers to reaching a component or a group, null for one still referenced; removeById(id: number) -> Field | null; addFields/mergeWith/addCfbFile/addCfbFiles/addJsonFile are Python-only
    messages: registerMsgtype(spelling: string, name?: string | null, description?: string | null) -> MsgType; getMsgtype(spelling: string) -> MsgType | null; msgtype(spelling: string) -> MsgType; each answers a copy of the registry's own definition
    membership: dialects() -> string[], the distinct FIX:branches names any field or definition carries, sorted; provenance a caller filters on, never a lookup tier
    snapshot: intoJson() -> string; toJSON() -> native snapshot object containing fields/components/groups; writeInto(location: IOBase | Url | string) -> void, `commit` with its report discarded; commit(location: IOBase | Url | string) -> { written: string[], skipped: number, removed: string[] }, each document digested where it lies so a second commit of one registry writes nothing, shards named by tag block as nine digits (fields/000000000.json, fields/000000650.json), the crate's own metadata group and the fixmsg component (the fixed row) written like every other
    protocol: readonly size: number, the scalars, the components and the groups; keys()/Symbol.iterator -> Generator<Field>, the scalars tag-major then identifier, then the components and the groups in name order (which category a definition is filed under is toJSON()'s three listings, never a shape read off the datatype: the crate's srcuuids serie is a field, its metadata Map is a group); equals(other: FixRegistry) -> boolean; stableHash() -> bigint; clone() -> FixRegistry; toString() -> string; native equality/hash include all categories; mutations refuse while a message, default registry or unfinished native walk shares the registry
    keys: a number tag (exact i32) or string name/dotted path; one namespace, canonical names/tags before aliases/alternate tags, the fold always; an identifier is the exact i32 digest field.fix.id answers and is only spelled through the ById doors (a bare number elsewhere is a tag); malformed input raises the native located error
  fix.MsgType
    readonly: name: string; msgcat: string | null (symbolic four-character `FIX:msgcat` metadata; `FixMsg.msgcat` is its resolved integer)
    instance: asStr() -> string; asField() -> Field; identifierValues(message: FixMsg) -> Array<[Field, Scalar]>; getGroupByTag(tag: number) -> Field | null; equals(other: MsgType) -> boolean; compare(other: MsgType) -> number; stableHash() -> bigint; clone() -> MsgType; toString() -> string; toJSON() -> native Field JSON
    protocol: an immutable copy of the registry's message definition; complete case-sensitive wire code; asField and identifierValues project independent mutable native Field wrappers; identifierValues redirects to the compiled native selection and retains each Scalar's exact datatype
  FIX iterators
    FixMessages: IterableIterator<FixMsg>; next() -> IteratorResult<FixMsg>; no collection on the binding path
    FixMessages is what every FixCodec stage answers - one line's messages, none, one or many a row (one per frame the line carries, one named unknown for a row whose payload is a JSON document, none for a line carrying no message), lines or records parsed, messages walked through a lifecycle, a batch read back; a JavaScript iterable behind it is pulled one item at a time through a loader-bound pull function; a refused line throws where it is met and the stream continues, a failure of the iterable throws as itself and ends it
  fix.FixMsg
    constructor: FixMsg(field: Field, value: Scalar | Record<string, unknown> | readonly unknown[], registry?: FixRegistry | null); a child stating a typed fact - a header or trailer tag (8, 35, 49, 56, 34, 52, 43, 385, 93, 89, 10), a crate column (65000-65100) but sourceurl (65026), one of the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003}, Text(58) - fills the holder that owns it and leaves the row; SendingTime reads UTC now when the value states none; the identity is settled
    statics: fromRow(schema: Field, row: Scalar | Record<string, unknown> | readonly unknown[], registry?: FixRegistry | null) -> FixMsg, the inverse of intoRow: the typed facts read off their columns, the content rebuilt from the fixentries column, every capture column carried under its own name and held as no fact, no clock read
    readonly: registry: FixRegistry, field: Field (the content row's schema), value: Scalar (the content row), size: number (the row's children), msgcat: number | null (the stable integer business category), text: string | null (Text(58)), metadata: Record<string, string> (a bridge's namespaced keys, sorted)
    graph leaves: marketOperations() -> graph.MarketData[], the typed graph leaves the message expands to: an order, a quote, an execution or an initial trade report is one; a book W or X one per NoMDEntries(268) occurrence, or one scoped snapshot control for an empty W; a message of any other type throws naming its MsgType
    typed holders, plain objects read once: header() -> FixHeaderView; capture() -> FixCaptureView
    graph getters: curruuid: string; crossuuid: string; crosscode: string; currhashcode: bigint; crosshashcode: bigint; currunix: bigint; state: string; seqnum: number; prevuuid: string | null; srcuuids: string[] (sorted unique provenance; the text line it was parsed out of, none for bytes; position carries no reference priority); carried: Record<string, Scalar> (the capture's own cells the message carries, each under its column; empty for bytes); creaunix / execunix / recdunix / exprtime / prevunix / snapunix: bigint | null
    market getters: price: string | null; currency: string; quantity: string | null; unit: string (UnitOfMeasure(996), empty where none); side: string; securityids: Record<string, string> (source to code, in key order: ISIN, CUSIP, SEDOL, BLOOMBERG, FIGI and any other source SecurityIDSource(22) or the SecurityAltID group names, the CUSIP an American ISIN embeds derived); cficode: string | null (standard CFICode(461)); miccode: string | null (SecurityExchange(207), ExDestination(100) or LastMkt(30), the first naming a MIC); lastpx/lastqty/avgpx/cumqty/leavesqty/prevpx/prevqty/spotrate/forwardpoints: string | null; the six FX lifts lastspotrate/lastforwardpoints/bidspotrate/bidforwardpoints/offerspotrate/offerforwardpoints: string | null (FIX's own 194, 195, 188-191); anomalies: Array<FixAnomalyView { field, reason }> (decimal text; the two FX parts null until a lift fills them); ticker: string | null (Symbol(55))
    operation getters: marketoperationid: number | null; tif: string | null (TimeInForce(59) as the code it stores); tradable: boolean | null; accountids: Record<string, string> (Account(1) and a CUSTOMERACCOUNT party); userids: Record<string, string> (SenderSubID(50), OnBehalfOfSubID(116), an ENTERINGTRADER or EXECUTINGTRADER party); altids: Record<string, string> (ORDERID, CLORDID, ORIGCLORDID, EXECID, QUOTEID, QUOTEREQID, MDREQID, TRADEID and the rest the message states; upper-cased keys, in key order); bid/ask: graph.Lane | null (the lane the message states a slot of; a buy order fills its own lane's size, a quote states both)
    writes: set(key: number | string, value: unknown) -> void, the value widened through Scalar.fromJs; a key reaching a typed fact records it on its holder (null clears it), a key reaching the capture's own column - sourceurl - throws, because no message holds it, any other key lands in the row typed by the field it resolves to, null stored as a stated null, an existing child replaced where it stands and an absent one appended; remove(key: number | string) -> Scalar | null, a typed fact cleared on its holder, a row child removed, null for a key reaching nothing; every write settles the identity again
    lookup, answering a Scalar: getById(id: number) -> Scalar | null; byId(id: number) -> Scalar; getByTag(tag: number) -> Scalar | null; byTag(tag: number) -> Scalar; getByName(name: string) -> Scalar | null; byName(name: string) -> Scalar; getByPath(path: string | FieldPath) -> Scalar | null; byPath(path: string | FieldPath) -> Scalar; get(key: number | string) -> Scalar | null; at(key: number | string) -> Scalar; a typed tag answers its holder's fact as the Scalar its column types (byTag(35) the type's text, byTag(52) datetime64(ns, UTC), byTag(44) a decimal128(38, 18), byTag(65039) a uuid, byTag(65017) a uint64), any other key reaches the row, Side(54) and Currency(15) among it
    projection: entries() -> FixEntryView[], the content row read as a tree, derived on the first ask and kept until a write, a group stating its count once on the entry that heads it; intoRow(field: Field) -> Scalar, each typed fact from its holder and a lane the message only settled - a sending clock it never stated - left to its column's null; intoBytes(separator?: number | null) -> Buffer, the header (52 only when stated), the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003} in that order, the entries, then the trailer 93, 89, 10, coded facts as their wire code; intoText(separator?: string | null) -> string, the same as text under one separator character (SOH unstated); digest() -> Buffer, sixteen bytes over the wire entries
    protocol: Symbol.iterator -> the entries; equals(other: FixMsg) -> boolean; stableHash() -> bigint; clone() -> FixMsg; toString() -> string; toJSON() -> {field, value}, the content row's two documents; native facts+schema+row equality/hash
  fix.FixCodec
    constructor: FixCodec(registry?: FixRegistry | null, options?: {separator?: number, payloadColumn?: string, captureNames?: string[], nullValues?: string[], direction?: string, batchByteSize?: number, batchRowSize?: number, threads?: number, snapshotNs?: bigint | null, officialTimeDelayMs?: number, includeMsgtypes?: string[], excludeMsgtypes?: string[], defaultSendingTime?: Scalar | Date | null, marketMetadata?: boolean} | null); defaultSendingTime redirects to try_with_default_sending_time - a Scalar crosses as itself and must already be DateTime64(ns, UTC), a Date is its UTC millisecond instant restated in nanoseconds, any other layout throws the core's refusal; direction is any spelling of a code of tag 385's set, resolved once - the core's Send code when unstated, no pin when '' - and a spelling outside the set throws naming the set; a pin is on the codec, a stage is a call; marketMetadata redirects to with_market_metadata, the core's true when unstated
    readonly: registry: FixRegistry; defaultSendingTime: Scalar | null, the SendingTime a new message takes when neither it nor its carrier states one - a row cell reaching tag 52, else the currunix of the TextLine it was read out of - null reads UTC now once per such message, so only a pinned default makes a parse of undated bytes repeatable; a line's own timestamp capture is context and stamps nothing, while the line's currunix - an mtime capture, else its handle's time - dates what it carries; separator: number | null; payloadColumn: string; nullValues: string[]; direction: string | null, the code of tag 385's set an unmarked line takes on the batch door - a line is read by the tag-385 field's FIX:directions, compiled once when the codec is built; batchByteSize: number, the core's 128 MiB when unstated; batchRowSize: number, the core's 32,768 when unstated, a batch closing on whichever bound it reaches first; threads: number, available CPUs when unstated, zero reading as one and one pulling lazily; line, message-row and write doors use 64-item chunks at lane depth two in input order, while Arrow capture parse doors hold at most one whole input batch per thread in flight in batch order; snapshotNs: bigint | null, a positive epoch-aligned lifecycle grid and null when disabled; officialTimeDelayMs: number, how far from SendingTime(52) an official transaction clock may stand and still date the message, the core's one second when unstated: the parse takes the TransactTime(60) the message states, else the TrdRegTimestamp(769) of its TrdRegTimestamps(768) group whose TrdRegTimestampType(770) stamps the event or a hop it crossed, nearest the sending clock, and the sending clock dates the message where none stands that near; a nonpositive delay admits only a clock equal to it; includeMsgtypes: string[]; excludeMsgtypes: string[], the core's Heartbeat, TestRequest and 'unknown' when unstated, each read before a frame is built and spelled as a code or a name, and an empty excludeMsgtypes keeps every type; marketMetadata: boolean, whether a market operation this codec builds carries, in its metadata, what its message states that no typed column reads - part of the leaf's identity
    statics: inferMsgtypeBytes(body: Buffer) -> Buffer | null; inferMsgtypeText(body: string) -> string | null
    parsing, streams: parseLine(row: Buffer) -> FixMessages; parseLines(lines: Iterable<string | ArrayBufferLike | ArrayBufferView>) -> FixMessages, lazy; parseTextLine(line: TextLine) -> FixMessages; parseTextLines(lines: Iterable<TextLine>) -> FixMessages, lazy
    parsing, one message: parseFixLine(body: Buffer) -> FixMsg; parseUllinkLine(body: Buffer) -> FixMsg; parseFixmlLine(body: Buffer) -> FixMsg; parsePairs(pairs: Array<[string, string]>) -> FixMsg; each takes one frame, and a body holding a second throws naming the byte that second opens at - 'expected one frame, got a second' - because a caller holding two holds a row, which is what parseLine reads; every parse settles the message whole - typed facts lifted, a nested XmlData exploded, deprecated fields restated to their latest aliases, the dictionary's FIX:derivation rules run, the security identifiers, the identifier maps and the order lanes filled, the identity derived - so there is no enriching pass
    lifecycle: lifecycle(messages: Iterable<FixMsg>) -> FixMessages, lazy, the one finite walk over events - the messages collected; raw observations with the same complete nonempty `(msgtype, msgsessionid, msgctxid, msgseqnum)` - one capture msgsesseventid - are coalesced first, the latest recdunix, a stated one before an unstated one, then currunix selecting the reference once over all of them, for a full FIX-content and graph merge: reference scalar conflicts lead, older facts fill absences, and repeating-group occurrences merge recursively at equal indexes in FIX tag order, while duplicate execunix and recdunix observations retain their earliest facts; the retained messages are sorted by instant, each stated as the one after the live message it follows (prevuuid, prevunix, seqnum, the chain's creaunix and latest execunix carried), a missing parentorderid filled from a different predecessor orderid and a missing clordid from the predecessor, then settled again; stated links win, a message no live one precedes is answered as it came, and srcuuids never travel along a chain
    formatting: formatMessages(messages: Iterable<FixMsg>, field: Field) -> Scalar[], the third verb - each message as the row one message field of the registry holds it, a column that field carries and the message does not lifted off its content record first, so a narrow row formats into a wider one; the target is a venue's own message type, fix.schema() itself, which keeps every column a capture lands in, or any Struct root a caller built
    market doors: marketOperations(messages: Iterable<FixMsg>) -> graph.MarketDataRowIterator, the sorted door - the capture collected when called, what bookArrowReader admits expanded as FixMsg.marketOperations() expands it, each leaf carrying its message's unmapped fields where marketMetadata says so, stably sorted by snapunix else currunix; source errors and expansion refusals first in source order, each thrown by its own next() with the stream continuing past it, and a failure of the iterable itself - a TypeError for an item that is not a FixMsg, or what the iterable threw - thrown once, as itself, in place of the end; neither the lifecycle nor the msgtype filter runs; marketArrowReader(messages: Iterable<FixMsg>) -> BatchReader, those operations as lifted graph.MarketData.field() rows under the codec's row and byte bounds, one intake failure - the iterable's own included - the reader's only item
    Arrow twins, over whatever BatchReader.from accepts, answering BatchReader: parseTextArrowReader(source) -> capture batches to batches of FIX rows, carried columns leading, batches closed on the bytes each row lands as against batchByteSize; with several threads, whole input batches are at most one job per thread in flight and answer in batch order; lifecycleArrowReader(source) -> the same schema in and out, rows walked as one ordered lifecycle, each row's capture cells carried by its message through the walk's own ordering and stated again at their own columns; marketOperationsArrowReader(source) -> batches of FIX rows to lifted graph.MarketData.field() rows, messages(source) into marketArrowReader, each row read as its own message, answering over rows no walk wrote what marketArrowReader answers for their messages; a walked capture reaches the sorted door as messages - marketArrowReader(codec.lifecycle(messages)) - never as the rows lifecycleArrowReader writes, whose walked facts (prevpx, prevqty, a carried side or ticker, an execution instant) are no cell of the row; a schema that makes no root refused before a row is read as lifecycleArrowReader refuses it; formatArrowReader(source, field: Field) -> the format twin, batches under field; a source carrying no content record is cast column for column instead of read back as messages; messages(source) -> FixMessages, lazy: one thread holds one batch at a time, while several retain bounded 64-row chunks that can span source batches and yield source order; each row's own cells carry into its message, so an arrowReader over them states the capture's own columns again; arrowReader(schema: Field, messages: Iterable<FixMsg>) -> BatchReader, batches closed on the bytes each row lands as, an item that is not a message is the reader's error; bookArrowReader(messages: Iterable<FixMsg>, snapshotMillis: number = 0, global: boolean = false) -> BatchReader, written as lifted graph.MarketData.field() rows of kind book_event, read back through graph.MarketData.fromArrowReader; sorted messages through native operations and per-symbol books (or one GLOBAL book), only an initial `35=AE` with `487` absent/New and `150` absent or execution-like (`F`, legacy `1`/`2`) projected natively from NoSides(552) into one composite Trade whose canonical children are flattened into the BookEvent execution list; non-New/cancel/correct/reverse/status AE remain named refusals; the native book boundary ignores administration, requests including AD/AQ, acknowledgements including AR, and non-executing EXEC, while preserving source errors and all malformed admitted records; standalone operation conversion remains strict; children state Side and side IDs but no TradeSideIndex, and anonymous sides use their canonical 128-bit occurrence-content digest rather than group index; a supplied snapshot rebases root and every child currunix while retaining child execunix, epoch-aligned snapshots are enabled when positive, and lifecycle enrichment remains explicit; no JavaScript Trade wrapper or binding-side side expansion; writeArrowReader(source, sink: { write(chunk: Uint8Array): unknown }) -> number of lines written, the codec's separator else SOH then a newline, refused before a row when the fixentries column is absent; a source reader is consumed
    instance: clone() -> FixCodec; toString() -> string
  ProtocolField FIX category metadata
    writable: counter: number; component/group/fieldRef/msgcat/msgtype: string; getters return null when absent; identifiers: string[]; remove(key) clears metadata using the protocol view
    invariant: counter validates exact i32 positive FIX tag, reference names follow native catalog naming rules, FIX:codeset names the code set a field reads by and the dictionary holds the members, direction rules remain inline FIX:directions on tag 385 and cross as a typed list; identifiers accepts an array of member names/aliases/decimal tags, resolves once to direct scalar names in component order, replaces whole, clears on [], and refuses invalid input atomically through the core
  graph
    values: Lane, BookRef, Order, Quote, Execution, OrderEvent, QuoteEvent, ExecutionEvent, TradeEvent, SnapshotPartition, BookSide, BookEvent, SnapshotEvent, MarketData, MarketDataRowIterator, BookIterator, EventIterator; a frozen namespace, the flat and Js-prefixed names deleted; the typed market leaves and MarketData over every one of them, what FixMsg.marketOperations() answers; nothing here resolves, folds, merges or validates a fact - a named fact is resolved by the column enums' own of_name, checked by its column's own field and stated through its column's own record, and every other constructor redirects to the native core door named beside it
    constants: GLOBAL_SYMBOL: string; ENTRY_ID: string; ENTRY_REF_ID: string; FOLLOWED_ALTIDS: readonly string[] (a frozen array)
    types: MarketItem (MarketData or any of the ten leaves); OperationFactsInput / OperationEventFactsInput (a Scalar record or a plain object keyed by column name); OperationElementConstructor<T>, OperationEventConstructor<T>, BookIteratorConstructor, EventIteratorConstructor; BookLimit ({price: string | null, quantity: string, uuids: string[]}, what BookSide.limits answers)
    shared facts (one set, answered by every class whose traits name them): element - curruuid / crossuuid: string; crosscode: string; currhashcode / crosshashcode: bigint; srcuuids: string[]; event - currunix: bigint; state: string; seqnum: number; creaunix / execunix / recdunix / exprtime / prevunix / snapunix: bigint | null; prevuuid: string | null; isExecution: boolean; market - price / quantity / lastpx / lastqty / avgpx / cumqty / leavesqty / prevpx / prevqty / spotrate / forwardpoints: string | null (decimal text); currency / side: string; unit: string; securityids / metadata: Record<string, string>; cficode / miccode: string | null; ticker: string | null; operation - marketoperationid: number | null; tif: string | null; tradable: boolean | null; accountids / userids / altids: Record<string, string>; bid / ask: Lane | null
    shared verbs: withPrevious(previous: Self) -> Self | null and mergeWith(other: Self) -> Self | null (same class only; null where it cannot follow or nothing changes); isAfter(other) / isBefore(other) -> boolean; restating(live: Self) -> Self on the dated classes
    shared protocol: immutable; equals(other) -> boolean (the core's ==); stableHash() -> bigint (currhashcode); clone(); toString() -> `<Class>(<curruuid>, ...)`; toJSON() -> string, the base64 IPC bytes of the value's one MarketData.arrowReader row, and static fromJSON(text: string) rebuilding it exactly through MarketData.fromArrowReader
  graph.Order
    constructor: new Order(facts?: Scalar | Record<string, unknown> | null), each key a market or operation column name or crosscode or srcuuids, folded as the column enums read it, widened through Scalar.from, then finalized; undefined skipped, null clears, a Lane under bid/ask crossing as its six slots, any other graph value (BookRef, SnapshotPartition, a leaf, MarketData, a walk) refused naming its key; an unknown name is refused naming the fact, a derived identity (curruuid, crossuuid, currhashcode, crosshashcode) refused as derived, any other event column refused as a clock, state or chain an undated element has none of
    readonly: the element, market and operation facts; kind: "order"
    at(unix: bigint | number) -> OrderEvent, the element dated and finalized
  graph.Quote
    as graph.Order, kind "quote"; at(unix) -> QuoteEvent
  graph.Execution
    as graph.Order, kind "execution"; at(unix) -> ExecutionEvent
  graph.OrderEvent
    constructor: new OrderEvent(currunix: bigint | number, facts?: Scalar | Record<string, unknown> | null), each key an event, market or operation column name, folded, then finalized; undefined skipped, null clears; an unknown name refused naming the fact, a derived identity (curruuid, crossuuid, currhashcode, crosshashcode) refused as derived, currunix refused as stated once positionally; the `book` key lifted out as the event's book control, a BookRef or null, anything else a TypeError
    readonly: the element, event, market and operation facts; kind: "order"; book: BookRef | null; action: string | null; scope: string; isFullSnapshot: boolean
    withBook(book: BookRef) -> OrderEvent, refinalized; intoElement() -> Order, the clocks and book control dropped and finalized
  graph.QuoteEvent
    as graph.OrderEvent, kind "quote"; intoElement() -> Quote
  graph.ExecutionEvent
    as graph.OrderEvent, kind "execution", isExecution always true; intoElement() -> Execution
  graph.TradeEvent
    not directly instantiable; statics fromParts(root: OrderEvent | QuoteEvent | ExecutionEvent | TradeEvent | MarketData, executions: ExecutionEvent[]) -> TradeEvent, the root's facts copied; an undated root throws naming its kind
    readonly: the element, event, market and operation facts; executions: ExecutionEvent[], in canonical side, cross code and identity order
  graph.Lane
    constructor: new Lane(input?: {price?, spotrate?, forwardpoints?, quantity?: string | number | bigint | null, currency?, unit?: string | null}), the object widened through Scalar.from as a fact is and checked by the lane struct's own field; an unknown slot refused by name; undefined and null both read back null
    readonly: price / spotrate / forwardpoints / quantity: string | null; currency: string | null; unit: string | null
    isStated() -> boolean; equals(other) -> boolean; stableHash() -> bigint; clone(); toString(); toJSON() -> the six slots new Lane reads back
  graph.BookRef
    constructor: new BookRef(input?: {action?, scope?: string | null, entryPx?, entrySize?: string | number | bigint | null, position?: number | null}), the object widened through Scalar.from as a fact is; an unknown slot refused by name; action reads through the MdUpdateAction vocabulary, refusing an unknown spelling; position an unsigned 32-bit integer; entryPx/entrySize cross the decimal field's own scalar door
    readonly: action: string | null; scope: string | null; position: number | null; entryPx / entrySize: string | null
    isStated() / isRangeDelete() / isPartial() -> boolean; equals(other) -> boolean; stableHash() -> bigint (its five slots as one record, the value Python's hash reads); clone(); toString(); toJSON() -> the five slots new BookRef reads back
  graph.SnapshotPartition
    constructor: new SnapshotPartition({scope: string, symbol?: string | null})
    readonly: scope: string; symbol: string | null
    compare(other) -> number, ordered by (symbol, scope) with null first; equals(other) -> boolean; stableHash() -> bigint (its symbol and scope as one record, the value Python's hash reads); clone(); toString(); toJSON() -> the two slots
  graph.BookSide
    constructor: new BookSide(side: string), read through the core Side vocabulary
    readonly: the element and market facts (side the side it holds); live / deltas: MarketData[] (each an order or quote event); length: number; isEmpty: boolean; bestPrice / bestQuantity: string | null (the first priced level's; null for an empty side or one of unpriced entries only); limits: BookLimit[], one plain object per price level {price: string | null, quantity: string, uuids: string[]} of decimal text, best first, the one limit of every unpriced entry last with price null, uuids the entries' curruuid in position then arrival order
    withOperation(operation: MarketItem) -> BookSide, a new side with the operation atomically applied; an order or quote stating no price rests at the unpriced level after every priced one; depth(levels: number) -> string | null, the exact sum of the first levels limits' quantities as decimal text, the unpriced limit counted where reached, '0' for an empty side or levels 0, null only past decimal; levels a whole number of at most 2^53, a negative or fractional one throws
  graph.BookEvent
    constructor: new BookEvent(currunix: bigint | number, symbol: string), an empty book
    readonly: the element, event and market facts; bid / ask: BookSide; executions: ExecutionEvent[]; snapshotPartitions: SnapshotPartition[]; isCrossed: boolean; isLocked: boolean (both bests stated and equal); spread: string | null (best ask less best bid as decimal text, negative when crossed, null where a side states no best); bboMidpoint / medianQuantity: string | null
    withOperations(operations: Iterable<MarketItem>) -> BookEvent, read whole into one atomic group: order, quote, execution and trade events and snapshot controls fold, any other kind is refused by name, a non-leaf item names its index
    imbalance(levels: number) -> string | null, (bid - ask) / (bid + ask) over the two sides' depth(levels) as decimal text, '1' bid-only, '-1' ask-only, null where the total is zero (both sides empty, or levels 0)
  graph.SnapshotEvent
    not directly instantiable; statics snapshot(event: any dated leaf | MarketData, scope?: string | null) -> SnapshotEvent, the event and market facts copied; an undated leaf throws naming its kind
    readonly: the element, event and market facts; book: BookRef, action always "snapshot"
  graph.MarketData
    constructor: new MarketData(leaf: MarketItem), through the core's own From; anything else throws naming what it got
    statics: kinds() -> string[], every MarketKind spelling in declaration order; field() -> Field, the lifted `marketdata` struct every leaf is written under; arrowReader(items: Iterable<MarketItem>, batchRowSize?: number, batchByteSize?: number) -> BatchReader, pulling the items lazily, a bad item's TypeError and a source's own failure arriving as the reader's error; fromArrowReader(reader: BatchReader) -> MarketDataRowIterator, tolerant: any subset of the lifted columns in any order, names folded, foreign columns ignored, a castable type cast once, a missing kind or a missing currunix on a dated row refused by name at its row; fromJSON(text: string) -> MarketData; plan(view: string, lifts?: Array<string | FieldPath> | null, crosscode?: string | null) -> Plan, the one plan a named view is (orders, quotes, executions, trades, book_sides, books, lifecycle; read ignoring ASCII case; only lifecycle takes, and needs, a crosscode), each lift a FieldPath or its text read once and appended after the view's own columns, its text reading back as the plan; applyView(view: string, reader: BatchSource, lifts?, crosscode?) -> BatchReader, exactly plan(view, lifts, crosscode) applied to whatever BatchReader.from accepts, bound once against its schema, the view and the lifts resolved before a native reader is taken and consumed; a lift naming a column the root does not hold refused where the plan binds
    readonly: the element and market facts, delegated to the leaf; kind: string; isEvent: boolean; book: BookRef | null (the control of an operation event or a snapshot control)
    asOrder() / asQuote() / asExecution() / asBookSide() / asOrderEvent() / asQuoteEvent() / asExecutionEvent() / asTradeEvent() / asBookEvent() / asSnapshotEvent() -> the leaf or null; intoLeaf() -> the leaf as its own class
  graph.MarketDataRowIterator
    IterableIterator<MarketData>: what MarketData.fromArrowReader answers - lazy, fused after an error thrown at the failing row - and what FixCodec.marketOperations answers - a refused item thrown by its own next(), the stream continuing, a failure of the iterable thrown once in place of the end
  graph.BookIterator
    constructor: new BookIterator(items: Iterable<MarketItem>, snapshotMillis: number = 0, global: boolean = false)
    readonly: global: boolean
    protocol: IterableIterator<BookEvent>, pulls its items lazily; a leaf a book does not fold throws naming its kind, a bad item's own TypeError and a source iterable's own failure both thrown as themselves
  graph.EventIterator
    constructor: new EventIterator(items: Iterable<MarketItem>, sorted: boolean = true, snapshotNs?: bigint | number | null); sorted=false collects and sorts the whole source inside the constructor, so a source failure throws there
    readonly: snapshotNs: bigint | null
    alive() -> MarketData[], the elements still alive after what the walk has read so far
    protocol: IterableIterator<MarketData>, pulls its items lazily; the order, quote, execution and trade events walk and every other leaf is yielded unchanged, in place
  replay
    statics: DEFAULT_STATE_DIR, bookAt, bookJson, booksBetween, booksJson, createReplayServer, eventJson, indexBooks, leafFromJson, leafJson, leavesJson, loadSource, main, merged, openScenarios, parseArgs, refusalText, rerun, rowsOf, synthetic, toJson, walk
    yggdryl/replay (node/replay.js over node/replay/, CommonJS): nothing new in the addon - every fact is graph, fix and IOBase; loadSource(spec, { rowheader?, sendingTime?, registry?, timezone?, id? }) -> { id, kind, name, operations, symbols, refusals }, spec 'synthetic', a .arrow/.ipc/.feather/.parquet file of marketdata rows, or any other file as a FIX capture read from its bytes (the ULBridge row header, the 2024-01-02T10:15:30Z sending clock and the process-default dictionary unless named); createReplayServer({ sources, stateDir?, webDir?, snapshotMillis = 0, global = false }) -> { server, stateDir, listen(port = 0, host = '127.0.0.1') -> Promise<url>, close() }, close removing the state folder the service made itself; synthetic() -> MarketData[] with synthetic.books(snapshotMillis = 0, global = false), synthetic.T0, synthetic.SYMBOLS; walk(operations, { snapshotMillis, global }) -> BookEvent[] through graph.BookIterator; indexBooks(books), booksBetween(index, symbol?, from?, to?), bookAt(index, symbol, at?), merged(base, inserted); rerun(base, scenario, options?, operations?) -> Promise<{ books, from }>; openScenarios(stateDir) -> Promise<{ dir, list, read, load, save(scenario, operations?), insert(scenario, leaf), remove }>, a scenario name lowercase [a-z0-9][a-z0-9._-]{0,127}; rowsOf(reader) -> { columns, rows }; bookJson / booksJson / leafJson / leavesJson / eventJson the one renderer - instants and 64-bit integers as decimal text, decimals as their exact text, a map column as [key, value] pairs in the native key order, stableHash as text; leafFromJson({ kind, currunix, facts }) -> the native leaf through its own constructor, kind one of order_event, quote_event, execution_event, order, quote, execution; refusalText(error), toJson(value, space?); parseArgs(argv), main(argv?) the command line
    routes: GET /api/field -> { columns, kinds }; GET /api/sources -> { snapshotMillis, global, views, sources }, views the package's enums.marketViews; GET /api/sources/:source/{books (server-sent events), book, lifecycle, view}; GET /api/scenarios -> { scenarios: [{ name, events }] }; GET/PUT/DELETE /api/scenarios/:name; POST /api/scenarios/:name/events; DELETE /api/scenarios/:name/events/:curruuid; GET /api/sources/:source/scenarios/:name/{books (server-sent events), diff -> { symbol, from, instants: [{ at, base, scenario, changes }] }}; GET /web/* the components and /web/app/ the application (/, /app, /app/ and /web/app answer 308 to /web/app/)
    command line: node node/replay.js <source> [--port 0] [--snapshot-millis 0] [--global] [--rowheader <regex>] [--sending-time <ns|ISO 8601>] [--registry <dir>] [--state <dir>] [--web <dir>]
  web
    yggdryl/web/<name>.js (node/web/): ES modules with no dependency, bundler or CDN, web/package.json {"type":"module"}; web/theme.css the tokens and every ygg-ui class; each module and its exports:
    instant.js: parseInstant, instantText, instantParts, formatInstant, formatClock, elapsed, toPixels, fromPixels, nanosPerPixel, compareInstant (bigint nanosecond instants)
    decimal.js: decimalParts, isDecimalText, formatDecimal, compareDecimal, maxDecimal, minDecimal, toFloat, sumDecimal (exact decimal text; a float for a pixel only)
    scales.js: linear, ticks, niceStep, padded; candles.js: bucketOf, aggregateCandles (executions by lastpx per bigint bucket)
    diff.js: sameValue, diffFacts, diffKeyed, diffSides, diffBooks, diffStreams (rows { at, base, scenario, changes }); scenario.js: createScenario, eventInstant, insertEvent, removeEvent, earliestAffected, serializeScenario, parseScenario
    shortcuts.js: SHORTCUTS, chordOf, commandFor, displayChord; store.js: createStore -> { get, set, subscribe, select }
    component.js: Component - constructor(props), mount(parent), update(state), destroy(), emit(name, detail), listen(target, type, handler), el - and element(tag, className, text)
    components: ladder.js Ladder; depth-chart.js DepthChart, cumulative; price-chart.js PriceChart; tape.js Tape; lifecycle.js Lifecycle; metrics.js Metrics; transport.js Transport, SPEEDS; modal.js Modal; drawer.js Drawer; toast.js Toasts; tooltip.js Tooltip; tabs.js Tabs; split.js Split; palette.js Palette, COMMANDS, fuzzy; shortcuts-sheet.js ShortcutsSheet; symbol-select.js SymbolSelect, GLOBAL; theme-toggle.js ThemeToggle, STORAGE_KEY; insert-form.js InsertForm; scenario-drawer.js ScenarioDrawer; diff-view.js DiffView, countChanges, changeLines; virtual.js VirtualRows; canvas.js fitCanvas, tokens, watchSize, watchTheme, tickLabel
    events (bubbling CustomEvent): ygg:limit-select { price, uuids }, ygg:seek { at }, ygg:select-element { crosscode, curruuid }, ygg:transport { command, value? }, ygg:open {}, ygg:close {}, ygg:tab { id }, ygg:resize { sizes }, ygg:command { id }, ygg:symbol { symbol, global }, ygg:theme { theme }, ygg:insert { kind, currunix, facts }, ygg:scenario { command, name, curruuid?, to? }
    app/: the one-page application the service serves - index.html the shell, app.js exporting the composition, api.js exporting createApi(baseUrl), one method per route answering the served JSON or { refusal, status }, app.css its layout
  iceberg
    statics: Table, Catalog, Compaction, IcebergOptions, ManifestFile, PartitionField, PartitionSpec, DataFile, ScanPlan, Snapshot, SnapshotRef, assignFieldIds, canPromote, schemaFromJson, schemaIntoJson
  iceberg.IcebergOptions
    instance: clone, compactAfterCommits, commitMaxBackoffMs, commitMinBackoffMs, commitRetries, compare, dataMimeType, equals, readParallelism, readParallelMinFileSize, readParallelMinFiles, stableHash, targetFileSize, writeParallelism, writeStaging
  iceberg.Catalog
    instance: append, namespace, namespaces, overwrite, properties, table, tables, updateProperties
  iceberg.Namespace
    instance: name, namespaces, properties, tables, updateProperties
  iceberg.Namespaces
    instance: create, entries, get, has, keys, names, openOrCreate, size, values
  iceberg.Tables
    instance: append, create, entries, get, has, keys, names, openOrCreate, overwrite, size, values
  iceberg.Table
    statics: create, open, openOrCreate
    instance: append, compact, createBranch, createTag, currentSnapshot, dataFiles, evolveSchema, expireSnapshots, fastForward, formatVersion, inspectFiles, inspectHistory, inspectSnapshots, location, manifests, manifestsAt, merge, mergeWhere, metadataFileName, metadataLocation, options, overwrite, overwriteWhere, plan, planAt, planMatching, properties, removeRef, root, scan, scanAt, scanMatching, scanRef, scanWhere, schema, schemas, setOptions, snapshotByRef, snapshots, spec, tableUuid, targetFileSize, toString, updateProperties, updateSchema, version
  iceberg.SchemaUpdate (via table.updateSchema())
    instance: addColumn, commit, dropColumn, makeNullable, renameColumn, updateDoc, updateType
  iceberg.PartitionSpec
    statics: fromJSON, identity, unpartitioned
    instance: clone, compare, equals, fields, intoJSON, isUnpartitioned, specId, stableHash, toJSON
  iceberg.PartitionField
    instance: clone, compare, equals, fieldId, name, sourceId, stableHash, transform
  iceberg.Snapshot
    instance: addedRows, clone, compare, equals, firstRowId, manifestList, operation, parentSnapshotId, schemaId, sequenceNumber, snapshotId, stableHash, summary, timestampMs
  iceberg.SnapshotRef
    instance: clone, compare, equals, kind, maxRefAgeMs, maxSnapshotAgeMs, minSnapshotsToKeep, snapshotId, stableHash
  iceberg.ManifestFile
    instance: addedFilesCount, addedRowsCount, addedSnapshotId, clone, compare, content, deletedFilesCount, deletedRowsCount, equals, existingFilesCount, existingRowsCount, firstRowId, manifestLength, manifestPath, minSequenceNumber, partitionSpecId, partitions, sequenceNumber, stableHash
  iceberg.DataFile
    instance: clone, columnSizes, compare, content, equals, filePath, fileSizeInBytes, lowerBounds, mimeType, nullValueCounts, partition, partitionNames, recordCount, sortOrderId, stableHash, toString, upperBounds, valueCounts
  iceberg.ScanPlan
    instance: clone, compare, equals, filesPlanned, filesSkipped, manifestsRead, manifestsSkipped, recordCount, stableHash
  iceberg.Compaction
    instance: bytesRewritten, clone, compare, equals, filesAfter, filesBefore, stableHash
  codec
    statics: from, into, fromStream, intoStream
    options: format, field, maxDepth, maxInputBytes, maxNodes, maxDocuments, indent, value
  fields
    statics: null, boolean, int8, int16, int32, int64, uint8, uint16, uint32, uint64, float16, float32, float64, datetime64, date32, date64, time, time32, time64, duration32, duration64, interval, bytes, binary, fixedSizeBinary, largeBinary, binaryView, largeBinaryView, sizedBinary, string, utf8, largeUtf8, utf8View, largeUtf8View, fixedUtf8, sizedUtf8, ascii, largeAscii, asciiView, largeAsciiView, fixedAscii, sizedAscii, cp1252, largeCp1252, cp1252View, largeCp1252View, fixedCp1252, sizedCp1252, uuid, version, url, urn, country, ccy, mic, cfi, isin, cusip, sedol, bloomberg, side, state, timeinforce, unit, serie, serieView, fixedSizeSerie, largeSerie, largeSerieView, struct, union, denseUnion, variant, geometry, geography, dictionary, decimal, bigdecimal, decimal32, decimal64, decimal128, decimal256, map, mapOf, runEndEncoded
    decimal(name, options?) -> DecimalField, the fixed decimal leaf - thirty-eight digits at scale eighteen, decimal128(38,18) storage under the yggdryl.decimal extension; decimal(name, precision, scale = 0, options?) -> DecimalWidthField, the narrowest of decimal32..decimal256; a scale with no precision is a TypeError ('decimal(): a scale needs a precision'), as Python raises, and options stated both in the precision's place and after it a TypeError; bigdecimal(name, options?) -> BigDecimalField, seventy-six digits at scale eighteen, decimal256(76,18) under yggdryl.bigdecimal; a value of either projects as the bigint of its units (defaultJSValue) and crosses asJs() back as the Scalar it is
    string(name, {layout?, charset?, bound?, fixed?, max?, ...field options}) / bytes(name, {layout?, bound?, fixed?, max?, ...field options}): the parameter keys build the datatype through DataType.string / DataType.bytes, every other key is a field option; one factory per string leaf - utf8, largeUtf8, utf8View, largeUtf8View, ascii, largeAscii, asciiView, largeAsciiView, cp1252, largeCp1252, cp1252View, largeCp1252View (name, value?), fixedUtf8 / fixedAscii / fixedCp1252 (name, width, value?), sizedUtf8 / sizedAscii / sizedCp1252 (name, max, value?) - and per byte leaf: fixedSizeBinary(name, byteWidth, value?), sizedBinary(name, max, value?)
  charset: CHARSETS, bom, canonicalName, decode, decodeLossy, encode, fromBom
  enums: charsets, codecs, compatibilitySchemes, dataTypeIds, dataTypeKinds, ioKinds, levels, pythonKinds, timeUnits, unionModes, ioModes, marketKinds, marketViews, mdUpdateActions, eventColumns, marketColumns, operationColumns; the last six the graph vocabulary's own listings - a MarketData.kind spelling, a MarketData.plan / applyView view spelling (orders, quotes, executions, trades, book_sides, books, lifecycle), a BookRef.action spelling, and the sixteen/nineteen/eight column names in schema order
  json
    statics: loads, load, dumps, dump, loadStream, dumpStream, loadsAll, loadAll, dumpAll, loadAllStream, dumpAllStream
  toml
    statics: loads, load, dumps, dump, loadStream, dumpStream
  xml
    statics: loads, load, dumps, dump, loadStream, dumpStream
  yaml
    statics: loads, load, dumps, dump, loadStream, dumpStream, loadsAll, loadAll, dumpAll, loadAllStream, dumpAllStream
