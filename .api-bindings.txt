# Binding API inventory
#
# Maintained by hand against Python's public stubs/package exports and
# JavaScript's public declarations/runtime exports: there is no generator, so a
# change that adds or retires a public name edits this file too.

### python: yggdryl
  FieldPath: __hash__, __len__, __reduce__, __repr__, __richcmp__, __str__, is_root, join, name, parent, root, segments, stable_hash
  TextEntries: __getitem__, __len__, __repr__, __str__, entry_by_path, get_entry_by_path
  TextEntry: __repr__, __richcmp__, __str__, entries, key, key_bytes, value, value_bytes
  TextLine: __init__(index, body: str | bytes | bytearray | memoryview, captures=None, options: TextOptions | None = None), __repr__, __richcmp__, __str__, body (the whole line, row header included), bodytype: MimeType, captures, crosscode: str, crosshashcode: int, crossuuid: Scalar (uuid), currhashcode: int, currunix: int, curruuid: Scalar (uuid), decoded_byte_size, dropped_byte_size, entries, entry_by_path, get_entry_by_path, index, mtime: int | None (the captured instant, else the handle's modification time), remove_entry_by_path, set_entry_by_path(path, value: str | bytes | bytearray | memoryview), sourceurl
    Rust-only: the TextLine Arrow converters media::text::into_arrow_batch, into_arrow_reader, from_arrow_batch, from_arrow_reader (no binding)
  TextLines: __iter__, __next__
  ArrowScalar: as_py, cast, column_size, field, from_, into_arrow_array, into_arrow_batch, into_arrow_reader, into_arrow_scalar, into_arrow_table, into_numpy, into_pandas, into_polars, into_scalar, is_consumed, is_streamed, row_size, shape
  StringEnum: __contains__, __eq__, __iter__, __len__, __repr__, from_json, from_logical_name, get, get_member, insert, into_intenum, into_json, into_members, member_name, members, name, prebuilt, remove
  StringParameters: __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __init__(layout='string', charset=None, bound=None), __le__, __lt__, __ne__, __reduce__, __repr__, __str__, bound, charset, fixed, layout, max, stable_hash; frozen: the declaration of a string column - one of the eighteen leaves, layout answering the canonical leaf name - what DataType.string reads and DataType.string_parameters answers; a charset is accepted only beside a charset-free spelling (string, fixed_string, string_view, large_string, large_string_view, varchar, char, ...)
  BytesParameters: __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __init__(layout='binary', bound=None), __le__, __lt__, __ne__, __reduce__, __repr__, __str__, bound, fixed, layout, max, stable_hash; frozen: the declaration of a byte column, what DataType.bytes reads and DataType.bytes_parameters answers
  Bound: column_indices, columns, eval, evaluate_arrow_batch, explain, field, filter_arrow_batch, filter_arrow_reader, filter_mask_arrow_batch, is_predicate, matches, partition_split, reads_rows, schema, statistics_certainty, statistics_prune, term
  BoundSelector: apply_arrow_batch, apply_arrow_reader, apply_row, explain, is_all, is_identity, output, projections, schema
  DataType: PARSE_RECURSION_LIMIT, __contains__, __iter__, __len__, arrow_scalar, ascii, ascii_packed, ascii_value, binary, binary_view, bytes, bytes_parameters, cast_arrow_array, cast_arrow_batch, cast_arrow_scalar, charset, code_name, code_width, crs, decimal, default_arrow_array, default_arrow_scalar, default_pyhint, default_scalar, dictionary_key, dictionary_value, duration32, duration64, edge_algorithm, equals, explode_fields, field, field_at, field_by_path, fixed_ascii, fixed_byte_width, fixed_size_binary, fixed_utf8, from_arrow, from_dict, from_fields, from_json, from_logical_name, from_pyhint, from_regex, from_str, from_toml, from_value, from_yaml, geography, geometry, get_field, get_field_at, get_field_by_path, has_default_crs, id, into_arrow, into_arrow_schema, into_dict, into_json, into_json_bytes, into_scheme_compat, into_toml, into_yaml, is_binary, is_bytes, is_code, is_decimal, is_default_value, is_floating, is_integer, is_nested, is_numeric, is_ordered, is_parameterized, is_signed_integer, is_string, is_temporal, is_unsigned_integer, is_wrapper, keys_sorted, kind, large_binary, large_utf8, logical_names, merge_with, pretty, remove_field, remove_field_at, remove_field_by_path, scalar, set_field, set_field_at, set_field_by_path, show_diff, show_diffs, stable_hash, string, string_parameters, time, timezone, union_mode, union_type_ids, unnest_fields, utf8, utf8_view, uuid_packed, uuid_value, validate, variant, with_fields
  Term: add, all, always_false, always_true, any, as_column, as_literal, at, attribute, attributes, between, bind, call, case, cast, check_budget, child, column, columns, compare, conjuncts, depth, divide, eq, explain, field, from_json, ge, glob, gt, has_attributes, ilike, into_json, is_always_false, is_always_true, is_in, is_literal, is_not_null, is_null, le, like, literal, lt, multiply, ne, negate, node_count, parameter, parameters, parse, path, remainder, simplify, slice, stable_hash, subtract, try_cast, typed_literal
  Filter: all, always_false, always_true, any, apply_arrow, apply_arrow_array, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_field, apply_records, attributes, bind, check_budget, columns, conjuncts, explain, from_json, has_attributes, into_json, is_always_false, is_always_true, parameters, parse, simplify, stable_hash, term
  Selector: __len__, all, all_except, apply_arrow, apply_arrow_array, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_field, apply_records, attributes, bind, check_budget, columns, excluded, explain, from_columns, from_field, from_json, into_field, into_json, is_all, is_columns, names, parameters, parse, projections, select, simplify, stable_hash, with_projection, without_columns
  Plan: apply_arrow, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_records, attributes, check_budget, columns, create_target, execute, explain, field, field_from, filter, from_field, from_json, into_expression, into_json, is_empty, is_identity, limit, merge_by, offset, ordering, parameters, parse, read_columns, read_sections, root_metadata, root_name, schema, selector, simplify, source, source_plan, stable_hash, verb, with_create, with_filter, with_limit, with_merge_by, with_offset, with_ordering, with_select, with_source, with_write, write_target
  Expression: apply_arrow, apply_arrow_array, apply_arrow_batch, apply_arrow_reader, apply_arrow_table, apply_field, apply_records, as_filter, as_plan, as_selector, attributes, check_budget, columns, explain, filter, from_json, into_json, is_filter, is_identity, is_plan, is_selector, is_sequence, kind, parameters, parse, plan, select, sequence, simplify, stable_hash, steps
  Records: __iter__, __next__, collect, field, from_arrow_reader, into_arrow_reader
  Field: __contains__, __iter__, __len__, accept, accept_encoding, accept_language, accept_ranges, alias, apply_arrow_batch, apply_arrow_reader, apply_arrow_schema, arrow, arrow_scalar, assign_parquet_field_ids, az, cache_control, canonicalize_value, cast, cast_arrow, cast_arrow_array, cast_arrow_batch, cast_arrow_reader, cast_arrow_scalar, cast_arrow_table, clear_properties, comment, content_disposition, content_encoding, content_language, content_length, content_location, content_range, content_type, default_arrow_array, default_arrow_scalar, default_pyhint, default_scalar, dictionary_id, dictionary_is_ordered, digest, digest_field_len, digest_field_names, digest_fields, display, dtype, equals, etag, expires, explode_fields, field, field_at, field_by_parquet_field_id, field_by_path, field_properties, file, fix, from_arrow, from_arrow_schema, from_dict, from_json, from_natural_value, from_pyhint, from_str, from_toml, from_value, from_yaml, get_field, get_field_at, get_field_by_path, get_property, glue, gs, has_partition_fields, has_property, http, http_location, iceberg, identity, index_of, into_arrow, into_arrow_schema, into_dataclass, into_dict, into_json, into_json_bytes, into_scheme_compat, into_toml, into_yaml, is_init, is_partition, is_struct, last_modified, layout_eq, location, max_parquet_field_id, media_type, merge_with, metadata, mime_type, mysql, name, nullable, only_digest_fields, only_partition_fields, pandas, parquet_field_id, partition, partition_field_len, partition_field_names, partition_fields, polars, postgres, postgresql, pretty, property_iter, protocol, python, range, remove_accept, remove_accept_encoding, remove_accept_language, remove_accept_ranges, remove_alias, remove_cache_control, remove_comment, remove_content_disposition, remove_content_encoding, remove_content_language, remove_content_length, remove_content_location, remove_content_range, remove_content_type, remove_display, remove_etag, remove_expires, remove_field, remove_field_at, remove_field_by_path, remove_http_location, remove_last_modified, remove_location, remove_media_type, remove_mime_type, remove_parquet_field_id, remove_property, remove_range, remove_string_enum, remove_vary, require_struct, s3, scalar, set_accept, set_accept_encoding, set_accept_language, set_accept_ranges, set_alias, set_cache_control, set_comment, set_content_disposition, set_content_encoding, set_content_language, set_content_length, set_content_location, set_content_range, set_content_type, set_dictionary_options, set_display, set_dtype, set_etag, set_expires, set_field, set_field_at, set_field_by_path, set_http_location, set_init, set_last_modified, set_location, set_media_type, set_metadata, set_mime_type, set_name, set_nullable, set_parquet_field_id, set_partition, set_property, set_range, set_string_enum, set_vary, show_diff, show_diffs, spark, sql, stable_hash, stable_layout_hash, string_enum, transform, unnest_fields, urn, validate, validate_struct_root, validate_value, vary, with_partition_fields, without_fields, without_partition_fields
  IOBase (the base class; construction answers the role subclass a name composes to, and buffered / into_text / into_coded / mkdir / create_dir answer the handle they built rather than self): __iter__, __len__, append, append_arrow_batch, append_arrow_reader, append_arrow_table, append_bytes, append_pandas, append_pandas_frame, append_polars, append_polars_frame, append_records, buffered, capacity, children_matching, children_where, clear, close, closed, codec, column_size, compress_into, copy_into, create_dir, cursor, decompress_into, delete_dir, delete_dir_contents, delete_file, delete_root_dir_contents, exists, filesystem, flush, from_bytes, from_fs, from_uri, glob, info, into_coded, into_media, into_text, is_atomic, is_dir, is_file, is_io, is_tabular, iterdir, joinpath, kind, ls, masked_uri, media_type, merge_arrow_batch, merge_arrow_reader, merge_arrow_table, merge_pandas, merge_pandas_frame, merge_polars, merge_polars_frame, merge_records, mkdir, move_into, name, normalize_path, open, open_append_stream, open_input_file, open_input_stream, open_output_stream, opened, overwrite_arrow_batch, overwrite_arrow_reader, overwrite_arrow_table, overwrite_pandas, overwrite_pandas_frame, overwrite_polars, overwrite_polars_frame, overwrite_records, parent, partitions, path, pread_exact, pstream_bytes, pwrite, pwrite_all, read_arrow_field, read_arrow_reader, read_arrow, read_bytes, read_digest, read_pandas, read_pandas_frame, read_parquet_geospatial_statistics, read_parquet_null_count, read_parquet_split_offsets, read_parquet_statistics, read_polars, read_polars_frame, read_range, read_range_bytes, read_range_digest, read_records, read_scalar, read_text, record_options, remove, reserve, rglob, row_size, same_location, scan_arrow, scan_polars, size, touch, truncate, unlink, uri, url, write_arrow_batch, write_arrow_reader, write_arrow_table, write_arrow, write_bytes, write_pandas, write_pandas_frame, write_polars, write_polars_frame, write_records, write_scalar, write_text
    properties: every record read and write takes options=None and **properties, each set on a copy of the options - the ones given, or the handle's own - by its own setter; an Ellipsis value is skipped; read_arrow(*, options=None, **properties) and write_arrow(value, mode="overwrite", *, options=None, **properties) are the shape-free pair, a structured text document reading only the field off them
  Xxh32: algorithm, apply_arrow_batch, as_digest, as_int, clear, seed, write_bytes, write_reader, write_scalar
  Xxh64: algorithm, apply_arrow_batch, as_digest, as_int, clear, seed, write_bytes, write_reader, write_scalar
  Xxh3: algorithm, apply_arrow_batch, as_digest, as_int, clear, secret, seed, write_bytes, write_reader, write_scalar
  Xxh128: algorithm, apply_arrow_batch, as_digest, as_int, clear, secret, seed, write_bytes, write_reader, write_scalar
  Digest: __bytes__, __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __index__, __int__, __le__, __len__, __lt__, __ne__, __reduce__, __repr__, __str__, algorithm, bits, from_bytes, from_int, stable_hash, width
  MediaType: base, charset, clear_encodings, encoding, encodings, extension, extensions, from_content_headers, from_extension, from_extensions, from_file_name, from_json, from_parts, from_path, from_str, from_value, into_json, is_application, is_archive, is_audio, is_binary, is_encoded, is_encoding, is_font, is_haptics, is_image, is_io, is_message, is_model, is_multipart, is_structured, is_tabular, is_text, is_textual, is_video, push_encoding, set_base, set_charset, set_encodings, stable_hash
  ProtocolField: __contains__, __iter__, __len__, add_branch, algorithm, apply_arrow_batch, branches, class_metadata, class_name, clear, comment, component, counter, derivation, description, directions, display, field_ref, get, group, has_branch, id, identifiers, import_path, is_coupled, is_holder, items, key, keys, kind, merge_with, module, msgtype, names, nulls, prefix, qualname, remove_algorithm, remove_role, remove_sources, remove_time, remove_unit, scheme, set, set_holder, sources, tag, tags, term, time, transform, unit, update, values
    FIX references: counter: int | None; component, field_ref, group, msgtype: str | None; each is a read/write property and assignment of None removes it
    FIX tags: tag, tags and counter admit positive i32 tags only - 0 (what an unresolved arrival entry records) or a negative is the core's ValueError, written or read back from stored metadata
    FIX identifiers: identifiers: list[str], direct scalar member names in component order; assignment accepts Iterable[str] of names, aliases or decimal tags, resolved by the native setter; empty input removes the property; invalid, ambiguous or duplicate selection raises ValueError atomically
    FIX directions: directions: list[FixDirection] (TypedDict code: str, patterns: list[str]), the FIX:directions rules of tag 385's field in the dictionary's order, [] when absent; assignable from any iterable of mappings, an empty one removes the property, a code outside the field's set, a code named twice under any spelling or a pattern the regex crate refuses is ValueError leaving the field unchanged
    FIX derivation: derivation: str | None, the FIX:derivation term in its canonical text - how the field's value is derived from the message where the message states none, evaluated by the enriching pass to a fixpoint; assignable from text, parsed through the expression grammar and stored canonical, a text that is not a term or one past the budget is ValueError leaving the field unchanged; assigning None removes the property
  MimeType: ARROW_FILE, ARROW_STREAM, AVRO, BROTLI, BZIP2, CBOR, COMPRESS, CSS, CSV, DIRECTORY, DOC, DOCX, FILE, FIX, FIXML, FIXUL, FLAC, GIF, GZIP, HTML, JAVASCRIPT, JPEG, JSON, JSON_LINES, LZ4, MARKDOWN, MESSAGE_PACK, MP3, MP4, OCTET_STREAM, ODS, OGG, ORC, OTF, PARQUET, PDF, PLAIN_TEXT, PNG, PROTOBUF, PUFFIN, RAR, SEVEN_ZIP, SNAPPY, SQLITE3, SVG, TAR, TOML, TSV, TTF, ULLINK, WAV, WEBM, WEBP, WOFF, WOFF2, XLS, XLSX, XML, XZ, YAML, ZIP, ZLIB, ZSTD, content_coding, extension, format, from_content_coding, from_content_type, from_extension, from_json, from_local_path, from_path, from_str, from_value, into_json, is_application, is_archive, is_audio, is_binary, is_directory, is_encoding, is_filesystem, is_font, is_haptics, is_image, is_io, is_known, is_message, is_model, is_multipart, is_structured, is_tabular, is_text, is_textual, is_video, stable_hash, structured_suffix, subtype, top_level
  PythonMetadata: __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __le__, __lt__, __ne__, __reduce__, __repr__, __str__, class_name, from_type, import_path, is_importable, is_keyword_constructed, kind, module, properties, qualname, stable_hash
  RecordOptions: apply_arrow_batch, apply_arrow_reader, batch_row_size, block_codec, commit_row_size, compression, field, filter, for_media_type, key_value_metadata, level, limit_arrow_reader, max_byte_size, max_row_group_size, max_row_size, merge_by, mime_type, name, partition_pairs, plan, remove_field, require_field, safe, select, stable_hash, sync_marker, timezone, write_batch_row_size
    sections: field, filter, select, merge_by and plan setters take any value Scalar.from_ reads - a native value, text, a list of projections or conditions, a dict of aliases, None - and the core reads it with from_scalar
  Statement: all, bind, from_json, into_json, is_all, limit, ordering, predicate, projections, select, stable_hash, with_limit, with_ordering, with_predicate
  TextOptions: apply_arrow_batch, apply_arrow_reader, autotype, batch_row_size, capture_names, commit_row_size, field, filter, framing, leading_fragment, level, lift_names, limit_arrow_reader, linesep, lstrip, max_byte_size, max_record_byte_size, max_row_size, merge_by, mime_type, name, parse_mtime, partition_pairs, plan, remove_field, rename_columns, require_field, rowheader, rstrip, safe, select, source_field, stable_hash, start_rownum, timezone, write_batch_row_size
  Timezone: NAIVE, UTC, abbreviation_at, aliases, from_offset, from_value, into_local, into_utc, is_fixed, is_known, is_naive, is_saving_at, is_utc, key, observes_saving, offset_at, registered, stable_hash, standard_offset, utcoffset
  Uri: __contains__, __iter__, __len__, account, authority, bucket, clear_extensions, default_port, extension, extensions, file_name, fragment, from_json, from_parts, from_path, from_str, from_value, has_authority, host_port, hostname, into_json, into_path, into_url, into_urn, is_storage, is_virtual_hosted, joinpath, key, media_type, mime_type, parameters, parent, parents, parts, password, path, path_segments, path_text, port, query, region, remove_extension, scheme, set_extension, set_extensions, set_file_name, set_media_type, set_mime_type, set_query, set_stem, stable_hash, stem, store_endpoint, user, validate
  Url: __contains__, __iter__, __len__, account, as_posix, as_uri, authority, bucket, clear_extensions, default_port, exists, extension, extensions, file_name, fragment, from_json, from_path, from_str, from_uri, from_value, full_match, full_match_under, glob_parts, host_port, hostname, into_json, into_path, into_uri, is_absolute, is_dir, is_file, is_glob, is_local, is_partitioned, is_pattern, is_private, is_recursive_glob, is_relative_to, is_storage, is_virtual_hosted, joinpath, key, local_mime_type, match, media_type, mime_type, name, parameters, parent, parents, partition, partitions, partitions_under, parts, password, path, path_segments, path_text, port, query, region, relative_to, remove_extension, scheme, set_extension, set_extensions, set_file_name, set_media_type, set_mime_type, set_query, set_stem, stable_hash, stem, store_endpoint, suffix, suffixes, user, with_name, with_partition, with_stem, with_suffix
  Urn: authority, clear_extensions, extension, extensions, file_name, fragment, from_json, from_str, from_uri, from_value, into_json, into_uri, media_type, mime_type, namespace, namespace_specific, path, path_segments, query, remove_extension, scheme, set_extension, set_extensions, set_file_name, set_media_type, set_mime_type, set_stem, stable_hash, stem
  Scalar: __bool__, __contains__, __iter__, __len__, absolute, add, as_bool, as_bytes, as_float, as_int, as_py, as_str, as_value_bytes, at, count, decimal, digest, divide, dtype, duration, family, from_, from_enum, from_struct, from_variant_bytes, get, get_or, has, id, into_array_field, into_arrow_array, into_arrow_batch, into_arrow_scalar, into_arrow_table, into_field, into_json, into_json_bytes, into_struct_field, into_variant_bytes, is_container, is_empty, is_integer, is_null, is_number, items, keys, kind, multiply, negate, path, remainder, remove, scale, set, stable_hash, subtract, unit, unscaled, values, zone
    from_(value): a pyarrow container, a pandas or polars frame or series, a numpy array or an Arrow C exporter lands as the Arrow variant holding an ArrowScalar, buffers shared; as_py() hands the ArrowScalar back; a Term, Filter, Selector, Plan or Expression crosses as its canonical text
  ArrowCastPlan: apply, field, nullability, preflight, representation, safe, schema, source_schema
  Bounds: attribute, column, from_partitions, with_attribute, with_column
  Digester: algorithm, apply_arrow_batch, as_digest, as_int, clear, write_bytes, write_reader, write_scalar
  TxHash: __bytes__, __copy__, __deepcopy__, __eq__, __ge__, __gt__, __hash__, __le__, __len__, __lt__, __ne__, __reduce__, __repr__, __str__, algorithm, digest, dtype, from_bytes, from_parts, into_datetime, into_scalar, into_uuid, stable_hash, unit, unix, width, with_unit
    into_uuid() -> Scalar: the lossy RFC 9562 UUIDv7 projection (the instant restated to signed nanoseconds and floored to the microsecond, then the digest's low 62 bits; neither unit nor algorithm survives) as a uuid-typed Scalar, because the binding has no Uuid wrapper class - as_py() answers the hyphenated str; a digest that is not 64 bits wide, an instant past signed 64-bit nanoseconds, or one a UUIDv7 cannot hold, is ValueError
  TxHasher: algorithm, apply_arrow_batch, column_txhashes, digest, digest_scalar, dtype, from_state, row_txhashes, unit, unix_of, width
  Version: Version(major: int, minor: int = 0, patch: int = 0); from_str(value: str) -> Version; readonly major/minor/patch: int; stable_hash() -> int; numeric comparison, hash, copy, deepcopy, pickle (major, minor, patch); widths u8/u8/u16, exactly four native bytes

  expression: COMPARISONS, FUNCTIONS, HOLDER_ATTRIBUTES, VERBS, Bound, Bounds, BoundSelector, Expression, Filter, Plan, Records, Selector, Term, UserFunction, needs_quoting, unregister_user_function, user_defined_filter, user_defined_function, user_function_signature, user_functions
    user functions: @user_defined_function(namespace="py", name=None, parameters=None, returns=None, vectorized=False) and @user_defined_filter register a callable as namespace.name from its type hints and defaults; the wrapper stays callable and answers term(*args), where(*args), signature (the struct Field), unregister(); Term.call takes the qualified name
    native: register_user_function(namespace, name, parameters, returns, callable, *, defaults=None, vectorized=False) -> Field, unregister_user_function(name) -> bool, user_functions() -> list[str], user_function_signature(name) -> Field | None
  media.partition: NULL_PARTITION, DEFAULT_RECORD_BATCH_ROW_SIZE, AVRO_MAX_SCHEMA_DEPTH, partition_text, with_partitions, without_partitions
  root constants: DEFAULT_FETCH_BYTE_SIZE, DEFAULT_STREAM_BATCH_SIZE, IPC_DICTIONARY_IDS_KEY
  __version__: (module or value)
  charset: CHARSETS, bom, canonical_name, decode, decode_lossy, encode, from_bom
  enums: AsciiCode, CFI, CHARSETS, CODECS, COMPATIBILITY_SCHEMES, CfiCode, Country, CountryCode, Currency, CurrencyCode, DATA_TYPE_IDS, DATA_TYPE_KINDS, DIGEST_ALGORITHMS, EDGE_ALGORITHMS, FORMATS, IO_KINDS, IO_MODES, IO_WRITE_MODES, LEADING_FRAGMENTS, LEVELS, MIC, MicCode, NULLABILITIES, PYTHON_KINDS, REPRESENTATIONS, TIME_UNITS, UNION_MODES, fixed_ascii
  enums.AsciiCode: the base of the fixed US-ASCII widths fixed_ascii(width) builds (at most sixteen bytes) and the four registered code bases (CountryCode, CurrencyCode, MicCode, CfiCode); lives in yggdryl.enums.string, declares through StringEnum / Field.set_string_enum
    classmethods: as_enum, dtype, from_code, from_field, from_str, into_field
    instance: into_str
    protocol: an open IntEnum whose member is its packed ASCII value and whose str/format is that value's text; a subclass body is the caller's vocabulary, so a member spelled as any of the seven names listed above is refused at class creation rather than shadowing it, and a stored declaration naming one is a ValueError
  enums.Country / enums.Currency / enums.MIC / enums.CFI: the shipped vocabularies, over CountryCode, CurrencyCode, MicCode, and CfiCode
  scalar classes: into_field (cached staticmethod; the class's non-null Struct root, not the leaf conversion Scalar.into_field names)
  combined: (module or value)
  refresh_logging: drops the cached Python log levels, so a level set after import applies
  coding: Coded, Gzip, Identity, Zlib, Zstd, gzip, zlib, zstd
  coding roles: Coded is the IOBase subclass presenting the decoded bytes of a coding; Identity, Gzip, Zlib and Zstd sit under it, and Codec.Deflate folds into Zlib. Coded: into_handle, level
  holder: Buffer, Buffered, File, Folder, FsFile, FsFolder, FsPath, IOBase, IOCursor, ObjectFile, ObjectFolder, ObjectPath, Path
  holder roles: every name above but IOBase and IOCursor is an IOBase subclass naming one core holder; construction answers the composition a name declares, so type(handle) says which implementation answers the contract
    Buffer: in-memory bytes; built with IOBase.from_bytes
    Path / File / Folder: the local roles, each constructible from a location; Path is what a name declaring nothing takes, and what addresses the stored bytes of a coded name
    Folder: classmethods temporary, home, config
    FsPath / FsFile / FsFolder: the same three roles on a foreign filesystem, each constructible as (filesystem, path, *, uri=None)
    ObjectPath / ObjectFile / ObjectFolder: the same three roles on Amazon S3, Google Cloud Storage, and Azure Blob Storage, each constructible as (location) or as (container, key, provider=) for the raw name a store uses - a raw name, unlike a location, does not say which store holds it - with an optional options= mapping in PyIceberg's s3./gcs./adls. names, PyArrow's, or each store's own environment names
    Buffered: cached_bytes, cached_pages, has_cached_page, clear_cache, into_handle, and the normalized
      configuration the core clamped to: page_size, max_bytes, ttl, page_index, page_start
  media: AVRO_MAX_SCHEMA_DEPTH, Avro, DEFAULT_RECORD_BATCH_ROW_SIZE, Ipc, Media, MediaType, MimeType, NULL_PARTITION, Parquet, RecordOptions, Text, TextOptions, avro, iceberg, partition_text, with_partitions, without_partitions
  media roles: Media is the IOBase subclass retaining a record implementation, with Ipc, Parquet and Avro under it; Text is its own holder variant and sits directly under IOBase. Media, Text: into_handle
  text: codec, json, toml, yaml
  IOBase text decode: read_text_lines(*, options) answers a lazy TextLines iterator; every record method routes through the same decode
  field: pure Field builder for native/Arrow/dataclass shapes, with optional name
  types: ArrowCastPlan, BloombergField, BooleanField, BytesField, BytesParameters, CfiField, CountryField, CurrencyField, CusipField, DataType, Date32Field, Date64Field, DateTime64Field, Decimal128Field, Decimal256Field, Decimal32Field, Decimal64Field, DecimalField, DenseUnionField, DictionaryField, Duration32Field, Duration64Field, Field, FieldPath, FixedSizeListField, Float16Field, Float32Field, Float64Field, GeographyField, GeometryField, Int16Field, Int32Field, Int64Field, Int8Field, IntervalField, IsinField, LargeListField, LargeListViewField, ListField, ListViewField, MapField, MicField, NullField, ProtocolField, PythonMetadata, RunEndEncodedField, Scalar, SedolField, SideField, StateField, StringEnum, StringField, StringParameters, StructField, Time32Field, Time64Field, TimeField, TimeInForceField, Timezone, TypedDataType, TypedField, UInt16Field, UInt32Field, UInt64Field, UInt8Field, UnionField, UrlField, UrnField, UuidField, VariantField, Version, VersionField, ascii, ascii_view, binary, binary_view, bloomberg, boolean, bytes, cfi, country, cp1252, cp1252_view, currency, cusip, date32, date64, datetime64, decimal, decimal128, decimal256, decimal32, decimal64, dense_union, dictionary, duration32, duration64, field, fixed_ascii, fixed_cp1252, fixed_size_binary, fixed_size_list, fixed_utf8, float16, float32, float64, geography, geometry, int16, int32, int64, int8, interval, isin, large_ascii, large_ascii_view, large_binary, large_binary_view, large_cp1252, large_cp1252_view, large_list, large_list_view, large_utf8, large_utf8_view, list, list_view, map, map_of, mic, null, run_end_encoded, scalar, sedol, side, sized_ascii, sized_binary, sized_cp1252, sized_utf8, state, string, struct, time, time32, time64, timeinforce, uint16, uint32, uint64, uint8, union, url, urn, utf8, utf8_view, uuid, variant, version (the package's __all__)
  types.VersionField: TypedField[Literal["version"], Version]; types.version(name: str, *, nullable: bool = True, metadata: MetadataInput = None) -> VersionField; scalar conversion uses the existing Field/Scalar boundary
  types.UrlField: TypedField[Literal["url"], str]; types.url(name: str, *, nullable: bool = True, metadata: MetadataInput = None) -> UrlField; types.UrnField: TypedField[Literal["urn"], str]; types.urn(name: str, *, nullable: bool = True, metadata: MetadataInput = None) -> UrnField; the two leaves of the uri family; a str in, the canonical text out through as_py
  types.StringField: TypedField[Literal["utf8", "large_utf8", "utf8_view", "large_utf8_view", "fixed_utf8", "sized_utf8", "ascii", "large_ascii", "ascii_view", "large_ascii_view", "fixed_ascii", "sized_ascii", "cp1252", "large_cp1252", "cp1252_view", "large_cp1252_view", "fixed_cp1252", "sized_cp1252"], str]; types.string(name, *, layout="string", charset=None, fixed=None, max=None, nullable=True, metadata=None) -> StringField (fixed and max together: TypeError; a charset only beside a charset-free layout); one factory per leaf: utf8/large_utf8/utf8_view/large_utf8_view/ascii/large_ascii/ascii_view/large_ascii_view/cp1252/large_cp1252/cp1252_view/large_cp1252_view(name, *, nullable, metadata), fixed_utf8/fixed_ascii/fixed_cp1252(name, width, *, nullable, metadata), sized_utf8/sized_ascii/sized_cp1252(name, max, *, nullable, metadata)
  types.BytesField: TypedField[Literal["binary", "fixed_size_binary", "large_binary", "binary_view"], bytes]; types.bytes(name, *, layout="binary", fixed=None, max=None, nullable=True, metadata=None) -> BytesField (fixed and max together: TypeError); binary/large_binary/binary_view(name, *, nullable, metadata) are it with a layout picked once; fixed_size_binary(name, byte_width, *, nullable, metadata) the fixed layout
  fix: FixCapture, FixCodec, FixHeader, FixMessages, FixMsg, FixRegistry, MarketEventData, MsgType, fix_cfb_fields, fix_crate_fields, fix_schema, fix_schema_carrying, fix_schema_tags, global_registry, install_global_registry
  fix.FixRegistry
    statics: from_fields(fields: Iterable[FieldLike]) -> FixRegistry; from_handle(location: IOBase | Url | str | PathLike[str]) -> FixRegistry; from_cfb_file(location: IOBase | Url | str | PathLike[str], dialect: str | None = None) -> tuple[FixRegistry, list[Field]], the dialect stamped as FIX:branches on every field and definition the file produces; from_json(document: str) -> FixRegistry
    mutations: add_field(field: FieldLike) -> bool; add_fields(fields: Iterable[FieldLike]) -> tuple[int, int]; merge_with(other: FixRegistry) -> tuple[int, int]; add_cfb_file(location: IOBase | Url | str | PathLike[str], dialect: str | None = None) -> tuple[int, int], the dialect or the location's stem; add_cfb_files(location: IOBase | Url | str | PathLike[str], pattern: str, dialect: str | None = None) -> tuple[int, int, int], (files, added, merged) over the core glob walk; add_json_file(location: IOBase | Url | str | PathLike[str]) -> tuple[int, int]; insert(field: FieldLike) -> Field | None; update(field: FieldLike) -> None; remove(key: int | str) -> Field | None; remove_by_id(id: int) -> Field | None
    one namespace: insert files a definition by the shape it has - a Struct is a component, a List of Structs or a Map a group, a message a component carrying FIX:msgtype - and there is no category argument and no definition door of its own: insert/update/remove and the field_by_* pairs reach scalar fields, components and groups alike, a definition answering the tag door under the identity the catalog derived for it
    fields: get_field(key: int | str) -> Field | None; field(key: int | str) -> Field; get_field_by_id(id: int) -> Field | None; field_by_id(id: int) -> Field; get_field_by_tag(tag: int) -> Field | None; field_by_tag(tag: int) -> Field; get_field_by_counter(tag: int) -> Field | None; field_by_counter(tag: int) -> Field, the repeating group one counter tag opens (two groups on one counter name nothing); get_field_by_name(name: str) -> Field | None; field_by_name(name: str) -> Field, a scalar before a component before a group; get_field_by_path(path: str | FieldPath) -> Field | None; field_by_path(path: str | FieldPath) -> Field
    messages: register_msgtype(spelling: str, name: str | None = None, description: str | None = None) -> MsgType; get_msgtype(spelling: str) -> MsgType | None; msgtype(spelling: str) -> MsgType, an exact wire code, then a folded canonical name, then an alias of tag 35's code set
    membership: dialects() -> list[str], the distinct FIX:branches names any field or definition carries, sorted; membership is provenance a caller filters on and no lookup consults it
    snapshots: into_json() -> str; stable_hash() -> int; write_into(location: IOBase | Url | str | PathLike[str]) -> None, shards named by their tag's hundred nine digits wide (tag 55 -> fields/000000000.json, tag 65003 -> fields/000000650.json) beside components/ and groups/, the crate's own dump among them as components/fixmsg.json, groups/identifiers.json and groups/metadata.json; __copy__()/__deepcopy__(memo: Any) -> FixRegistry; __reduce__() -> tuple[object, tuple[str]]
    keys: an int tag fits i32 and excludes bool; a str is a name/dotted path; a counter is the tag of the int32 counting a group's occurrences; bare queries use the core deterministic match - canonical tag before alternate, canonical name before alias, one fold; an identifier is the int field.fix.id answers and is read only by the *_by_id methods (bool refused, outside i32 is OverflowError); absent required lookup raises KeyError, invalid mutation raises ValueError
    contents: every registry holds the crate's own definitions from construction - fix_crate_fields() in tag order from 65003, the two Map groups identifiers (65020) and metadata (65049) among them - and seeds the standard clocks SendingTime (52) and TransactTime (60) beside them; len() counts the scalar fields, the components and the groups, while __iter__ walks the scalars alone
    protocol: mutable and unhashable; equality includes every definition; __len__/__bool__/__contains__/__getitem__ operate on the one namespace; __iter__ -> FixFieldIterator lazily follows canonical identifiers, tag-major then id; mutations refuse while a message, a MsgType view, an unfinished iterator or the process default shares the registry
  fix.MsgType
    readonly: name: str (the folded canonical name), value: str (the exact wire code), field: Field (read-only); get_group_by_tag(tag: int) -> Field | None; stable_hash() -> int
    selection: identifier_values(message: FixMsg) -> list[tuple[Field, Scalar]], the compiled native selection of non-null direct members in declaration order; declaration Fields are read-only, Scalars retain their native types; no nested-group traversal
    protocol: an immutable copy of the definition beside the registry it came from, which it keeps shared; native equality/order/hash, __str__() -> str, copy/deepcopy -> MsgType, __reduce__() -> tuple[object, tuple[str, str]] carrying the registry snapshot and the definition's name
  native FIX iterators
    FixFieldIterator: Iterator[Field]; FixMsgIterator: Iterator[tuple[str, Scalar]]; FixMessages: Iterator[FixMsg]; each returns self from __iter__, yields one native item from __next__, retains bounded native cursor state and is unhashable
    FixMessages is what every FixCodec stage answers - one line's messages, none, one or many a row, lines or records parsed, messages chained by the lifecycle, a batch read back; a Python iterable behind it is pulled one item at a time; a refused line raises ValueError where it is met and the stream continues, a failure of the iterable raises as itself and ends it
  fix.FixMsg
    constructor: FixMsg(field: FieldLike, value: Scalar | Mapping[str, Any] | Sequence[Any], registry: FixRegistry | None = None); a child stating a typed fact fills the holder that owns it and leaves the row, and SendingTime is the stated one else UTC now
    statics: from_row(schema: FieldLike, row: Scalar | Mapping[str, Any] | Sequence[Any], registry: FixRegistry | None = None) -> FixMsg, the inverse of into_row with the content rebuilt from the fixentries column and no clock read
    readonly: registry: FixRegistry, field: Field (the content row's schema), value: Scalar (the content row)
    holders: event() -> MarketEventData; header() -> FixHeader (the frame, the trailer's signaturelength/signature/checksum included); capture() -> FixCapture; the FIX numbers and identifiers the message lifted are reached by their own tags - by_tag(44), by_tag(11) - and there is no lifted() view of them; text: str | None (tag 58); metadata: dict[str, str] (what a bridge stated under its own namespaces, folded, sorted)
    graph facts, as properties answering what event() answers: curruuid: Scalar (uuid), crossuuid: Scalar (uuid), crosscode: str, currhashcode: int, crosshashcode: int, currunix: int (ns since the epoch, UTC), state: Scalar (state code), seqnum: int, prevuuid: Scalar | None, parentuuids: list[Scalar] (the whole chain before it, oldest first), srcuuids: list[Scalar] (the text line it was parsed out of; none for bytes), carried: list[tuple[str, Scalar]] (the capture's own cells the message carries, each under its column; empty for bytes), identifiers: dict[str, str], px: Scalar, qty: Scalar, side: Scalar, currency: Scalar, and the instrument's own isincode / cusipcode / sedolcode / bloombergcode / cficode / miccode (Scalar | None), each read off FIX's fields and answered by no column
    typed tags: a lookup by a frame tag {8,35,49,56,34,52,43,385,93,89,10}, a crate tag (65000..65100) but sourceurl (65026), one of the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003} or 58 answers its holder, typed as its column is (by_tag(35) is text, by_tag(52) a nanosecond UTC datetime64, by_tag(44) an exact decimal128(38,18), a crate identity a uuid); every other key reaches the content row, Side(54) and Currency(15) among it
    writes: set(key: int | str, value: object) -> None, a typed key its holder (None clears it) and any other key the row, typed by the field the key resolves to, KeyError for a key reaching nothing, and ValueError for a refused value or for a key reaching the capture's own column - sourceurl - which no message holds; remove(key: int | str) -> Scalar | None, clearing a typed fact or removing a row child; every write settles the identity again - the cross code, the hash code and the two identities
    lookups: get_by_id(id: int) -> Scalar | None; by_id(id: int) -> Scalar; get_by_tag(tag: int) -> Scalar | None; by_tag(tag: int) -> Scalar; get_by_name(name: str) -> Scalar | None; by_name(name: str) -> Scalar; get_by_path(path: str | FieldPath) -> Scalar | None; by_path(path: str | FieldPath) -> Scalar; get(key: int | str, default: object = None, /) -> object; each answers an owned Scalar
    views: stable_hash() -> int; digest() -> bytes, sixteen big-endian bytes over the wire entries; entries() -> list[FixEntryTuple], the content row as a tree of (tag, name, value | None, entries) tuples - a group one tuple under its counter with the count as its value and an occurrence tuple per member set, a component a tuple with no value, tag 0 for a key no dictionary resolved, and never a typed fact; into_row(schema: FieldLike) -> Scalar; into_bytes(separator: int = 1) -> bytes; into_text(separator: str = "\x01") -> str, both emitting the header and the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003} in front of the content (SendingTime only when the message stated it) and the trailer 93, 89, 10 behind it, a coded fact as its wire code
    protocol: native facts+row equality/hash against the registry, and a hashed message is frozen - set/remove after hash() raise TypeError, a copy takes writes again; copy/deepcopy -> FixMsg; __reduce__() -> tuple[object, tuple[str, str, str]] carrying the row with the typed facts appended as their own columns, the value and the registry snapshot; __iter__ -> FixMsgIterator of (name, Scalar) pairs over the content row; __len__ counts its children
  fix.FixHeader
    readonly: beginstring: str, msgtype: str, sendercompid: str | None, targetcompid: str | None, msgseqnum: int | None, sendingtime: int (ns since the epoch, UTC, as the intake settled it), stated_sendingtime: bool (only a stated one goes back on the wire), possdupflag: bool | None, msgdirection: str | None, and the trailer's signaturelength: int | None, signature: bytes | None, checksum: str | None
    protocol: an immutable copy of the message's header; equality and hash over its facts, copy/deepcopy -> FixHeader, __repr__
  fix.FixCapture
    readonly: msgpluginid: str | None, msgctxid: str | None, msgsessionid: str | None
    protocol: an immutable copy of what the line's own bridge row header stated about the capture it was written for; what a reader said about the line - the object it came out of - is the capture's own column, carried by the message and stated again by into_row; none of it is FIX or content, so none of it is an entry or on the wire, and the session instance and the context name the chain where the row header brackets both, through the cross code, which the currhashcode leaves out; equality and hash over its facts, copy/deepcopy -> FixCapture, __repr__
  fix.MarketEventData
    readonly identity: curruuid: Scalar (uuid), crossuuid: Scalar (uuid), crosscode: str, currhashcode: int, crosshashcode: int, identifiers: dict[str, str], parentuuids: list[Scalar], srcuuids: list[Scalar]
    readonly event: currunix: int, state: Scalar, seqnum: int, creaunix: int | None, expirunix: int | None, prevunix: int | None, prevuuid: Scalar | None, snapunix: int | None; every instant is nanoseconds since the epoch, UTC
    readonly market: px: Scalar, qty: Scalar, currency: Scalar, unit: str, side: Scalar, isincode/cusipcode/sedolcode/bloombergcode/cficode/miccode: Scalar | None, lanes bidpx/bidqty/askpx/askqty: Scalar | None, bidcurrency/askcurrency: Scalar | None, bidunit/askunit: str | None
    protocol: an immutable copy of the message's event; equality over every fact and hash over the code they digest to, copy/deepcopy -> MarketEventData, __repr__; a zero px or qty, an XXX currency, an UNKNOWN side and a 00UNKNOWN state are what stating nothing looks like
  fix.FixCodec
    constructor: FixCodec(registry: FixRegistry | None = None, *, default_sending_time: Scalar | datetime.datetime | None = None, separator: int | None = None, payload_column: str = "body", capture_names: Sequence[str] | None = None, null_values: Sequence[str] | None = None, direction: str | None = None, batch_byte_size: int | None = None, batch_row_size: int | None = None, include_msgtypes: Sequence[str] | None = None, exclude_msgtypes: Sequence[str] | None = None, threads: int | None = None); default_sending_time redirects to try_with_default_sending_time - a Scalar crosses as itself and must already be a nanosecond UTC datetime64, a datetime is cast once through that clock, any other layout is the core's ValueError; direction is any spelling of a code of tag 385's set; a pin is on the codec, a stage is a call
    readonly: registry: FixRegistry; default_sending_time: Scalar | None; separator: int | None; payload_column: str; null_values: list[str]; direction: str | None; batch_byte_size: int, the core's 128 MiB when unstated; batch_row_size: int, the core's 32,768 when unstated, a batch closing on whichever bound it reaches first; threads: int, 1 when unstated - the threads the line and row doors read on, more reading a stream a chunk ahead, each line on some thread, answered in the lines' order, zero reading as one; include_msgtypes: list[str]; exclude_msgtypes: list[str], the core's Heartbeat, TestRequest and "unknown" when unstated, each read before a frame is built and spelled as a code or a name, and an empty exclude_msgtypes keeps every type
    statics: infer_msgtype_bytes(body: bytes | bytearray | memoryview) -> bytes | None; infer_msgtype_text(body: str) -> str | None
    parsing, streams: parse_line(row: bytes | bytearray | memoryview) -> FixMessages; parse_lines(lines: Iterable[bytes | bytearray | memoryview]) -> FixMessages, lazy; parse_text_line(line: TextLine) -> FixMessages; parse_text_lines(lines: Iterable[TextLine]) -> FixMessages, lazy
    parsing, one message: parse_fix_line(body: bytes | bytearray | memoryview) -> FixMsg; parse_ullink_line(body: bytes | bytearray | memoryview) -> FixMsg; parse_fixml_line(body: bytes | bytearray | memoryview) -> FixMsg; parse_pairs(pairs: Sequence[tuple[str, str]]) -> FixMsg; each takes one frame, and a body holding a second is ValueError naming the byte that second opens at
    what a parse does: builds the message, lifts its typed facts onto their holders, explodes a nested XmlData into it, restates deprecated fields to their latest aliases, runs the dictionary's FIX:derivation rules, fills the identifiers the message component declares and an order's lanes, then settles the identity - there is no enrich step and no enrich door
    chaining: lifecycle(messages: Iterable[FixMsg]) -> FixMessages, lazy, the one walk: sorted by instant, each message stated as the one after the live message it follows under its cross identity, carrying prevuuid, prevunix, seqnum, the whole chain before it as its parentuuids and the lifecycle's creaunix, and settled again around them; srcuuids never travel along a chain
    formatting: format_messages(messages: Iterable[FixMsg], field: FieldLike) -> list[Scalar], each message as the row one message field of the registry holds it, a column that field carries and the message does not lifted off its content record first
    Arrow twins, over pyarrow.RecordBatchReader / Table / RecordBatch / any Arrow C stream exporter, answering pyarrow.RecordBatchReader: parse_text_arrow_reader(source) -> capture batches to batches of FIX rows, carried columns leading, batches closed on the bytes each row lands as against batch_byte_size; lifecycle_arrow_reader(source) -> the same schema in and out, the rows walked as one lifecycle with the record untouched, each row's capture cells carried by its message through the walk's own ordering and stated again at their own columns; format_arrow_reader(source, field: FieldLike) -> the format twin, batches under field; messages(source) -> FixMessages, lazy, one batch held at a time, each row's own cells carried into its message, so an arrow_reader over them states the capture's own columns again; arrow_reader(schema: FieldLike, messages: Iterable[FixMsg]) -> pyarrow.RecordBatchReader; write_arrow_reader(source, sink: IO[bytes]) -> int lines written, refused before a row when the fixentries column is absent
    protocol: copy/deepcopy preserves the shared registry; unhashable
  fix functions
    fix_schema(registry: FixRegistry | None = None, name: str = "fix") -> Field, the one fixed row a whole capture lands in - the crate's own columns in bands (when, which event, which message and session, the instrument, the order chain, the values, how it went, the groups, the frame), each spelled by the dictionary's folded canonical name with its tag on FIX:tag, one fixentries list closing the row under the nofixentries that counts it; beginstring, currunix, creaunix, currhashcode, crosshashcode, curruuid and crossuuid are the non-null columns, and a column is read by name rather than by position
    fix_schema_carrying(carrier: FieldLike, read: FieldLike) -> Field, a capture's own columns in front, dropping a capture column whose folded name a FIX column already takes; fix_schema_tags() -> list[int]; fix_crate_fields() -> list[Field], what the crate adds beside the specification in tag order from 65003; fix_cfb_fields(location: IOBase | Url | str | PathLike[str], dialect: str | None = None) -> list[Field]; global_registry() -> FixRegistry; install_global_registry(registry: FixRegistry) -> None
  media.iceberg: Catalog, Compaction, DataFile, IcebergOptions, ManifestFile, Namespace, Namespaces, PartitionField, PartitionSpec, ScanPlan, SchemaUpdate, Snapshot, Table, Tables, assign_field_ids, can_promote, schema_from_json, schema_into_json
  scalar: decorator installing cached static into_field()
  media.avro
    exports: Block, BlockIterator, Container, Schema, blocks, dumps, dumps_single, loads, loads_single
    decode limits: max_depth, max_input_bytes, max_nodes; blocks is a fused lazy iterator
  media.avro.Schema
    statics: from_value
    instance: fingerprint, into_canonical_form, into_json, kind, stable_hash
  media.avro.Container
    instance: get, metadata, rows, schema, stable_hash
  media.avro.BlockIterator
    instance: get, metadata, schema, __iter__, __next__
  media.avro.Block
    instance: count, rows, size
    protocol: intentionally unhashable lazy block with resolution context
  media.iceberg.IcebergOptions
    instance: commit_max_backoff_ms, commit_min_backoff_ms, commit_retries, compact_after_commits, data_mime_type, read_parallel_min_file_size, read_parallel_min_files, read_parallelism, stable_hash, target_file_size, write_parallelism, write_staging
    protocol: native equality/order; hashing freezes all setters; copy/pickle unlock
  media.iceberg.Compaction
    instance: bytes_rewritten, files_after, files_before, stable_hash
  media.iceberg.PartitionField
    statics: from_json
    instance: field_id, into_json, name, source_id, stable_hash, transform
  media.iceberg.PartitionSpec
    statics: from_json, identity, unpartitioned
    instance: fields, into_json, is_unpartitioned, spec_id, stable_hash
  media.iceberg.Snapshot
    statics: from_json
    instance: into_json, manifest_list, operation, parent_snapshot_id, schema_id, sequence_number, snapshot_id, stable_hash, summary, timestamp_ms
  media.iceberg.ManifestFile
    instance: added_files_count, added_rows_count, added_snapshot_id, deleted_files_count, existing_files_count, existing_rows_count, is_data, length, partition_spec_id, path, sequence_number, stable_hash
  media.iceberg.DataFile
    instance: column_sizes, content, file_size_in_bytes, lower_bounds, mime_type, null_value_counts, partition, path, record_count, sort_order_id, split_offsets, stable_hash, upper_bounds, value_counts
  media.iceberg.ScanPlan
    instance: files_planned, files_skipped, manifests_read, manifests_skipped, record_count, stable_hash
  text.codec
    exports: from_io, from_stream, into_io, into_stream
    inference: explicit format -> source/destination suffix -> one native content sniff; anonymous output defaults to JSON
    parse options: field, cls, safe, errors, max_depth, max_input_bytes, max_nodes, max_documents
    output options: format, utf8 (into_io), indent; destination=None returns bytes or str, otherwise writes and returns None
  text.json
    exports: Buffer, Destination, Readable, Source, Writable, dump, dump_all, dumps, dumps_all, load, load_all, loads, loads_all
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes; dump_all requires a destination
    parse: load/loads/load_all/loads_all(source, *, cls=None, field=None, safe=True, errors="raise", max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None)
  text.toml
    exports: Buffer, Destination, Readable, Source, Writable, dump, dumps, load, loads
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes
    parse: load/loads(source, *, cls=None, field=None, safe=True, errors="raise", placeholders=None, environment=False, max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None)
  text.yaml
    exports: Buffer, Destination, Readable, Source, Writable, dump, dump_all, dumps, dumps_all, load, load_all, loads, loads_all
    output: dump(value, destination=None, *, utf8=False, indent=<format default>) -> bytes | str | None; dumps(value, *, indent=<format default>) -> bytes; dump_all requires a destination
    parse: load/loads(source, *, cls=None, field=None, safe=True, errors="raise", placeholders=None, environment=False, max_depth=None, max_input_bytes=None, max_nodes=None, max_documents=None); load_all/loads_all use the same native limits
  arrow
    exports: SHAPES, ArrowScalar
    ingestion: ArrowScalar.from_(value, field=None, *, safe=True, nullability="default", representation="value") reads a pyarrow Scalar/Array/ChunkedArray/RecordBatch/Table/RecordBatchReader/Dataset/Scanner, a pandas or polars frame or series, a numpy array, an Arrow C data or stream exporter, or any value a Scalar holds
    shapes: scalar, array, batch, stream; only a stream is one-shot
    redirects: IOBase.read_arrow / write_arrow
    Rust-only: ArrowScalar::from_batch_as, from_reader_as, from_values, from_rows, root, schema, as_array, as_batch
  hashing: txhash, xxhash; the one public path to both digest modules - yggdryl.xxhash and yggdryl.txhash do not exist
  hashing.xxhash
    exports: SECRET_MINIMUM_LENGTH, Digest, Digester, Xxh32, Xxh64, Xxh3, Xxh128, bits, column_digests, digest, is_secretable, is_seedable, row_digests, width, xxh32, xxh64, xxh3, xxh128
    input: str | bytes | bytearray | memoryview | any buffer; bytes and str are borrowed, every other buffer is read through one bounded window
    one-shot: xxh32(data, seed=0), xxh64(data, seed=0), xxh3(data, seed=0, secret=None), xxh128(data, seed=0, secret=None) -> int
    redirects: IOBase.read_digest / read_range_digest and Scalar.digest, algorithm defaulting to "xxh3-64"
    Rust-only: the reader/writer pair and the Hashed<H> handle
  hashing.txhash
    exports: DEFAULT_UNIT, UNIX_WIDTH, TxHash, TxHasher, column_txhashes, compose, decompose, digest, dtype, restate_unix, row_txhashes, txh32, txh64, txh3, txh128, unix_array, unix_now, unix_of, width
    instant: every unix argument is int | datetime | date | str | Scalar; an int is the count already, everything else crosses into a Scalar and takes the core's one intake, so a zoned datetime already counts from the epoch and a naive one reads as UTC
    one-shot: txh32(data, unix, seed=0), txh64(data, unix, seed=0), txh3(data, unix, seed=0, secret=None), txh128(data, unix, seed=0, secret=None) -> TxHash at microseconds
    columns: row_txhashes / column_txhashes / compose / decompose / unix_array take and answer pyarrow arrays, unit defaulting to "us" and the algorithm to "xxh3-64"
    redirects: field.digest.time / unit / is_coupled / remove_time / remove_unit on the digest view; TxHasher.apply_arrow_batch is Digester.apply_arrow_batch under the hasher's state
### javascript: yggdryl
  top-level
    statics: intoField
  field-class protocol
    statics: intoStructField (getter only; memoized; stored fields and methods rejected)
  Uri
    statics: from, fromPath, fromString, fromJSON
    instance: account, at, authority, bucket, clearExtensions, clone, compare, equals, extension, extensions, fileName, fragment, hostname, intoPath, intoUrl, intoUrn, isVirtualHosted, joinPath, key, length, mediaType, mimeType, password, path, pathSegments, query, region, removeExtension, scheme, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, storeEndpoint, toJSON, toString, user
  MediaType
    statics: from, fromContentHeaders, fromExtension, fromFileName, fromPath, fromString, fromJSON, fromParts, fromExtensions
    instance: at, base, charset, clearEncodings, clone, compare, contains, encoding, encodings, equals, extension, extensions, getEncoding, isApplication, isArchive, isAudio, isBinary, isEncoded, isEncoding, isFont, isHaptics, isImage, isIo, isMessage, isModel, isMultipart, isStructured, isTabular, isText, isTextual, isVideo, length, pushEncoding, setBase, setCharset, stableHash, toJSON, toString, setEncodings
  Url
    statics: from, fromPath, fromString, fromUri, fromJSON
    instance: account, asPosix, asUri, at, authority, bucket, clearExtensions, clone, compare, equals, exists, extension, extensions, fileName, fragment, fullMatch, hostname, intoPath, intoUri, isAbsolute, isDir, isFile, isGlob, isPrivate, isRelativeTo, isVirtualHosted, joinpath, length, match, mediaType, mimeType, name, parent, parents, partition, partitions, parts, password, path, pathSegments, query, region, relativeTo, removeExtension, scheme, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, storeEndpoint, suffix, suffixes, toJSON, toString, user, withName, withStem, withSuffix
  DataType
    statics: from, fromString, fromRegex, time, string, bytes, utf8, largeUtf8, utf8View, ascii, fixedUtf8, fixedAscii, binary, largeBinary, binaryView, fixedSizeBinary, fromLogicalName, logicalNames, fromJSON, fromJSONBytes, fromFields, variant, geometry, geography, fromArrow
    instance: asciiPacked, asciiValue, bytesParameters, charset, clone, codeWidth, compare, contains, defaultArrowScalar, defaultJSHint, defaultJSValue, equals, explodeFields, field, fieldAt, fieldByPath, fixedByteWidth, get, getField, getFieldAt, getFieldByPath, id, intoSchemeCompat, keys, kind, length, mergeWith, nested, removeField, removeFieldAt, removeFieldByPath, setField, setFieldAt, setFieldByPath, scalar, showDiff, showDiffs, stableHash, stringParameters, toJSON, toJSONBytes, toString, unnestFields, values
    stringParameters / bytesParameters: plain objects {layout, charset?, bound?, fixed?, max?} where layout is the leaf's canonical name (= the datatype id) and a key is absent when the leaf gives no such reading; DataType.string / DataType.bytes read the same shape as input, with at most one of bound/fixed/max
  StringEnum
    statics: fromJson, fromLogicalName, memberName, prebuilt
    instance: clone, equals, get, getMember, insert, intoEnum, intoJson, intoMembers, length, members, name, remove, toString
  Term
    statics: all, alwaysFalse, alwaysTrue, any, attribute, call, column, fromJson, literal, parameter, parse, typedLiteral
    instance: add, and, asColumn, at, attributes, between, bind, cast, checkBudget, child, clone, columns, compare, comparison, conjuncts, depth, divide, eq, equals, explain, field, ge, glob, gt, hasAttributes, ilike, intoJson, isAlwaysFalse, isAlwaysTrue, isIn, isLiteral, isNotNull, isNull, key, le, like, lt, multiply, ne, negate, nodeCount, not, or, parameters, remainder, simplify, slice, stableHash, subtract, toJSON, toString, tryCast
  Bound
    instance: columnIndices, columns, eval, explain, field, filterArrow, filterArrowBatch, filterArrowReader, filterArrowTable, isPredicate, matches, partitionSplit, readsRows, schema, term, toString
  Filter
    statics: all, alwaysFalse, alwaysTrue, any, fromJson, parse
    instance: and, applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyField, applyRecords, attributes, bind, checkBudget, clone, columns, compare, conjuncts, equals, explain, hasAttributes, intoJson, isAlwaysFalse, isAlwaysTrue, not, or, parameters, simplify, stableHash, term, toJSON, toString
  Selector
    statics: all, allExcept, fromColumns, fromField, fromJson, parse
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyField, applyRecords, attributes, bind, checkBudget, clone, columns, compare, equals, excluded, explain, intoField, intoJson, isAll, isColumns, length, names, parameters, projections, simplify, stableHash, toJSON, toString, withoutColumns, withProjection
  BoundSelector
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyRecords, applyRow, explain, isAll, isIdentity, output, projections, schema, toJSON, toString
  Plan
    statics: fromField, fromJson, parse
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyRecords, attributes, checkBudget, clone, columns, compare, createTarget, equals, execute, explain, field, fieldFrom, filter, intoExpression, intoJson, isEmpty, isIdentity, limit, mergeBy, offset, ordering, parameters, readColumns, readSections, rootMetadata, rootName, schema, selector, simplify, source, sourcePlan, stableHash, toJSON, toString, verb, withCreate, withFilter, withLimit, withMergeBy, withOffset, withOrdering, withSelect, withSource, withWrite, writeTarget
  Expression
    statics: filter, fromJson, parse, plan, select, sequence
    instance: applyArrow, applyArrowBatch, applyArrowReader, applyArrowTable, applyField, applyRecords, asFilter, asPlan, asSelector, attributes, checkBudget, clone, columns, compare, equals, explain, intoJson, isFilter, isIdentity, isPlan, isSelector, isSequence, kind, parameters, simplify, stableHash, steps, toJSON, toString
  Records
    statics: fromArrowReader
    instance: collect, field, intoArrowReader, next, Symbol.iterator, toString
  Bounds
    statics: fromPartitions
    instance: attribute, column, withAttribute, withColumn
    inputs: every Either-typed parameter is spelled out in the typings - a Term | string operand, a Filter | Term | string predicate, a Selector | Term | string | Array<Term | string> selector, a Plan | Selector | Filter | Field | string plan; the loader widens bind parameters and the Arrow holders (BatchReader, Table, RecordBatch) and applyRecords rows, and Records iterates as JavaScript does
    user functions: Term.call takes a qualified namespace.name and the parser reads it; nothing registers in JavaScript, so a bind refuses the function by name
  Urn
    statics: from, fromString, fromUri, fromJSON
    instance: at, authority, clearExtensions, clone, compare, equals, extension, extensions, fileName, fragment, intoUri, length, mediaType, mimeType, namespace, namespaceSpecific, path, pathSegments, query, removeExtension, scheme, setExtension, setExtensions, setFileName, setMediaType, setMimeType, setStem, stableHash, stem, toJSON, toString
  Field
    statics: from, fromString, fromJSON, fromJSONBytes, fromArrow
    instance: accept, acceptEncoding, acceptLanguage, acceptRanges, alias, arrow, az, cacheControl, cast, castArrow, castArrowArray, castArrowBatch, castArrowReader, clear, clearProperties, clone, comment, compare, contentDisposition, contentEncoding, contentLanguage, contentLength, contentLocation, contentRange, contentType, defaultArrowScalar, defaultJSHint, defaultJSValue, delete, dictionaryId, dictionaryIsOrdered, digest, digestFieldLen, digestFieldNames, digestFields, display, dtype, entries, equals, etag, expires, explodeFields, field, fieldAt, fieldByPath, fieldLen, fieldProperties, file, fix, get, getField, getFieldAt, getFieldByPath, getProperty, glue, gs, has, hasPartitionFields, hasProperty, http, httpLocation, iceberg, identity, indexOf, intoSchemeCompat, isPartition, keys, lastModified, location, mediaType, mergeWith, mimeType, mysql, name, nullable, onlyDigestFields, onlyPartitionFields, pandas, parquetFieldId, partition, partitionFieldLen, partitionFieldNames, partitionFields, polars, postgres, postgresql, propertyIter, protocol, python, range, removeAccept, removeAcceptEncoding, removeAcceptLanguage, removeAcceptRanges, removeAlias, removeCacheControl, removeComment, removeContentDisposition, removeContentEncoding, removeContentLanguage, removeContentLength, removeContentLocation, removeContentRange, removeContentType, removeDisplay, removeEtag, removeExpires, removeField, removeFieldAt, removeFieldByPath, removeHttpLocation, removeLastModified, removeLocation, removeMediaType, removeMimeType, removeParquetFieldId, removeProperty, removeRange, removeStringEnum, removeVary, s3, set, setAccept, setAcceptEncoding, setAcceptLanguage, setAcceptRanges, setAlias, setCacheControl, setComment, setContentDisposition, setContentEncoding, setContentLanguage, setContentLength, setContentLocation, setContentRange, setContentType, setDictionaryOptions, setDisplay, setDtype, setEtag, setExpires, setField, setFieldAt, setFieldByPath, setHttpLocation, setLastModified, setLocation, setMediaType, setMimeType, setName, setNullable, setParquetFieldId, setPartition, setProperty, setRange, setStringEnum, setVary, scalar, showDiff, showDiffs, size, spark, sql, stableHash, stringEnum, toJSON, toJSONBytes, toString, unnestFields, update, urn, values, vary, withPartition, withPartitionFields, withoutPartitionFields
  ProtocolField
    instance: addBranch, branches, clear, codeName, codeValue, comment, delete, derivation, description, directions, display, entries, get, has, hasBranch, id, identifiers, key, keys, mergeWith, names, nulls, prefix, scheme, set, size, tag, tags, toJSON, toString, update, values
    typed fix vocabulary: branches, tag, tags, names, identifiers, description, nulls, directions, derivation are accessor pairs, id a read-only accessor (number | null, the i32 digest of the tag and the folded name, derived on every read), addBranch(name)/hasBranch(name) methods, all answered only by the `fix` view; every other scheme throws a TypeError naming its own scheme; branches crosses as string[] (sorted, lowercase, [] when absent; assigning [] removes FIX:branches); directions crosses as FixDirection[] ({code: string, patterns: string[]}, [] when absent; assigning [] removes FIX:directions); derivation crosses as string | null (the FIX:derivation term in its canonical text, null when absent; assigning text parses it through the expression grammar and stores it canonical, a text that is not a term throws leaving the field unchanged; assigning null removes FIX:derivation)
    FIX tags: tag, tags and counter admit positive i32 tags only - 0 (what an unresolved arrival entry records) or a negative throws the core's refusal, written or read back from stored metadata
  MimeType
    statics: from, fromContentCoding, fromContentType, fromExtension, fromPath, fromString, fromJSON, OCTET_STREAM, JSON, JSON_LINES, YAML, TOML, CSV, TSV, PARQUET, ARROW_FILE, ARROW_STREAM, AVRO, ORC, PUFFIN, PLAIN_TEXT, MARKDOWN, HTML, CSS, JAVASCRIPT, XML, PDF, CBOR, MESSAGE_PACK, PROTOBUF, SQLITE3, PNG, JPEG, GIF, WEBP, SVG, MP3, WAV, OGG, FLAC, MP4, WEBM, WOFF, WOFF2, TTF, OTF, XLS, XLSX, ODS, DOC, DOCX, GZIP, ZSTD, BROTLI, ZLIB, COMPRESS, BZIP2, XZ, LZ4, SNAPPY, ZIP, SEVEN_ZIP, RAR, TAR
    instance: clone, compare, contentCoding, equals, extension, format, isApplication, isArchive, isAudio, isBinary, isEncoding, isFont, isHaptics, isImage, isIo, isKnown, isMessage, isModel, isMultipart, isStructured, isTabular, isText, isTextual, isVideo, stableHash, structuredSuffix, subtype, toJSON, toString, topLevel
  IOBase
    statics: from, fromBytes, fromFs, fromUri
    instance: appendArrowBatch, appendArrowReader, appendArrowTable, appendBytes, appendRecords, buffered, childrenMatching, childrenWhere, clear, close, closed, codec, columnSize, compressInto, copyInto, createDir, cursor, decompressInto, deleteDir, deleteDirContents, deleteFile, deleteRootDirContents, exists, filesystem, flush, glob, info, intoPath, intoText, isAtomic, isDir, isFile, isIo, isTabular, iterdir, joinpath, kind, ls, maskedUri, mediaType, mergeArrowBatch, mergeArrowReader, mergeArrowTable, mergeRecords, mkdir, moveInto, name, normalizePath, open, openAppendStream, opened, openInputFile, openInputStream, openOutputStream, overwriteArrowBatch, overwriteArrowReader, overwriteArrowTable, overwriteRecords, parent, partitions, path, pstreamBytes, pwrite, readArrowField, readArrowReader, readBytes, readDigest, readParquetGeospatialStatistics, readParquetStatistics, readRange, readRangeBytes, readRangeDigest, readRecords, readScalar, readText, readTextLines, recordOptions, remove, rglob, rowSize, sameLocation, size, toString, touch, truncate, unlink, uri, url, writeArrowBatch, writeArrowReader, writeArrowTable, writeBytes, writeRecords, writeScalar, writeText
    properties: every record read and write takes an options value and, beside it, a plain object of option properties - or the plain object alone in the options position - set on a copy by their own setters; undefined is skipped; readRecords(cls?, options?, properties?); recordOptions() answers TextOptions for a plain-text handle
  IOCursor
    instance: flush, position, read, seek, streamBytes, tell, write
  ByteIterator
    instance: next, Symbol.iterator
  BatchReader
    statics: fromIpc, from
    instance: combined, consumed, field, intoIpc, intoTable
  RecordOptions
    statics: forMediaType, forMimeType, from
    instance: batchRowSize, blockCodec, clone, commitRowSize, compare, compression, equals, field, filter, keyValueMetadata, level, maxByteSize, maxRowGroupSize, maxRowSize, mergeBy, mimeType, name, partitionPairs, plan, safe, select, stableHash, syncMarker, timezone, toString, withBatchRowSize, withBlockCodec, withCommitRowSize, withCompression, withField, withFilter, withKeyValue, withLevel, withMaxByteSize, withMaxRowGroupSize, withMaxRowSize, withMergeBy, withName, withPlan, withSafe, withSelect, withSyncMarker
    sections: field, filter, select, mergeBy and plan are read/write properties, the setters taking a Selector | Term | string | Array<Term | string> (filter: Filter | Term | string; plan: Plan | Selector | Filter | Field | string); partitionPairs() answers the equalities the filter pins
  TextOptions
    instance: autotype, batchRowSize, clone, commitRowSize, compare, equals, field, filter, framing, leadingFragment, level, liftNames, linesep, lstrip, maxByteSize, maxRecordByteSize, maxRowSize, mergeBy, mimeType, name, parseMtime, partitionPairs, plan, renameColumns, rowheader, rstrip, safe, select, sourceField, stableHash, startRownum, timezone, toString, withBatchRowSize, withCommitRowSize, withField, withFilter, withLevel, withMaxByteSize, withMaxRowSize, withMergeBy, withName, withPlan, withSafe, withSelect
  FieldPath
    statics: root
    instance: equals, isRoot, join, length, name, parent, segments, stableHash, toString
  TextEntries
    instance: at, entryByPath, getEntryByPath, length, toArray, toString
  TextEntry
    instance: entries, key, keyBytes, toString, value, valueBytes
  TextLine
    constructor: new TextLine(index: number, body: string | Buffer, captures?: Array<string | null>)
    constructor: new TextLine(index: number, body: string | Buffer, captures?: Array<string | null> | null, options?: TextOptions), the body the whole line, row header included, read under the options
    instance: body, bodytype: string, captures, crosscode: string, crosshashcode: bigint, crossuuid: string, currhashcode: bigint, currunix: bigint, curruuid: string, decodedByteSize, droppedByteSize, entries, entryByPath, getEntryByPath, index, mtime: bigint | null (the captured instant, else the handle's modification time), removeEntryByPath, setEntryByPath, sourceurl, toString
    Rust-only: the TextLine Arrow converters media::text::into_arrow_batch, into_arrow_reader, from_arrow_batch, from_arrow_reader (no binding)
  TextLineIterator
    instance: next; IOBase.readTextLines(options?) answers one
  Timezone
    statics: UTC, aliases, from, fromOffset, fromString, registered
    instance: abbreviationAt, clone, compare, equals, getTimezoneOffset, intoLocal, intoUtc, isFixed, isKnown, isSavingAt, isUtc, key, observesSaving, offsetAt, stableHash, standardOffset, toJSON, toString
  Version
    constructor: Version(major: number, minor?: number | null, patch?: number | null); numeric widths u8/u8/u16
    statics: fromStr(text: string) -> Version
    readonly: major: number, minor: number, patch: number
    instance: equals(other: Version) -> boolean; compare(other: Version) -> number; stableHash() -> bigint; clone() -> Version; toString()/toJSON() -> string
    typed field: fields.version(name, options?) -> VersionField; native JavaScript value Version, native Arrow value canonical version text
  Scalar
    statics: decimal, duration, float, from, fromArrowArray, fromArrowBatch, fromArrowScalar, fromArrowTable, fromEnum, fromVariantBytes
    instance: absolute, add, asBytes, asJs, asStr, at, clone, compare, count, digest, divide, dtype, equals, family, get, has, id, intoArrayField, intoArrowArray, intoArrowBatch, intoArrowScalar, intoArrowTable, intoField, intoJson, intoJsonBytes, intoStructField, intoVariantBytes, isEmpty, isTruthy, kind, length, multiply, negate, path, remainder, remove, scale, set, stableHash, subtract, Symbol.iterator, toJSON, toString, unit, unscaled, zone
    from(value, options?): optional options.field redirects to native Field.scalar once for validation and canonicalization; a Term, Filter, Selector, Plan or Expression crosses as its canonical text
  Digest
    statics: from, fromBytes
    instance: algorithm, bits, bytes, clone, compare, equals, stableHash, toJSON, toString, value, width
  Xxh32
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, seed, writeBytes, writeScalar
  Xxh64
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, seed, writeBytes, writeScalar
  Xxh3
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, secret, seed, writeBytes, writeScalar
  Xxh128
    instance: algorithm, applyArrowBatch, asDigest, clear, clone, secret, seed, writeBytes, writeScalar
  hashing namespace
    values: xxhash, txhash; one frozen object, the one public path to both families - there is no top-level xxhash or txhash export
  hashing.xxhash namespace
    values: SECRET_MINIMUM_LENGTH, Digest, Xxh32, Xxh64, Xxh3, Xxh128
    functions: digest, xxh32, xxh64, xxh3, xxh128
  TxHash
    statics: from, fromBytes, fromParts
    instance: algorithm, bytes, clone, compare, digest, dtype, equals, intoDatetime, intoScalar, intoUuid, stableHash, toJSON, toString, unit, unix, width, withUnit
    intoUuid() -> Scalar: the lossy RFC 9562 UUIDv7 projection (the instant restated to signed nanoseconds and floored to the microsecond, then the digest's low 62 bits; neither unit nor algorithm survives) as a uuid-typed Scalar, because the binding has no Uuid wrapper class - asJs() answers the hyphenated string; a digest that is not 64 bits wide, an instant past signed 64-bit nanoseconds, or one a UUIDv7 cannot hold, throws
  TxHasher
    statics: fromState
    instance: algorithm, applyArrowBatch, digest, digestScalar, dtype, unit, unixOf, width, clone
  hashing.txhash namespace
    values: DEFAULT_UNIT, UNIX_WIDTH, TxHash, TxHasher
    functions: digest, dtype, restateUnix, txh32, txh64, txh3, txh128, unixNow, unixOf, width
    instant: every unix argument is bigint | number | Date | string | Scalar; a bigint or an integer number is the count already, a Date is its UTC millisecond instant, a string is timestamp text, and a Scalar holds any of those
    Rust and Python only: the coupled columns (row_txhashes, column_txhashes, compose, decompose, unix_array)
  AvroSchema
    statics: from
    instance: canonicalForm, clone, compare, equals, fingerprint, fromSingleObject, intoCanonicalForm, intoJSON, intoSingleObject, kind, stableHash, toJSON, toString
  AvroDecodeLimits
    fields: maxDepth, maxInputBytes, maxNodes
  AvroDecodeOptions
    fields: maxDepth, maxInputBytes, maxNodes, readerSchema
  AvroBlocks
    instance: get, metadata, next, schema, Symbol.iterator
  AvroBlock
    instance: count, rows, size
  avro
    statics: Schema, blocks, dumps, dumpsSingle, loads, loadsSingle
    decode limits: maxDepth, maxInputBytes, maxNodes; blocks is a fused lazy iterator
  fix
    values: FixRegistry, FixMsg, FixCodec, MsgType, FixMessages
    functions: schema(registry?: FixRegistry | null, name?: string | null) -> Field; schemaCarrying(carrier: Field, read: Field) -> Field; schemaTags() -> number[]; crateFields() -> Field[]; globalRegistry() -> FixRegistry; installGlobalRegistry(registry: FixRegistry) -> void
    plain objects (generated interfaces, every absent fact null): FixEventView {curruuid: string, crossuuid: string, crosscode: string, currhashcode: bigint, crosshashcode: bigint, identifiers: Record<string, string>, parentuuids: string[], srcuuids: string[], currunix: bigint, state: string, seqnum: number, creaunix: bigint | null, expirunix: bigint | null, prevunix: bigint | null, prevuuid: string | null, snapunix: bigint | null, px: string, qty: string, currency: string, unit: string, side: string, isincode/cusipcode/sedolcode/bloombergcode/cficode/miccode: string | null, bidpx/bidqty/bidcurrency/bidunit/askpx/askqty/askcurrency/askunit: string | null}; FixHeaderView {beginstring: string, msgtype: string, sendercompid: string | null, targetcompid: string | null, msgseqnum: number | null, sendingtime: bigint, possdupflag: boolean | null, msgdirection: string | null, signaturelength: number | null, signature: Buffer | null, checksum: string | null}; FixCaptureView {msgpluginid: string | null, msgctxid: string | null, msgsessionid: string | null}; FixEntryView {tag: number, name: string, value: string | null, entries: FixEntryView[]}; a UUID crosses as its hyphenated text, a hash and an instant (nanoseconds since the epoch, UTC) as a bigint, a price or a quantity as its decimal text, a code as the text it is
  fix.FixRegistry
    constructor: FixRegistry()
    statics: fromFields(fields: Field[]) -> FixRegistry; fromHandle(location: IOBase | Url | string) -> FixRegistry; fromCfbFile(location: IOBase | Url | string, dialect?: string | null) -> [FixRegistry, Field[]], every produced field stamped with the dialect on FIX:branches; fromJson(input: string) -> FixRegistry
    lookup: getFieldById(id: number) -> Field | null; fieldById(id: number) -> Field; getFieldByTag(tag: number) -> Field | null; fieldByTag(tag: number) -> Field; getFieldByCounter(tag: number) -> Field | null; fieldByCounter(tag: number) -> Field, the repeating group a counter tag opens, nothing where two groups share one; getFieldByName(name: string) -> Field | null; fieldByName(name: string) -> Field, a scalar, then a component, then a group; getFieldByPath(path: string | FieldPath) -> Field | null; fieldByPath(path: string | FieldPath) -> Field; getField/get(key: number | string) -> Field | null; field(key: number | string) -> Field; has(key: number | string) -> boolean
    mutation, every definition through the field doors: addField(field: Field) -> boolean; insert(field: Field) -> Field | null, a Struct as a component (a message when it carries FIX:msgtype), a List of Structs or a Map as a group, else a scalar; update(field: Field) -> void, into the scalar of the same identity or the component/group of the same folded name; remove(key: number | string) -> Field | null, a name no scalar answers to reaching a component or a group, null for one still referenced; removeById(id: number) -> Field | null; addFields/mergeWith/addCfbFile/addCfbFiles/addJsonFile are Python-only
    messages: registerMsgtype(spelling: string, name?: string | null, description?: string | null) -> MsgType; getMsgtype(spelling: string) -> MsgType | null; msgtype(spelling: string) -> MsgType; each answers a copy of the registry's own definition
    membership: dialects() -> string[], the distinct FIX:branches names any field or definition carries, sorted; provenance a caller filters on, never a lookup tier
    snapshot: intoJson() -> string; toJSON() -> native snapshot object containing fields/components/groups; writeInto(location: IOBase | Url | string) -> void, shards named by tag block as nine digits (fields/000000000.json, fields/000000650.json), the crate's own identifiers/metadata groups and the fixmsg component (the fixed row) written like every other
    protocol: readonly size: number, the scalars, the components and the groups; keys()/Symbol.iterator -> Generator<Field>, the scalars tag-major then identifier, then the components and the groups in name order (which category a definition is filed under is toJSON()'s three listings, never a shape read off the datatype: the crate's parentuuids and srcuuids lists are fields, its identifiers and metadata Maps are groups); equals(other: FixRegistry) -> boolean; stableHash() -> bigint; clone() -> FixRegistry; toString() -> string; native equality/hash include all categories; mutations refuse while a message, default registry or unfinished native walk shares the registry
    keys: a number tag (exact i32) or string name/dotted path; one namespace, canonical names/tags before aliases/alternate tags, the fold always; an identifier is the exact i32 digest field.fix.id answers and is only spelled through the ById doors (a bare number elsewhere is a tag); malformed input raises the native located error
  fix.MsgType
    readonly: name: string
    instance: asStr() -> string; asField() -> Field; identifierValues(message: FixMsg) -> Array<[Field, Scalar]>; getGroupByTag(tag: number) -> Field | null; equals(other: MsgType) -> boolean; compare(other: MsgType) -> number; stableHash() -> bigint; clone() -> MsgType; toString() -> string; toJSON() -> native Field JSON
    protocol: an immutable copy of the registry's message definition; complete case-sensitive wire code; asField and identifierValues project independent mutable native Field wrappers; identifierValues redirects to the compiled native selection and retains each Scalar's exact datatype
  FIX iterators
    FixMessages: IterableIterator<FixMsg>; next() -> IteratorResult<FixMsg>; no collection on the binding path
    FixMessages is what every FixCodec stage answers - one line's messages, none, one or many a row (one per frame the line carries, one named unknown for a row whose payload is a JSON document, none for a line carrying no message), lines or records parsed, messages walked through a lifecycle, a batch read back; a JavaScript iterable behind it is pulled one item at a time through a loader-bound pull function; a refused line throws where it is met and the stream continues, a failure of the iterable throws as itself and ends it
  fix.FixMsg
    constructor: FixMsg(field: Field, value: Scalar | Record<string, unknown> | readonly unknown[], registry?: FixRegistry | null); a child stating a typed fact - a header or trailer tag (8, 35, 49, 56, 34, 52, 43, 385, 93, 89, 10), a crate column (65000-65100) but sourceurl (65026), one of the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003}, Text(58) - fills the holder that owns it and leaves the row; SendingTime reads UTC now when the value states none; the identity is settled
    statics: fromRow(schema: Field, row: Scalar | Record<string, unknown> | readonly unknown[], registry?: FixRegistry | null) -> FixMsg, the inverse of intoRow: the typed facts read off their columns, the content rebuilt from the fixentries column, every capture column carried under its own name and held as no fact, no clock read
    readonly: registry: FixRegistry, field: Field (the content row's schema), value: Scalar (the content row), size: number (the row's children), text: string | null (Text(58)), metadata: Record<string, string> (a bridge's namespaced keys, sorted)
    typed holders, plain objects read once: event() -> FixEventView; header() -> FixHeaderView; capture() -> FixCaptureView
    graph getters: curruuid: string; crossuuid: string; crosscode: string; currhashcode: bigint; crosshashcode: bigint; currunix: bigint; state: string; seqnum: number; prevuuid: string | null; parentuuids: string[] (the whole chain before it, oldest first); srcuuids: string[] (the text line it was parsed out of; none for bytes); carried: Record<string, Scalar> (the capture's own cells the message carries, each under its column; empty for bytes); identifiers: Record<string, string>; px: string; qty: string; side: string; currency: string; isincode: string | null; cusipcode: string | null; sedolcode: string | null; bloombergcode: string | null; cficode: string | null; miccode: string | null (the last six read off FIX's own fields and answered by no column)
    writes: set(key: number | string, value: unknown) -> void, the value widened through Scalar.fromJs; a key reaching a typed fact records it on its holder (null clears it), a key reaching the capture's own column - sourceurl - throws, because no message holds it, any other key lands in the row typed by the field it resolves to, null stored as a stated null, an existing child replaced where it stands and an absent one appended; remove(key: number | string) -> Scalar | null, a typed fact cleared on its holder, a row child removed, null for a key reaching nothing; every write settles the identity again
    lookup, answering a Scalar: getById(id: number) -> Scalar | null; byId(id: number) -> Scalar; getByTag(tag: number) -> Scalar | null; byTag(tag: number) -> Scalar; getByName(name: string) -> Scalar | null; byName(name: string) -> Scalar; getByPath(path: string | FieldPath) -> Scalar | null; byPath(path: string | FieldPath) -> Scalar; get(key: number | string) -> Scalar | null; at(key: number | string) -> Scalar; a typed tag answers its holder's fact as the Scalar its column types (byTag(35) the type's text, byTag(52) datetime64(ns, UTC), byTag(44) a decimal128(38, 18), byTag(65039) a uuid, byTag(65017) a uint64), any other key reaches the row, Side(54) and Currency(15) among it
    projection: entries() -> FixEntryView[], the content row read as a tree, derived on the first ask and kept until a write, a group stating its count once on the entry that heads it; intoRow(field: Field) -> Scalar, each typed fact from its holder and a lane the message only settled - a sending clock it never stated - left to its column's null; intoBytes(separator?: number | null) -> Buffer, the header (52 only when stated), the lifted FIX tags {6,11,14,17,31,32,37,38,41,44,53,117,131,151,198,262,1003} in that order, the entries, then the trailer 93, 89, 10, coded facts as their wire code; intoText(separator?: string | null) -> string, the same as text under one separator character (SOH unstated); digest() -> Buffer, sixteen bytes over the wire entries
    protocol: Symbol.iterator -> the entries; equals(other: FixMsg) -> boolean; stableHash() -> bigint; clone() -> FixMsg; toString() -> string; toJSON() -> {field, value}, the content row's two documents; native facts+schema+row equality/hash
  fix.FixCodec
    constructor: FixCodec(registry?: FixRegistry | null, options?: {separator?: number, payloadColumn?: string, captureNames?: string[], nullValues?: string[], direction?: string, batchByteSize?: number, batchRowSize?: number, threads?: number, includeMsgtypes?: string[], excludeMsgtypes?: string[], defaultSendingTime?: Scalar | Date | null} | null); defaultSendingTime redirects to try_with_default_sending_time - a Scalar crosses as itself and must already be DateTime64(ns, UTC), a Date is its UTC millisecond instant restated in nanoseconds, any other layout throws the core's refusal; direction is any spelling of a code of tag 385's set, resolved once - the core's Send code when unstated, no pin when '' - and a spelling outside the set throws naming the set; a pin is on the codec, a stage is a call
    readonly: registry: FixRegistry; defaultSendingTime: Scalar | null, the SendingTime a new message takes when neither it nor its carrier states one - null reads UTC now once per such message, so only a pinned default makes a parse of undated bytes repeatable; a line's own timestamp is capture context and stamps nothing; separator: number | null; payloadColumn: string; nullValues: string[]; direction: string | null, the code of tag 385's set an unmarked line takes on the batch door - a line is read by the tag-385 field's FIX:directions, compiled once when the codec is built; batchByteSize: number, the core's 128 MiB when unstated; batchRowSize: number, the core's 32,768 when unstated, a batch closing on whichever bound it reaches first; threads: number, 1 when unstated - the threads the line and row doors read on, more reading a stream a chunk ahead, each line on some thread, answered in the lines' order, zero reading as one; includeMsgtypes: string[]; excludeMsgtypes: string[], the core's Heartbeat, TestRequest and 'unknown' when unstated, each read before a frame is built and spelled as a code or a name, and an empty excludeMsgtypes keeps every type
    statics: inferMsgtypeBytes(body: Buffer) -> Buffer | null; inferMsgtypeText(body: string) -> string | null
    parsing, streams: parseLine(row: Buffer) -> FixMessages; parseLines(lines: Iterable<string | ArrayBufferLike | ArrayBufferView>) -> FixMessages, lazy; parseTextLine(line: TextLine) -> FixMessages; parseTextLines(lines: Iterable<TextLine>) -> FixMessages, lazy
    parsing, one message: parseFixLine(body: Buffer) -> FixMsg; parseUllinkLine(body: Buffer) -> FixMsg; parseFixmlLine(body: Buffer) -> FixMsg; parsePairs(pairs: Array<[string, string]>) -> FixMsg; each takes one frame, and a body holding a second throws naming the byte that second opens at - 'expected one frame, got a second' - because a caller holding two holds a row, which is what parseLine reads; every parse settles the message whole - typed facts lifted, a nested XmlData exploded, deprecated fields restated to their latest aliases, the dictionary's FIX:derivation rules run, identifiers and order lanes filled, the identity derived - so there is no enriching pass
    lifecycle: lifecycle(messages: Iterable<FixMsg>) -> FixMessages, lazy, the one walk over events - the messages collected, sorted by instant, each stated as the one after the live message it follows (prevuuid, prevunix, seqnum, the whole chain before it as parentuuids, the chain's creaunix carried) and settled again; a message no live one precedes is answered as it came; srcuuids never travel along a chain
    formatting: formatMessages(messages: Iterable<FixMsg>, field: Field) -> Scalar[], the third verb - each message as the row one message field of the registry holds it, a column that field carries and the message does not lifted off its content record first, so a narrow row formats into a wider one; the target is a venue's own message type, fix.schema() itself, which keeps every column a capture lands in, or any Struct root a caller built
    Arrow twins, over whatever BatchReader.from accepts, answering BatchReader: parseTextArrowReader(source) -> capture batches to batches of FIX rows, carried columns leading, batches closed on the bytes each row lands as against batchByteSize; lifecycleArrowReader(source) -> the same schema in and out, rows walked as one lifecycle, each row's capture cells carried by its message through the walk's own ordering and stated again at their own columns; formatArrowReader(source, field: Field) -> the format twin, batches under field; a source carrying no content record is cast column for column instead of read back as messages; messages(source) -> FixMessages, lazy, one batch held at a time, each row's own cells carried into its message, so an arrowReader over them states the capture's own columns again; arrowReader(schema: Field, messages: Iterable<FixMsg>) -> BatchReader, batches closed on the bytes each row lands as, an item that is not a message is the reader's error; writeArrowReader(source, sink: { write(chunk: Uint8Array): unknown }) -> number of lines written, the codec's separator else SOH then a newline, refused before a row when the fixentries column is absent; a source reader is consumed
    instance: clone() -> FixCodec; toString() -> string
  ProtocolField FIX category metadata
    writable: counter: number; component/group/fieldRef/msgtype: string; getters return null when absent; identifiers: string[]; remove(key) clears metadata using the protocol view
    invariant: counter validates exact i32 positive FIX tag, reference names follow native catalog naming rules, codes remain inline FIX:codes, direction rules remain inline FIX:directions on tag 385 and cross as a typed list; identifiers accepts an array of member names/aliases/decimal tags, resolves once to direct scalar names in component order, replaces whole, clears on [], and refuses invalid input atomically through the core
  iceberg
    statics: Table, Catalog, Compaction, IcebergOptions, ManifestFile, PartitionField, PartitionSpec, DataFile, ScanPlan, Snapshot, SnapshotRef, assignFieldIds, canPromote, schemaFromJson, schemaIntoJson
  iceberg.IcebergOptions
    instance: clone, compactAfterCommits, commitMaxBackoffMs, commitMinBackoffMs, commitRetries, compare, dataMimeType, equals, readParallelism, readParallelMinFileSize, readParallelMinFiles, stableHash, targetFileSize, writeParallelism, writeStaging
  iceberg.Catalog
    instance: append, namespace, namespaces, overwrite, properties, table, tables, updateProperties
  iceberg.Namespace
    instance: name, namespaces, properties, tables, updateProperties
  iceberg.Namespaces
    instance: create, entries, get, has, keys, names, openOrCreate, size, values
  iceberg.Tables
    instance: append, create, entries, get, has, keys, names, openOrCreate, overwrite, size, values
  iceberg.Table
    statics: create, open, openOrCreate
    instance: append, compact, createBranch, createTag, currentSnapshot, dataFiles, evolveSchema, expireSnapshots, fastForward, formatVersion, inspectFiles, inspectHistory, inspectSnapshots, location, manifests, manifestsAt, merge, mergeWhere, metadataFileName, metadataLocation, options, overwrite, overwriteWhere, plan, planAt, planMatching, properties, removeRef, root, scan, scanAt, scanMatching, scanRef, scanWhere, schema, schemas, setOptions, snapshotByRef, snapshots, spec, tableUuid, targetFileSize, toString, updateProperties, updateSchema, version
  iceberg.SchemaUpdate (via table.updateSchema())
    instance: addColumn, commit, dropColumn, makeNullable, renameColumn, updateDoc, updateType
  iceberg.PartitionSpec
    statics: fromJSON, identity, unpartitioned
    instance: clone, compare, equals, fields, intoJSON, isUnpartitioned, specId, stableHash, toJSON
  iceberg.PartitionField
    instance: clone, compare, equals, fieldId, name, sourceId, stableHash, transform
  iceberg.Snapshot
    instance: addedRows, clone, compare, equals, firstRowId, manifestList, operation, parentSnapshotId, schemaId, sequenceNumber, snapshotId, stableHash, summary, timestampMs
  iceberg.SnapshotRef
    instance: clone, compare, equals, kind, maxRefAgeMs, maxSnapshotAgeMs, minSnapshotsToKeep, snapshotId, stableHash
  iceberg.ManifestFile
    instance: addedFilesCount, addedRowsCount, addedSnapshotId, clone, compare, content, deletedFilesCount, deletedRowsCount, equals, existingFilesCount, existingRowsCount, firstRowId, manifestLength, manifestPath, minSequenceNumber, partitionSpecId, partitions, sequenceNumber, stableHash
  iceberg.DataFile
    instance: clone, columnSizes, compare, content, equals, filePath, fileSizeInBytes, lowerBounds, mimeType, nullValueCounts, partition, partitionNames, recordCount, sortOrderId, stableHash, toString, upperBounds, valueCounts
  iceberg.ScanPlan
    instance: clone, compare, equals, filesPlanned, filesSkipped, manifestsRead, manifestsSkipped, recordCount, stableHash
  iceberg.Compaction
    instance: bytesRewritten, clone, compare, equals, filesAfter, filesBefore, stableHash
  codec
    statics: from, into, fromStream, intoStream
    options: format, field, maxDepth, maxInputBytes, maxNodes, maxDocuments, indent, value
  fields
    statics: null, boolean, int8, int16, int32, int64, uint8, uint16, uint32, uint64, float16, float32, float64, datetime64, date32, date64, time, time32, time64, duration32, duration64, interval, bytes, binary, fixedSizeBinary, largeBinary, binaryView, largeBinaryView, sizedBinary, string, utf8, largeUtf8, utf8View, largeUtf8View, fixedUtf8, sizedUtf8, ascii, largeAscii, asciiView, largeAsciiView, fixedAscii, sizedAscii, cp1252, largeCp1252, cp1252View, largeCp1252View, fixedCp1252, sizedCp1252, uuid, version, url, urn, country, currency, mic, cfi, isin, cusip, sedol, bloomberg, side, state, timeinforce, list, listView, fixedSizeList, largeList, largeListView, struct, union, denseUnion, variant, geometry, geography, dictionary, decimal, decimal32, decimal64, decimal128, decimal256, map, mapOf, runEndEncoded
    string(name, {layout?, charset?, bound?, fixed?, max?, ...field options}) / bytes(name, {layout?, bound?, fixed?, max?, ...field options}): the parameter keys build the datatype through DataType.string / DataType.bytes, every other key is a field option; one factory per string leaf - utf8, largeUtf8, utf8View, largeUtf8View, ascii, largeAscii, asciiView, largeAsciiView, cp1252, largeCp1252, cp1252View, largeCp1252View (name, value?), fixedUtf8 / fixedAscii / fixedCp1252 (name, width, value?), sizedUtf8 / sizedAscii / sizedCp1252 (name, max, value?) - and per byte leaf: fixedSizeBinary(name, byteWidth, value?), sizedBinary(name, max, value?)
  charset: CHARSETS, bom, canonicalName, decode, decodeLossy, encode, fromBom
  enums: charsets, codecs, compatibilitySchemes, dataTypeIds, dataTypeKinds, ioKinds, levels, pythonKinds, timeUnits, unionModes, ioModes
  json
    statics: loads, load, dumps, dump, loadStream, dumpStream, loadsAll, loadAll, dumpAll, loadAllStream, dumpAllStream
  toml
    statics: loads, load, dumps, dump, loadStream, dumpStream
  yaml
    statics: loads, load, dumps, dump, loadStream, dumpStream, loadsAll, loadAll, dumpAll, loadAllStream, dumpAllStream
